# Changelog

All notable changes to this project will be documented in this file, starting with version 1.0.0 and recent changes involved just before that release.

## [Unreleased]

### Added
- **Server-sim RendererBridge.runUpdate is now callable end-to-end on a live session**. Implemented the previously-stubbed `runUpdate(session, dt)` per its docstring intent (sync world→globals, call `pre(dt)`, conditionally call `mid(dt, isHitStopped)`, sync globals→world) plus two new exported helpers (`syncWorldToGlobals`, `syncGlobalsToWorld`) that mirror the renderer's client-side `window.X = …` bridge block from `game.js`. `syncWorldToGlobals(session)` writes `global.arena`, `global.player`, `global.player2`, `global.frame`, `global.wave`, `global.score`, `global.bossActive`, `global.saveData`, `global.HERO_LOGIC`, `global.ENEMY_LOGIC`, plus the same fields on `global.runState` (player, player2, frame, wave, score, gameRunning, gamePaused, isVersusMode, isCoopMode, bossActive, currentWeather, currentObjective, activeMutators) so leaf-module bare-name reads resolve to this session's data. `syncGlobalsToWorld(session)` pulls back the `frame`/`wave`/`score`/`bossActive` mutations the helpers wrote during the tick. **Important caveat documented in the runUpdate JSDoc:** this is **not** a drop-in replacement for `GameSession._tick()`. GameSession owns damage authority via `_processMeleeAttacks` / `_updateProjectiles` / `_applyEnemyContactDamage` — those paths apply real server-authoritative HP mutation. The leaf modules from `core/*.js` execute their own collision passes, but their `applyDamage` / `createExplosion` helpers go through loader.js stubs that are intentionally lossy server-side (smoke-test grade, not parity grade). Wiring `runUpdate` into `_tick()` directly would break damage authority. Use it for smoke + parity testing + future server-renderer parity validation; defer driving it from `_tick` until the damage authority migrates to the leaf modules' ECS-read path. **New parityTest Test 10** asserts `bridge.runUpdate(gs, 1000/60)` runs cleanly against a session warmed up with 30 legacy ticks (player input, spawned enemies, fired projectiles) — verifies the call returns true, session state doesn't regress (`world.frame`, `wave`), and the snapshot path still works afterwards (`gs._sendSnapshot()` produces a valid `SNAPSHOT` with `wave: number` + `enemies: array`). **ParityTest: 87/87 → 93/93 passing.** Build green, 84/84 Vitest, lint 95/95 baseline.

### Fixed
- **Server-sim RendererBridge `pre(dt)` + `mid(dt, hitstop)` invocations now run end-to-end without throwing**. Follow-on from the earlier RendererBridge smoke test (Test 9). After fixing the `BASE_HERO_STATS` overwrite + `window.addEventListener` stub, the next blockers were missing global stubs in `server/simulation/loader.js` for every bare-name helper the extracted leaf modules (`core/updateGameplayPre.js`, `core/updateGameplayMid.js`) reach. Added ~30 no-op stubs in a new "Section 10" of loader.js: boss-cinematic helpers (`_renderBossIntroCinematic` / `_renderBossDeathCinematic` / `_renderBossChoiceScreen` return `false` so the cinematic gate falls through to the normal update path); FX + camera-mode helpers (`triggerHitStop`, `triggerImpact`, `triggerScreenShake`, `triggerVibration`, `applyScreenShake`, `isPhotoMode`, `isReducedMotion`, `isChaosActive`); UI side-effects (`showNotification`, `updateDrawRevivalMarkers`, `drawCoopDistanceWarning`, `createDeathBurst`, `_renderMinimap`); story/wave-flow helpers (`triggerStory`, `advanceWave`, `notifyWaveAdvance`, `updateChaosObjective`, `checkChaosEvent`, `isStoryBossWave`, `isWaveCleared`, `gameOver`); coop/decoy helpers (`getDecoyTarget`, `getCoopTarget`, `getHeroTheme`); damage path (`applyDamage` with a minimal `target.hp -= dmg` fallback so the leaf module's collision pass doesn't no-op silently, plus `recordPlayerDamage` / `createExplosion` / `getCollectionBonuses` / `_recordPhase`); online net-sync (`_onlineFrame`, `_onlineInterpBuf`, `_onlineRenderTime`); and the spatial-hash globals from #173 phase 10 (`_SPATIAL_HASH_MIN`, `_enemySpatialHash`, `_projectileSpatialHash`, `_enemyHashActive`, `_projectileHashActive`). The spatial-hash query helpers (`queryEnemiesNear`, `queryProjectilesNear`) return **empty arrays** (not null) so callers' `_cands.length` reads don't crash when the hash is inactive server-side. Also wired the Boss class into the loader (was missing — `global.Boss = loadClass('Boss', 'Boss')`), expanded the `GameSession` arena stub with no-op `update` / `updateCamera` / `updateCameraForTwo` / `draw` methods + `camera.width`/`camera.height` fields, and added per-test session-state sync (`global.arena`, `global.player`, `global.player2`, `runState.player/player2/wave/frame/gameRunning`) in parityTest Test 9 before calling `pre(dt)`. **ParityTest: 85/85 → 87/87 passing.** The `pre(dt)` + `mid(dt, false)` server-side invocations now complete without throwing, validating that the extracted helpers + their bare-name global lookups resolve cleanly through the loader. **Next step:** wire the bridge into `GameSession._tick()` to actually drive the extracted helpers instead of GameSession's parallel custom loop — but that's a behavior change (damage authority migration to ECS state) tracked separately. Build green, 84/84 Vitest, lint 95/95 baseline.

### Fixed
- **Server-sim parityTest crashed at hero #9 (`lightning`) — `BASE_HERO_STATS` overwrite + `window.addEventListener` stub gaps**. Two root-cause fixes that together restore parityTest to a fully-green 80/80 baseline and unblock a new RendererBridge smoke test that loads all four extracted gameplay helpers (`_updateGameplayPre`, `_updateGameplayMid`, `_drawGameplayMid`, `_drawGameplayPost`) server-side. **Bug 1: `Constants.js` clobbered the server-sim's DLC-expanded hero table.** `server/simulation/loader.js` pre-seeds `global.BASE_HERO_STATS` from `server/simulation/constants.js` which has all 19 heroes baked in (8 base + 11 DLC), so the loader can boot Player + GameSession without running any DLC index.js's `injectHero()` method. But Player.js's ESM import chain reaches `Constants.js`, whose module-load `Object.assign(window, {BASE_HERO_STATS, …})` block then overwrote `globalThis.BASE_HERO_STATS` with the renderer's 8-hero base table — silently truncating the DLC heroes. parityTest iterated 9 heroes in test 3 (passing fire through makuta from the server's table snapshot) then crashed when `getHeroStats('lightning')` returned undefined for `_cloneStats(BASE_HERO_STATS[type])` and `base.ultModifiers = …` threw on the undefined `base`. Fix: `Constants.js`'s `Object.assign` now skips the `BASE_HERO_STATS` key when the existing `window.BASE_HERO_STATS` has more entries than the renderer's local table (length-check guard). Other constants (POWERUP_TYPES, BOSS_TYPES, ENEMY_TYPES, etc.) still propagate normally. **Bug 2: `Camera.js`'s unguarded `window.addEventListener('keydown', …)` at module-load broke server-side `require()` of `core/updateGameplayPre.js`** (which imports Camera.js for `isPhotoMode`). Loader.js's `global.window = global` shim made `typeof window` truthy but Node's `global` has no `addEventListener` method. Fix: loader.js now sets `global.window.addEventListener = () => {}` plus `removeEventListener` no-op stubs before any module-load happens. **RendererBridge smoke test added** (parityTest Test 9): asserts `getUpdatePre()` / `getUpdateMid()` / `getDrawMid()` / `getDrawPost()` all return functions, then attempts a server-side `pre(dt)` invocation. The function-load assertions all pass; the invocation throws on `arena.updateCamera is not a function` (first arena method the helper reaches) — expected, deferred to a follow-up loader-stub-completeness audit / per-session arena setup. ParityTest: **80/80 → 85/85 passing** (5 new bridge assertions). Build green, 84/84 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.3 (MeleeSwipe re-evaluated as kept-as-class)**. Original deferral (server-sim coupling concerns) re-evaluated under the shared "ECS perf is a function of N" rationale from Boss / Player exclusions. MeleeSwipe peak alive count is ~3 swipes with 15-frame lifetime; coordination between client (`core/updateGameplayMid.js` PvP damage check against `additionalPlayers` / `player2`) and server (`GameSession._processMeleeAttacks` enemy damage authority) goes through a single shared source of truth — the plain MeleeSwipe class instances in `meleeAttacks[]`. Dual-write to typed-array slots would create two stores for identical low-volume state at N≈3, gaining zero SoA cache locality. MeleeSwipe class stays as-is. **ECS arc now uniformly closed at 10 entity slices migrated** (PowerUp, CardDrop, Particle, FloatingText, MemoryShard, GoldDrop, HolyMask, Companion, Projectile, Enemy) + **two intentional kept-as-class outcomes** (5.3 MeleeSwipe, 5.12 Boss) under shared rationale: ECS perf is a function of N; irrelevant at low-N or singleton scope. Player remains excluded from start (1-2 instances, no pool, ECS gain ~0). No code changes in this commit — design clarification only.

### Changed
- **#5 — ECS migration, Phase 5.12 (Boss kept-as-class — ECS arc closed)**. Boss intentionally **not** migrated to ECS. Rationale: Boss has 52 fields with sparse conditional initialization per `bossType` (TANK / SPEEDSTER / NOVA / RHINO / HYDRA / MAKUTA / GREEN_GOBLIN) and a 1449-LOC class body with type-keyed multi-phase attack patterns; **peak alive count is 1-2 instances**, so the SoA cache-locality win that ECS migration provides for high-volume entities (Particle N=300, Projectile N=512, Enemy N=256) is irrelevant. The `tasks/ecs-plan.md` doc updated to mark Boss as kept-as-class alongside Player — both excluded under the same "ECS perf is a function of N; irrelevant at N=1-2" rationale. Boss class instances continue to live in `runState.bossInstances[]`; the `enemies` mixed-storage sentinel from phase 5.11b iterates them transparently alongside ECS Enemy slots, preserving the legacy `enemies[0] instanceof Boss` boss-active check at `game.js:3296`. The 30+ `instanceof Boss` checks across the codebase work unchanged. **#5 ECS arc closed at 10 of 12 entity slices migrated** (5.1 PowerUp, 5.2 CardDrop, 5.4 Particle, 5.5 FloatingText, 5.6 MemoryShard, 5.7 GoldDrop, 5.8 HolyMask, 5.9 Companion, 5.10 Projectile, 5.11 Enemy) + two intentional exclusions (5.3 MeleeSwipe deferred per server-sim coupling, 5.12 Boss kept-as-class per N=1-2 rationale). No code changes in this commit — design clarification only.

### Changed
- **#5 — ECS migration, Phase 5.11b (Enemy compat-ctor + mixed-storage sentinel)**. Full Enemy migration. The 591-LOC `class Enemy` body in `Enemy.js` stays intact — constructor logic (190 lines of subtype init / elite roll / chaos / mutator / DLC `ENEMY_LOGIC[subType].init(this)` hook) runs unchanged against the throwaway `this`. At the very end of the constructor, `allocEnemySlot(runState)` allocates a typed-array slot; `copyToEnemySlot(runState, slot, this)` iterates every own property on `this` (including any custom `_*` fields the DLC `init()` mutated like `_voidPulseTimer`, `isChaos`, `knockbackResist`) and writes each through the slot proxy's setter — known fields land in their typed array, `_*` fields land in a per-slot extras dictionary at `runState.enemyExtras[slot]`. Constructor returns the slot proxy. **DLC ref-stash concern verified moot:** `grep -rnE "_\w+\.push\(enemy\)|store\w*\.push\(enemy\)" dlc/` returns zero matches; DLCs mutate `enemy` in `init/update/draw` hooks but don't stash external refs to the throwaway `this`. **`EnemySlotInternal` proxy** in `core/systems/enemySystem.js` exposes all 28 fields via `Object.defineProperty` getter/setter pairs over typed arrays — Float32 for position/hp/damage/radius/speed/xpValue/alpha/biomeSpeedMod, Int32 for shootCooldown/summonCooldown/frozenTimer/slowTimer/poisonStacks/hitFlashTimer, Uint8 for sides, Uint16 for color (palette ord) + subType (ord into `ENEMY_SUBTYPES`), parallel `Array` for eliteType/parentBoss/targetPreference/_bodyGradient (object refs), 4-bit bitfield for isElite/isSummonedMinion/_ghost/dead flags. `update()` / `draw()` methods stay on `Enemy.prototype` — the proxy's `getPrototypeOf` trap returns the registered prototype so `slot.update()` / `slot.draw()` dispatch as normal class methods. Inside those methods `this` is the proxy and every `this.X` read/write routes through the typed-array getter/setter. **`window.enemies` mixed-storage sentinel** in `Enemy.js`: a `Proxy({}, handler)` that looks like a real `Array` to consumers but routes operations to a combined view of `runState.bossInstances` (Boss class instances, until phase 5.12) and the ECS Enemy slots. **Iteration order: bosses first, then ECS slots** — this preserves the legacy `enemies.unshift(boss)` → `enemies[0] instanceof Boss` invariant used by `game.js:3296`'s boss-active detection. Implemented traps: `length` (boss count + enemy count), numeric subscript (route by index), `Symbol.iterator` + `forEach` / `some` / `every` / `find` / `findIndex` / `filter` / `map` / `slice` (iterate combined view), `indexOf(X)` / `includes(X)` (Boss → `bossInstances.indexOf(X)`, slot proxy → `bossInstances.length + _slotIdx()`), `splice(i, 1)` (Boss range → `bossInstances.splice`, slot range → `killEnemy`), `push(X)` (Boss instance → `bossInstances.push`, slot proxy → no-op), `unshift(X)` (Boss → `bossInstances.unshift` preserves first-position), `length = 0` setter → `clearEnemies(runState)`. **Renderer-internal changes:** `runState.enemies = []` field dropped from `createRunState()` schema; game.js destructure block drops `enemies`; replaced by `const enemies = window.enemies;` (the sentinel installed by Enemy.js's module-load `window.enemies = _enemiesSentinel` line); `window.enemies = enemies` plain export at the bridges block removed. **Zero leaf-module rewrites** — the ~131 `enemies.forEach`, 46 `enemies.length`, 22 `enemies.push`, 10 `enemies.find`, 10 `enemies.filter`, 8 `enemies.unshift`, 8 `enemies.some`, 3 `enemies.splice`, 2 `enemies.slice`, 2 `enemies.includes`, 1 `enemies.map` consumer patterns across `core/updateGameplayPre.js` / `core/updateGameplayMid.js` / `core/drawGameplayMid.js` / `Boss.js` / `Spawner.js` / `EvilHeroes.js` / `Arena.js` / `Managers/AudioManager.js` / 6 DLCs work unchanged through the sentinel. Build green, 84/84 Vitest, lint 95/95 baseline.

### Added
- **#5 — ECS migration, Phase 5.11 skeleton (Enemy typed-array storage)**. System module `core/systems/enemySystem.js` shipped (~210 LOC) implementing the storage layout from `tasks/ecs-enemy-boss-design.md`. `initEnemies(rs)` allocates `MAX_ENEMIES = 256` slots across 28 typed-array fields: `enemyX/Y` / `enemyHp/maxHp/damage` / `enemyRadius/speed` / `enemyXpValue` / `enemyAlpha` / `enemyBiomeSpeedMod` / `enemyColor` (Uint16 palette ord) / `enemySides` / `enemySubType` (Uint8 ord into `ENEMY_SUBTYPES`) / `enemyShootCooldown` / `enemySummonCooldown` / `enemyFrozenTimer` / `enemySlowTimer` / `enemyPoisonStacks` / `enemyHitFlashTimer` / `enemyFlags` (4-bit bitfield: isElite, isSummonedMinion, _ghost, dead) / `enemyEliteType` / `enemyParentBoss` / `enemyTargetPreference` / `enemyBodyGradient` / `enemyId` / `enemyExtras` / `enemySlotProxy` / `enemyCount`. Also allocates `runState.bossInstances = []` — the mixed-storage scaffolding for the `enemies` sentinel that 5.11b/5.12 will install (the sentinel iterates ECS Enemy slots first, then Boss class instances, until 5.12 collapses both into a unified entity slot table). API includes `allocEnemySlot(rs)` / `killEnemy(rs, i)` / `clearEnemies(rs)` / `getEnemySubType(rs, i)` / `hasEnemyFlag(rs, i, flag)` / `setEnemyFlag(rs, i, flag, on)` / palette-interning helpers `internEnemyColor(c)` / `getEnemyPaletteColor(ord)` / dynamic subtype registration via `getOrAddSubtypeOrdinal(type)` so DLC enemy types auto-extend `ENEMY_SUBTYPES`. **Wired into `createRunState()`** via `initEnemies(rs)` so every fresh runState has the typed arrays + boss-instances scaffolding. **No consumers wired yet** — Enemy class still owns instance storage, `runState.enemies` legacy array unchanged, no `enemies` sentinel installed. The remaining 5.11b work is multi-hour: lift the Enemy class's 190-line constructor body + 220-line update + 170-line draw into a `spawnEnemyFull(rs, ...)` factory that writes directly to a slot proxy from the start (a "construct-then-copy" pattern fails because DLC `ENEMY_LOGIC[sub].init(this)` hooks may stash `this` refs, breaking subsequent storage-side writes). Build green, 84/84 Vitest, lint 95/95 baseline.

- **#5 — ECS migration, Phases 5.11 + 5.12 design pass (Enemy + Boss)**. Joint design doc shipped to `tasks/ecs-enemy-boss-design.md` covering the last two entity slices. Key inventory findings: (1) Boss does NOT extend Enemy — verified via `grep -rE "extends Enemy"` returning zero hits; the two classes are independent and just happen to share the `enemies` array. (2) `instanceof Enemy` is unused across the codebase; `instanceof Boss` is the canonical discriminator at 30+ sites in `game.js`, `core/updateGameplayMid.js`, `core/updateGameplayPre.js`, `Managers/AudioManager.js`, and 6 DLC files. (3) Enemy field surface is ~28 fields per instance (`x`, `y`, `hp`, `maxHp`, `damage`, `radius`, `speed`, `subType`, `eliteType`, `isElite`, `isSummonedMinion`, `parentBoss`, `shootCooldown`, `summonCooldown`, `targetPreference`, `alpha`, `frozenTimer`, `slowTimer`, `poisonStacks`, `hitFlashTimer`, `biomeSpeedMod`, `_bodyGradient`, `_ghost`, `_id`, etc.). (4) Boss has ~50 fields with many conditionally-initialized per `bossType` (Makuta phase-3 alone has 14 `mk*` fields). (5) Consumer-pattern frequency on `enemies.X`: 131× `forEach`, 46× `length`, 22× `push`, 10× `find`, 10× `filter`, 8× `unshift`, 8× `some`, 3× `splice`, 2× `slice`, 2× `includes`, 1× `map`. **Locked strategy: split slices, mixed storage.** 5.11 migrates Enemy only; Boss stays as class instance for 5.11 and lives in `runState.bossInstances[]`. The `enemies` sentinel iterates ECS Enemy slots first, then Boss instances, presenting a uniform Array-like API to consumers. 5.12 then migrates Boss, collapsing both into a unified entity slot table. **Trade-off documented:** `enemies.unshift(new Boss())` (8 callsites) semantically becomes "prepend to boss list" rather than "prepend to combined list" — bosses iterate AFTER enemies, not first. Verified the 8 callsites use unshift purely for "spawn boss as primary target" semantics, not for ordering-dependent game logic. **Compat strategy mirrors Projectile 5.10b:** `new Enemy()` returns an ECS slot proxy with `.update()` / `.draw()` methods delegating to system fns at the proxy's slot index, so leaf-module `enemies.forEach(e => e.update())` patterns work unchanged. Code work deferred to follow-up session (591 LOC Enemy ctor + 28-field shim + mixed-storage sentinel + 99 leaf-module patterns is ~2-3h focused work, beyond safe single-session scope). No code changes in this commit.

### Changed
- **#5 — ECS migration, Phase 5.10b (Projectile compat shim, sentinel Array Proxy)**. Completes the Projectile slice with **zero leaf-module rewrites**. `Entities/Projectile.js` rewritten as a thin shim: `Projectile.acquire(x, y, velocity, damage, color, radius, type, knockback, isEnemy, isExplosive?, isCrit?)` spawns an ECS slot via `spawnProjectile(runState, …)` and returns the system's Proxy-wrapped slot accessor. `new Projectile(...)` ctor mirrors the acquire path via `ProjectileCtor extends Projectile.acquire`. `Projectile.release()` becomes a no-op — kill happens through the sentinel's `splice` / `length = 0` paths. **`window.projectiles` smart sentinel:** a `Proxy({}, handler)` that looks like a real `Array` to consumers but routes every operation to the ECS slots. Implemented traps: `length` → `runState.projectileCount`; numeric subscript (`projectiles[i]`) → lazy slot proxy; `Symbol.iterator` + `forEach` / `some` / `every` / `find` / `findIndex` / `filter` / `map` / `slice` / `concat` → iterate slot proxies with the standard callback signature; `indexOf(slotProxy)` → return the proxy's `_slot` index directly; `splice(start, deleteCount)` → call `killProjectile` `deleteCount` times at `start` (swap-with-last semantics handle the index stability); `push(...)` → no-op (slot already allocated in `acquire`); `length = 0` setter → `clearProjectiles(runState)`. This means **~50 existing `projectiles.X` access patterns in `core/updateGameplayMid.js`** (`projectiles.forEach`, `projectiles.some(p => p._orphanAt !== undefined)`, `projectiles.filter(p => …)`, `projectiles.length`, `projectiles[index]`, `projectiles.indexOf(proj)`, `projectiles.splice(index, 1)`, `projectiles.length = 0`) **work transparently against ECS storage without a single line change**. **~86 spawn sites across Player.js / Enemy.js / Spawner.js / game.js / 17+ DLCs** continue using `Projectile.acquire(...)` / `new Projectile(...)` via the shim — the per-slot proxy returned exposes `.x` / `.y` / `.velocity.x` / `.life` / `.type` / `.onHit` / `.shooterType` / `.isWildfire` / etc. setters that write through to the typed arrays, plus `_`-prefixed DLC custom fields (`_rot`, `_healAccum`, `_orphanAt`, `_snapBuf`, etc.) routed via a Proxy trap to a per-slot extras dictionary. DLC `p.update = function() { … }` / `p.draw = function() { … }` overrides land on `runState.projectileUpdateFn[i]` / `projectileDrawFn[i]` and are invoked through the system's dispatch in `updateProjectiles` / `drawProjectiles` (binding `this` to the slot proxy so the existing closure bodies — `this.x = player.x`, `this.life--` — work unchanged). **Renderer-internal changes:** `runState.projectiles = []` field dropped from `createRunState()` schema; game.js destructure block drops `projectiles`; replaced by `const projectiles = window.projectiles;` (the sentinel installed by the shim's module-load `window.projectiles = _projectilesSentinel` line); `window.projectiles = projectiles` plain export at the bridges block removed (shim owns it now). **Tests rewritten:** `tests/entityPools.test.js`'s `describe('Projectile pool')` block (6 assertions on `Projectile._pool` / `acquire` / `release` round-trip identity) replaced with `describe('Projectile ECS slots')` asserting slot-reuse semantics — kill + spawn reuses slot 0, crit radius applies once (not double-multiplied on reuse), pierce defaults survive across slot reuse, `MAX_PROJECTILES` cap returns the `_DEAD` sentinel (`_slot === -1`) on overflow. **Class deletion deferred** — the shim is the canonical entry point for callers and stays in place; future cleanup can fold the shim into the system module if desired. Build green, 84/84 Vitest, lint 95/95 baseline.

### Added
- **#5 — ECS migration, Phase 5.10 partial (Projectile system module + design pass)**. Design preamble shipped to `tasks/ecs-projectile-hooks.md` cataloging the Projectile hook surface across the codebase: 86 spawn sites, 19 `onHit` callbacks (17× `() => 'STOP'`, 2× closures), 20 per-instance `update`/`draw` overrides in PoisonHero/GravityHero/WindBosses, plus optional fields (`shooterType`, `isWildfire`, `isCryo`, `outlineColor`, `ownerIsPlayer`) and online net-sync fields (`_id`, `_snapBuf`, `_lastSnapX/Y`, `_orphanAt`). The design concludes that a pure type-keyed `PROJECTILE_BEHAVIOR` registry — the recommendation in the cross-phase `tasks/ecs-design.md` — **fails** for the 20 DLC override sites because they close over per-spawn locals (`player`, `_w`, `waveIdx`, `_rot`, `_healAccum`, etc.) that don't exist outside the spawning function. Locked hybrid layout instead: type-keyed default `PROJECTILE_BEHAVIOR` for the 14 base draw branches (`fire`, `ice`, `water`, `plant`, `metal`, `black`, `time`, `love`, `smoke`, `mirror`, `psycho`, `light`, `thorn`, `dream`) + per-slot optional `projectileUpdateFn` / `projectileDrawFn` / `projectileOnHitFn` arrays for the closure-bearing customs. Custom `_`-prefixed DLC fields (`_rot`, `_healAccum`, `_maxR`, etc.) stored on the proxy object itself (not typed arrays) via a Proxy trap so DLC `this._rot += 0.03` patterns work unchanged. System module `core/systems/projectileSystem.js` shipped (~720 LOC) implementing the full layout with `MAX_PROJECTILES = 512`: typed-array storage for hot-path fields (x, y, vx, vy, damage, radius, knockback, life, pierce, type ord, color ord, 8-flag bitfield, shooterType ord), parallel `Array` for object refs (`owner`, `outlineColor`, override fns, custom-field bag, slot proxy), palette-interned colors, dynamic type-ordinal registration so DLC types auto-extend `PROJECTILE_TYPES`, and a `ProjectileSlotInternal` accessor class wrapped in a `Proxy` for `_`-prefixed field forwarding. **No consumers wired yet** — `Entities/Projectile.js` class stays in place as the canonical path; leaf modules + game.js + DLCs continue using the legacy class. The system module stands alone, validating syntax + tree-shake. Compat shim + ~50 leaf-module pattern rewrites + ~86 spawn-site migrations + Projectile pool-test rewrite split to Phase 5.10b/5.10c (one-session-each follow-ups). Build green, 84/84 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.9 (Companion, first behavioral system)**. Orbiting AI companion entity converted to component arrays. `class Companion` removed from `Companion.js` (file deleted, 172 LOC); replaced by `core/systems/companionSystem.js` exporting `initCompanions` / `spawnCompanion(rs, type, player)` / `killCompanion` / `clearCompanions` / `updateCompanions` / `drawCompanions` / `findCompanionByType` / `getCompanionType` / `serializeCompanions` with `MAX_COMPANIONS = 16`. **First slice with non-trivial per-frame behavior** rather than just data + render: orbit motion (`angle += 0.02` per frame, position eases toward `player + Math.cos/sin(angle) * 60`), 120-frame attack cooldown, nearest-enemy targeting within 400 px, three type-keyed synergy effects (Fire companion + Ice player → Thermal Shock damage with frozen-target bonus; Plant companion + Metal player → heal player; generic 15-damage otherwise), and an 8-frame beam VFX to the last hit target that decays via `companionLastTimer`. Storage: Float32 position + per-slot orbit angle + cached `player.speed * 0.9`, Int32 cooldown + beam-visibility timer, Uint8 active flag, Uint16 palette-interned color, plus parallel `string[]` for `type`, `Array` for the owning `Player` ref, and `Array` for the last enemy target ref (object refs can't go in typed arrays). **ChaosMode.js migration:** the legacy `companions.find(c => c.type === type)` / `companions.findIndex(...)` / `companions.splice(idx, 1)` patterns rewrote to `findCompanionByType(runState, type)` / `killCompanion(runState, idx)`. The `spawnChaosCompanion` helper now calls `spawnCompanion(runState, type, runState.player)` and checks `>= 0` for cap success. **Save format unchanged:** `saveRunState`'s `companions: companions.map(c => ({ type: c.type }))` rewrote to `serializeCompanions(runState)` which returns the same `[{type}]` shape; restore path uses `spawnCompanion` in a forEach loop. Other dropped scaffolding: `runState.companions = []` field, game.js destructure entry, `window.companions`, `_world.companions` mirror, `companions.length = 0` reset → `clearCompanions(runState)`, `global.companions = []` in `server/simulation/loader.js`, `this.companions = []` in `shared/world.js`, `import './Companion.js'` in `main.js`, `GAME_API.companions` getter. The leaf-module pickup paths (`core/updateGameplayMid.js`'s `companions.forEach(c => c.update())` and `core/drawGameplayMid.js`'s `companions.forEach(c => c.draw(ctx))`) became single `updateCompanions(runState)` / `drawCompanions(ctx, runState)` calls. Build green, 84/84 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.8 (HolyMask, direct rewrite)**. Wave-reward + boss-drop entity converted to component arrays. `class HolyMask` removed from `Entities/HolyMask.js` (file deleted, 211 LOC mostly draw code); replaced by `core/systems/holyMaskSystem.js` exporting `initHolyMasks` / `spawnHolyMask(rs, x, y, isTrueGolden?)` / `killHolyMask` / `clearHolyMasks` / `drawHolyMasks` with `MAX_HOLYMASKS = 32`. Storage: Float32 position + per-slot rotation angle, Uint8 `isTrueGolden` flag. The plan flagged this slice as "sub-table on entity — type→buff lookup pattern" but the original class had no such sub-table — `isTrueGolden` is a plain boolean and buff effects (`damageMultiplier += 0.5`, `maxHp += 50`, `cooldownMultiplier *= 0.8`, etc.) are applied inline by the collision loop in `core/updateGameplayMid.js`, not via any per-entity lookup. The two large draw routines (`_drawGolden` for the standard mask, `_drawTrueGolden` for the wave-10 reward) were lifted from class methods into module-private functions in the new system; `cachedRadial` import preserved for the `holyMask:gold` gradient. **No compat shim** since the class had only 3 spawn sites: `EvilMode.js`'s wave-10 reward, `game.js`'s endgame trigger, and `core/updateGameplayMid.js`'s boss-death drop. All rewrote to `spawnHolyMask(runState, x, y, isTrueGolden)`. The two collision-loop branches (single-player + co-op P2) in `core/updateGameplayMid.js` rewrote to iterate `runState.holyMask*` by slot index with `killHolyMask` for pickup. Other dropped scaffolding: `runState.holyMasks = []` field, game.js destructure entry, `window.holyMasks`, `window.HolyMask`, `_world.holyMasks` mirror, `holyMasks.length = 0` reset → `clearHolyMasks(runState)`, `this.holyMasks = []` in `shared/world.js`, `import './Entities/HolyMask.js'` in `main.js`, `GAME_API.holyMasks` getter. Build green, 84/84 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.7 (GoldDrop, direct rewrite)**. Currency drop converted to component arrays. `class GoldDrop` removed from `Entities/GoldDrop.js` (file deleted, 120 LOC); replaced by `core/systems/goldDropSystem.js` exporting `initGoldDrops` / `spawnGoldDrop(rs, x, y)` / `killGoldDrop` / `clearGoldDrops` / `drawGoldDrops` with `MAX_GOLDDROPS = 128`. Storage: Float32 position + spin angle + bob phase, Uint8 value (5..14) + tier (0|1|2). Per-tier color palettes lifted from the class draw method as module-level const arrays (`_RIM` / `_FACE` / `_SHINE` / `_GLOW`). The plan flagged "magnet behavior" as the first behavioral system but the pickup loop is plain pickup-range collision (`dist < player.pickupRange`) — the "Golden Magnet (Chance Convergence)" comment in the legacy update loop was misleading. **No compat shim** since the class had only 3 renderer spawn sites + 3 DLC spawn sites; all rewrote directly to `spawnGoldDrop(runState, x, y)`. The DLC sites in `dlc/faith_of_fortune/ChanceHero.js` passed an unused third arg (`25` / `50` / `10` — went to the class's `world` parameter slot and was harmless); dropped during migration. **Tests rewritten:** `tests/entityPools.test.js` had a `describe('GoldDrop pool', …)` block asserting on `GoldDrop._pool` / `GoldDrop.acquire` / `GoldDrop.release` round-trips. Pool semantics no longer exist (slot allocation replaces them), so the block was rewritten to assert on `runState.goldDrop*` typed arrays directly: spawn allocates slot 0, kill + spawn reuses the freed slot (swap-with-last), and the `MAX_GOLDDROPS` cap returns -1 on overflow. Test count went 83 → 84 (one new cap-overflow assertion). **Side fix — Node-test import safety:** importing `runState` from a test now pulls the full ECS system chain into Node where `window` is undefined. Three unguarded `window.X = …` writes in `Constants.js` (`window.ENEMIES_PER_WAVE`, `window.ACHIEVEMENTS = []`) and `RunState.js` (`window.RunState = {…}`) were wrapped in `typeof window !== 'undefined'` guards. The `ACHIEVEMENTS` array shape changed from `const ACHIEVEMENTS = window.ACHIEVEMENTS` reading-from-window to a local `const ACHIEVEMENTS = []` that's mirrored to `window` at the existing module-bottom `Object.assign(window, …)` block. Other dropped scaffolding: `runState.goldDrops = []` field, game.js destructure entry, `window.goldDrops` plain export, `window.GoldDrop` class export, `_world.goldDrops` mirror, `goldDrops.length = 0` reset → `clearGoldDrops(runState)`, `global.goldDrops = []` in `server/simulation/loader.js`, `global.GoldDrop = loadClass(...)` server-side loader, `this.goldDrops = []` in `shared/world.js`, `import './Entities/GoldDrop.js'` in `main.js`, `GAME_API.goldDrops` getter. Build green, 84/84 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.6 (MemoryShard, direct rewrite)**. Story-mode memory pickup entity converted to component arrays. `class MemoryShard` removed from `MemoryShard.js` (file deleted, 80 LOC); replaced by `core/systems/memoryShardSystem.js` exporting `initMemoryShards` / `spawnMemoryShard(rs, x, y, heroType)` / `killMemoryShard` / `clearMemoryShards` / `drawMemoryShards` / `getMemoryShardColor` with `MAX_MEMORYSHARDS = 32`. Storage: palette-interned colors (Uint16), Float32 position, and a `string[]` side table for the `heroType` field (cardKey-style — values are open-set DLC type names, not enumerable). The original class's per-instance `floatOffset` was always `Math.sin(Date.now()/500)*5` — identical for every shard each frame — so it's computed once per `drawMemoryShards` call instead of stored per slot. **No compat shim** since the class had only 4 spawn sites (all in `Arena.js`); they rewrote to direct `spawnMemoryShard(runState, pos.x, pos.y, type)` calls. **DLC migration:** three DLCs (`echos_of_eternity`, `rise_of_the_rock`, `tournament_of_thunder`) patched `MemoryShard.prototype.getColorByType` to override colors for `time` / `earth` / `lightning` shards respectively. The class deletion would have made those patches no-op-guarded (the `typeof MemoryShard !== 'undefined'` check goes false) and the lightning override in particular would have changed behavior — the class fallback gave `'#f1c40f'` while the DLC override gave `'#ffeb3b'`. Migrated all three DLC overrides to `window._MEMORY_SHARD_COLORS[type] = '#hex'` registry assignments (the registry path was already in the original class's `getColorByType` and is preserved in the new system's `_getColorByType` helper). Other dropped scaffolding: `runState.memoryShards = []` field, game.js destructure entry, `window.memoryShards = memoryShards` plain export, `_world.memoryShards = memoryShards` mirror, `memoryShards.length = 0` reset → `clearMemoryShards(runState)`, `global.memoryShards = []` in `server/simulation/loader.js`, `this.memoryShards = []` in `shared/world.js`, `import './MemoryShard.js'` in `main.js`. The leaf-module pickup-dispatch loop in `core/updateGameplayMid.js` (single-player + co-op P2 collection paths, both ~30-line story-state-update flows) rewrote to iterate `runState.memoryShard*` arrays by slot index. `core/drawGameplayMid.js` calls `drawMemoryShards(ctx, runState)`. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.5 (FloatingText, compat-shim pattern)**. Second-highest-volume entity. Same shape as Phase 5.4 — ~155 spawn sites across `game.js`, `Player.js` (27), `Enemy.js`, `Boss.js`, `Companion.js`, `Arena.js`, `EvilHeroes.js`, `Spawner.js`, `TutorialMode.js`, `core/updateGameplayPre.js`, and 4+ DLCs — converted to component-array storage. New system module `core/systems/floatingTextSystem.js` exports `initFloatingTexts` / `spawnFloatingText(rs, x, y, text, color, size)` / `killFloatingText` / `clearFloatingTexts` / `updateFloatingTexts` / `drawFloatingTexts` / `detectCrit` with `MAX_FLOATING_TEXTS = GAMEPLAY.MAX_FLOATING_TEXTS` (80). Storage: palette-interned colors (Uint16), Float32 position/velocity/life/maxLife/size, Uint8 isCrit flag, and a `string[]` side table for the text content (text is per-instance variable — `'42'`, `'CRIT!'`, `'STORM!'`, hero-specific damage labels — so a `Uint16Array` palette doesn't apply). Cap-overflow semantics changed from "drop oldest" to "drop newest" (return -1 from `spawnFloatingText`) — visually indistinguishable in damage-storm scenarios within ~10 frames either way; O(1) versus the prior O(n) splice. **Compat shim:** `Entities/FloatingText.js` rewritten same as Particle — `FloatingText.acquire(...)` returns a `FloatingTextSlot` proxy whose `.velocity.x/y` / `.life` / `.maxLife` / `.size` / `.isCrit` / `.text` / `.x` / `.y` / `.alpha` setters write back to the slot. `FloatingText.release` is a no-op. `window.floatingTexts` becomes a sentinel `{push:no-op, splice:no-op, forEach:no-op, length:runState.floatingTextCount}`. Existing `floatingTexts.push(FloatingText.acquire(...))` patterns work unchanged through the shim. **Renderer-internal changes:** game.js destructure drops `floatingTexts`; `runState.floatingTexts = []` removed from `createRunState()`; both `floatingTexts.length = 0` reset sites replaced with `clearFloatingTexts(runState)`; `window.floatingTexts = floatingTexts` plain export removed; `_world.floatingTexts = floatingTexts` mirror removed; debug-overlay `ftN = floatingTexts.length` reads `runState.floatingTextCount`; `GAME_API.floatingTexts` getter replaced with `floatingTextCount`. The leaf modules drive the hot path directly: `core/updateGameplayMid.js`'s far-offscreen cull + tick loop calls `killFloatingText` + `updateFloatingTexts`; `core/drawGameplayMid.js`'s draw pass calls `drawFloatingTexts(ctx, runState)`. `shared/world.js`'s `this.floatingTexts = []` instance field dropped. `core/systems/floatingTextSystem.js` adds ~150 LOC. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.4 (Particle, compat-shim pattern)**. Highest-volume entity in the game — ~115 spawn sites across `game.js`, `Player.js`, `Enemy.js`, `Spawner.js`, `Biomes.js`, `TutorialMode.js`, `UI/MenuBackground.js`, and 17 DLC files — converted to component-array storage. New system module `core/systems/particleSystem.js` exports `initParticles` / `spawnParticle(rs, x, y, color, vx?, vy?, life?)` / `killParticle` / `clearParticles` / `updateParticles` / `drawParticles` with `MAX_PARTICLES = 300`. Storage uses palette-interned colors (`Uint16Array` of ordinals into a module-level `_palette[]` of CSS strings) per `tasks/ecs-design.md`; the original `Entities/Particle.js` sprite cache migrated into the system as a private module-level cache keyed by palette ordinal. **Compat-shim strategy:** rewriting 115 disparate spawn sites — many multi-line `const p = Particle.acquire(); p.velocity.x = …; p.life = …; particles.push(p);` patterns scattered through DLC code — would have been highly invasive. Instead, `Entities/Particle.js` was rewritten as a thin compat layer: `Particle.acquire(x, y, color, vel?)` now spawns an ECS slot and returns a `ParticleSlot` proxy whose `.velocity.x/y` / `.life` / `.alpha` / `.x` / `.y` setters write back to the slot. `Particle.release` is a no-op — life decay kills slots automatically inside `updateParticles`. The `window.particles` global becomes a sentinel object with `push: () => {}` and `get length()` reading `runState.particleCount`, so DLC guards like `typeof particles !== 'undefined' && particles.length < MAX_PARTICLES` stay correct. Renderer-hot-path call sites in `core/updateGameplayMid.js` (per-frame far-offscreen cull + tick + kill-on-zero-alpha) and `core/drawGameplayMid.js` (camera-bounds draw pass) bypass the shim entirely and call the system functions directly. **Renderer-internal changes:** game.js's destructure block dropped the `particles` alias; `runState.particles = []` removed from `createRunState()`; `window.particles = particles` plain export, the `_world.particles = particles` mirror, the `createDeathBurst` 300-cap check, the debug-overlay `partN = particles.length` read, the `GAME_API.particles` getter, and the unused `MAX_PARTICLES` const import all dropped (the cap is enforced inside `spawnParticle`). 0 entity-shim files deleted (kept for back-compat); `core/systems/particleSystem.js` adds ~170 LOC. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.3 (MeleeSwipe) deferred**. Server simulation in `server/simulation/GameSession.js` uses `_world.meleeAttacks` as the authoritative damage-resolution container for online-coop melee hits — each swipe carries an instance-specific `hitList` Set of enemies-already-damaged that persists across the 15-frame swipe lifetime, plus the `owner` ref for friendly-fire exclusion. ECS swap would either require a dual-write pattern (Player.melee() pushes both to `runState.meleeSwipe*` and `_world.meleeAttacks`) or a redesign of the server's melee processing to read from ECS. Neither fits a single-session slice, so the phase is documented as deferred in `tasks/ecs-plan.md` until after the no-coupling entities land. The class file `Entities/MeleeSwipe.js` stays in place.

### Changed
- **#5 — ECS migration, Phase 5.2 (CardDrop)**. Second ECS slice, same shape as 5.1. `class CardDrop` removed from `Entities/CardDrop.js`; replaced by component-array storage on `runState` (`cardDropX`, `cardDropY`, `cardDropAngle`, `cardDropScale`, `cardDropScaleDir`, `cardDropKey`, `cardDropCount`) plus a system module `core/systems/cardDropSystem.js` exporting `initCardDrops` / `spawnCardDrop` / `killCardDrop` / `clearCardDrops` / `drawCardDrops`. Storage strategy per `tasks/ecs-design.md`: dense head + swap-with-last with `MAX_CARDDROPS = 32` cap. `cardKey` stays a regular `string[]` side-table — keys are arbitrary game-content identifiers (`${enemyType}_${i}`) not enumerated, so the design doc's `Uint16Array` palette pattern doesn't apply. Pickup-collision loop in `core/updateGameplayMid.js` rewritten to iterate by slot index and call `killCardDrop(runState, index)`; spawn site in `game.js`'s `checkDrop()` calls `spawnCardDrop(runState, x, y, cardKey)`; draw site in `core/drawGameplayMid.js` calls `drawCardDrops(ctx, runState)`. One legacy `cardDrops.length = 0` reset site replaced by `clearCardDrops(runState)`. Dropped: the `cardDrops` field from `RunState.createRunState()` and from game.js's destructure block, `window.cardDrops = …`, `window.CardDrop = …`, `_world.cardDrops = cardDrops` resync, `this.cardDrops = []` in `shared/world.js`, and `import { CardDrop } from './Entities/CardDrop.js'` in both `game.js` and `main.js`. `Entities/CardDrop.js` deleted entirely (42 LOC removed). Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#5 — ECS migration, Phase 5.1 (PowerUp)**. First ECS slice. `class PowerUp` removed from `Entities/PowerUp.js`; replaced by component-array storage on `runState` (`powerUpX`, `powerUpY`, `powerUpType`, `powerUpTimer`, `powerUpOscill`, `powerUpCount`) plus a system module `core/systems/powerUpSystem.js` exporting `initPowerUps` / `spawnPowerUp` / `killPowerUp` / `clearPowerUps` / `updatePowerUps` / `drawPowerUps` / `getPowerUpType`. Storage strategy per `tasks/ecs-design.md`: dense head + swap-with-last with `MAX_POWERUPS = 16` cap. Pickup-collision loop in `core/updateGameplayMid.js` rewritten to iterate by slot index; spawn site in `core/updateGameplayPre.js` calls `spawnPowerUp(runState)` instead of `powerUps.push(new PowerUp())`; draw site in `core/drawGameplayMid.js` calls `drawPowerUps(ctx, runState)`. Four legacy `powerUps.length = 0` reset sites replaced by `clearPowerUps(runState)`. Dropped: the `powerUps` field from `RunState.createRunState()` and from game.js's destructure block, `window.powerUps = …`, `window.PowerUp = …`, `_world.powerUps = powerUps` resync, `global.powerUps = []` in `server/simulation/loader.js`, `this.powerUps = []` in `shared/world.js`, and `import { PowerUp } from './Entities/PowerUp.js'` in both `game.js` and `main.js`. `Entities/PowerUp.js` deleted entirely (29 LOC removed). Validates the cross-phase ECS pattern end-to-end with the lowest-blast-radius entity in the 12-slice plan. Build green, 83/83 Vitest, lint 95/95 baseline.

### Fixed
- **Telemetry POST blocked by CORS preflight on cross-origin LAN setup**. Browser console: `"The value of the 'Access-Control-Allow-Credentials' header in the response is '' which must be 'true' when the request's credentials mode is 'include'"`. Root cause: `navigator.sendBeacon` (used by `Managers/TelemetryManager.js:142`) sends with credentials mode `'include'` per spec, so when the Vite dev server (`http://localhost:5173`) targets the API server (`http://192.168.178.63:3001`) the preflight needs `Access-Control-Allow-Credentials: true`. Fix: added `credentials: true` to the `cors()` middleware config in `server/server.js:257`. Safe alongside the existing dynamic-origin callback because the cors middleware echoes the specific request origin (never `*`) when an origin function is used, which is what spec requires for credentialed responses.

### Changed
- **Server README brought up to date**. `server/README.md` rewrote from a four-feature pitch (cloud saves, online multiplayer, global lobby, leaderboard) into the full surface that now actually ships: TLS / HTTPS + WSS, `ALLOWED_WS_ORIGINS` CSRF allowlist, `ADMIN_PASSWORD_HASH` bcrypt admin auth (with the `scripts/admin-hash.js` helper + ephemeral dev password + 8-hour admin JWT + 5/15-min login rate limit + audit trail), server-authoritative session tokens for verified leaderboard entries, `verified` + `dailySeed` filters, speedrun leaderboard, community map workshop (upload/browse/like/score), crash reporting, opt-in telemetry pipeline, world events, friends, hero balance, voice-chat WebRTC signalling, 30-s reconnect grace + 5-min rematch window, story/maze relay messages, snapshot delta encoding + `permessage-deflate`. WebSocket message tables expanded to cover `STORY_CONTINUE`, `MAZE_NODE_SELECTED`, `RETURN_TO_LOBBY`, `WEBRTC_*`, `VOICE_MUTE`, `PARTNER_RECONNECTING`. REST table split into public/player vs admin; per-endpoint rate limits and payload caps inlined. Admin Dashboard tab list grew from five to eight (added Balance, Analytics, Events, Technical). Configuration table now lists `SERVER_HOST`, `ALLOWED_WS_ORIGINS`, `TLS_*`, `ADMIN_PASSWORD_HASH`, `NODE_ENV`, and notes the production fail-secure boot behaviour for missing `JWT_SECRET` / `ADMIN_PASSWORD_HASH`. Pi setup section now points at the hardened `scripts/server-systemd.service.example` and `scripts/server-setup.md` instead of an inline minimal unit.

### Fixed
- **Crash mid-frame: `_renderMinimap is not defined` (draw-half audit)**. Same class of bug as the update-half fixes, applied to the draw leaf modules. `core/drawGameplayMid.js:223` calls `_renderMinimap()` (defined at `game.js:5659`) for the #170 minimap pass; never bridged. Added `window._renderMinimap = _renderMinimap`. Sister module `core/drawGameplayPost.js` was already defensive (every `globalThis.X` read is `|| 0` / `|| []`-guarded, and the local `isReducedMotion` wrapper returns false if `globalThis.isReducedMotion` is missing), so it ran clean. Cross-checked every bare-name function call / class ref in `drawGameplayMid.js` against `window.X` bridges: `getHeroTheme` (`Arena.js:1206`), `isChaosActive` (`UI/Shop.js:284`), `isReducedMotion` (`game.js:3046`), `TestingGrounds` (`TestingGrounds.js:308`), `TutorialMode` / `Boss` / `applyScreenShake` (the existing leaf-module-bridge block) all resolve. Only `_renderMinimap` was missing.
- **Crash mid-frame: `_recordPhase is not defined` + remaining `updateGameplayMid` bridges audit**. Third (and intended final) sweep over #173 phase 10 leaf-module bridge gaps. After the prior two fixes, the per-frame phase-timing call `_recordPhase('enemies', performance.now() - _enemiesT0)` at `core/updateGameplayMid.js:1411` still threw — `_recordPhase` (defined at `game.js:5860`) was never on window. Added `window._recordPhase = _recordPhase`. Plus a forward-looking audit of every bare-name function call / class access in both `core/updateGameplayPre.js` and `core/updateGameplayMid.js`: cross-referenced against `window.X = …` lines in `game.js`, the `_ownedDescriptor` reverse-alias mechanism in `GameContext.js` (which auto-installs `window.canvas` / `window.ctx` / `window.saveData` / `window.gameConfig` getters as setters fire during init), and the per-symbol shims scattered across `Camera.js` / `Constants.js` / `Biomes.js` / `EvilHeroes.js` / `dlc/echos_of_eternity/MazeOfTime.js`. Three latent gaps surfaced and got bridged: `getCollectionBonuses` (called unconditionally on every enemy kill at lines 841/992/1110/1112 — would crash on first death) and `_onlineInterpBuf` / `_onlineRenderTime` (online-mode ghost-entity interpolation at lines 121/151/155/166 — gated behind `runState.isOnlineMode` so only crashes in online sessions, but the gating was the only thing keeping this latent). **Known remaining latent issue, not in scope here**: `_onlineFrame++` at `core/updateGameplayMid.js:147` reads + writes a bare-name identifier that lives as a module-scope `let` in `game.js`; a `window._onlineFrame` bridge would only let the leaf module *read* it, not write back to the renderer's `let`. Online mode currently triggers a strict-mode `ReferenceError` on the increment. Fix is structural — either move `_onlineFrame` onto `runState` (matches the #11 carve pattern), or wrap it in a getter/setter pair on a globally-exposed object — and tracked as a follow-up rather than another whack-a-mole bridge.
- **Crash mid-frame: `_SPATIAL_HASH_MIN is not defined` + additional `updateGameplayMid` bridges**. Second-wave follow-on from the #173 phase 10 leaf-module extraction. `core/updateGameplayMid.js` line 908 reads `_SPATIAL_HASH_MIN` (the broad-phase rebuild threshold) at the top of the per-frame spatial-hash setup, and `game.js`'s window-bridge block never exposed it — the `_SPATIAL_HASH_MIN`, `_enemySpatialHash`, `_projectileSpatialHash`, `queryEnemiesNear`, `queryProjectilesNear` block at game.js:339-343 listed four of the five symbols but missed the constant itself. Added `window._SPATIAL_HASH_MIN = _SPATIAL_HASH_MIN`. Also pre-emptively bridged three more class refs the leaf module uses unguarded: `Projectile` (called via `Projectile.release(proj)` at ~15 sites for the #20 P3 pool-return pattern), `GoldDrop` (called at `GoldDrop.release(drop)` / `GoldDrop.acquire(x, y)`), and `HolyMask` (called at `new HolyMask(enemy.x, enemy.y)` in the boss-drop path). All four are pre-existing game.js bindings — `_SPATIAL_HASH_MIN` is a top-of-module `const`, the three classes are existing ESM imports — so the bridge lines pick up at the existing leaf-module-bridge block without changing initialization order.
- **Crash at run start: missing leaf-module bridges (`_renderBossIntroCinematic`, `_renderBossDeathCinematic`, `_renderBossChoiceScreen`, `Enemy`, `_replaceArrInPlace`, `EvilMode`)**. Follow-on from the #173 phase 10 leaf-module extraction. `core/updateGameplayPre.js` was documented as resolving every renderer-owned helper "via bare-name global lookup — `window.X` bridges in the renderer", but six of those bridges were never added to `game.js`'s leaf-module-bridge block at ~line 2413. The first frame of `_updateGameplayPre` calls the three boss cinematic helpers unconditionally (`_renderBossIntroCinematic()` / `_renderBossDeathCinematic()` / `_renderBossChoiceScreen()`), so the very first gameplay tick threw `ReferenceError: _renderBossIntroCinematic is not defined` and the GameLoop bailed via CrashReporter. Added the three cinematic-helper bridges plus the three other latent gaps (`Enemy` — used unguarded by spawn paths at lines 517/562; `_replaceArrInPlace` — used by the tutorial-cap block at line 621; `EvilMode` — currently `typeof`-guarded so silently no-ops Evil Mode logic instead of crashing, exposed here so the guard actually finds it). All six are pre-existing game.js module-scope bindings (class imports / function declarations), so the new `window.X = X` lines pick up at the existing block-end without disturbing hoist order.
- **Crash at run start: `Cannot access 'runState' before initialization` + `isPhotoMode is not defined`**. Two regressions from the #173 phase 10 / #11 phase 8 cleanup. (1) `saveRunState()` in `game.js` declared a local `const runState = {…}` snapshot object whose name shadowed the imported `runState` singleton; the early-exit guard `if (!runState.gameRunning || runState.wave <= 0) return;` at the top of the function therefore read the local through the TDZ window before the snapshot was built, throwing on the first wave-start `saveRunState()` call from `resumeWaveGeneration()`. Local renamed to `payload`; the three internal touch sites (`saveData.savedRun = payload`, `console.log("Run saved at Wave " + payload.wave)`, and the `const payload = {…}` declaration) updated; the ~40 `runState.X` reads inside the object literal continue to reference the imported singleton as intended. (2) `core/updateGameplayPre.js` called `isPhotoMode()` via bare-name global lookup at the top of `_updateGameplayPre`, but `Camera.js` never exposed `window.isPhotoMode` (only `window.togglePhotoMode`), so the lookup threw on the next gameplay frame. Added explicit `import { isPhotoMode } from '../Camera.js'` to the leaf module — the same pattern the sibling leaf module already uses for `runState`. Both fixes restore the run-start path: `startGame()` → `advanceWave()` → `resumeWaveGeneration()` → `saveRunState()` → first frame of `_updateGameplayPre`.

### Changed
- **#173 — Update/draw split, Phase 10 (update halves extracted into leaf modules — closes #173)**. The two pure update helpers `_updateGameplayPre` (~612 lines) and `_updateGameplayMid` (~1454 lines) lifted out of `game.js` into `core/updateGameplayPre.js` and `core/updateGameplayMid.js`. Joins the previously-extracted `core/drawGameplayPost.js` + `core/drawGameplayMid.js` so all four pure gameplay halves now live in dedicated leaf modules; `game.js` keeps only the `_runGameplayFrame` orchestrator that calls them in order. **Singleton runState:** `RunState.js` gained `export const runState = createRunState()` so leaf modules can `import { runState } from '../RunState.js'` instead of coupling to game.js's module scope; `game.js` switched from `const runState = createRunState()` to importing the same singleton. `window.runState = runState` for DLC bare-name access. **Renderer-side plumbing:** game.js now sets `window.X = X` for every symbol the extracted update halves reach via bare-name global lookup — added `MeleeSwipe`, `PowerUp`, `MEMORY_STORIES`, `updateChaosObjective`, `checkChaosEvent`, `isWaveCleared`, `drawCoopDistanceWarning`, `updateDrawRevivalMarkers`, `createDeathBurst` on top of the pre-existing `Boss` / `applyScreenShake` / `TutorialMode` exposures from earlier phases. Three new `defineProperty` bridges for module-local state that the leaf modules mutate: `_enemyHashActive`, `_projectileHashActive` (spatial-hash active flags rebuilt each frame in `_updateGameplayMid`), and `masksDroppedInWave` (the holy-mask drop counter). **Server bridge:** `server/simulation/RendererBridge.js` updated to point at the new `core/updateGameplay{Pre,Mid}.js` paths. The `require()` call in `_tryLoad` still fails silently on ESM-only leaf modules (returns null), so the bridge is structurally correct but driving the helpers server-side still needs the loader to bridge ESM↔CJS or stub every bare-name global the helpers reach. Tracked as follow-up. **Closes the #173 arc:** ten phases shipped over the last 8 days. Build green, 83/83 Vitest, lint 95/95 baseline.

### Removed
- **#11 — RunState carve, Phase 8 (redundant window mirrors dropped)**. Cleanup pass closing out the eight-phase #11 carve. Dropped 9 `window.{player, player2, frame, meleeAttacks}` mirror writes that became no-ops once the Phase 7 `defineProperty` bridges started routing `window.X = v` through to `runState.X = v` — the explicit writes had been left in place during phases 2-7 so each phase commit could be reviewed independently. Dropped 13 `window._world.{enemies, projectiles, ...}` array-resync sites that became no-ops once the Phase 2 mutate-in-place pattern (`arr.length = 0`) made entity-array refs stable for the lifetime of the session — `_world.enemies` is bound once at `startGame()` after `World.createClientWorld()` and never needs to be re-bound. The per-frame `_world.{frame, wave, score, gamePaused, isLevelingUp, isShopping, currentWeather, currentObjective, bossActive}` scalar-copy block in `_runGameplayFrame` stays in place — `_world` does not read from `runState`, so primitives still need a fresh copy every tick. The defineProperty bridges themselves stay in place: they are the canonical access path for DLC + leaf modules. **#11 carve complete (all 8 phases shipped)**: `runState` now owns the run-scoped subset of `game.js` module-scope state — ~85 fields across entity arrays, run-lifecycle scalars, mode flags, story/objective/weather, combat/cinematic, players + revival + stats. This unblocks **#173 phase 10** (the `_updateGameplayPre` + `_updateGameplayMid` halves can now be extracted into `core/*.js` leaf modules since the state they mutate is reachable via the `RunState.js` ESM import) and **#5 ECS** (replacing per-entity classes with component arrays on `runState` is now a mechanical rename rather than a cross-cutting refactor). Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#11 — RunState carve, Phase 7 (players + revival + stats migrated)**. Final batch of the RunState carve. Five module-scope fields — `player`, `player2`, `p1RevivalMarker`, `p2RevivalMarker`, `currentRunStats` — now live on the `runState` container. ~961 bare references rewritten via the acorn-tokenizer script (most are `player.X` chains where the leading `player` identifier gets renamed; the `.X` member access stays intact). The five local declarations deleted. The pre-existing `currentRunStats` defineProperty bridge auto-redirected via the rename. Four new bridges added — `player`, `player2`, `p1RevivalMarker`, `p2RevivalMarker` — so external consumers (`EvilMode.js`, `Boss.js`, `Player.js`, `TutorialMode.js`, DLC) reading `window.player` always see the current runState ref instead of a stale snapshot. The scattered `window.player = runState.player` / `window.player2 = …` mirror writes throughout `startGame()` and wave-reset paths become idempotent after the bridge (setter assigns `runState.X` to itself); they are left in place for a Phase 8 cleanup pass. **#11 phases 1-7 complete** — `runState` now owns the run-scoped subset of game.js module-scope state. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#11 — RunState carve, Phase 6 (combat / cinematic migrated)**. Fourteen module-scope fields covering boss intro/death cinematics, boss choice screen, and player death cinematic — `bossDeathTimer`, `bossIntroTimer`, `bossIntroName`, `bossIntroSkippable`, `bossIntroCamStart{X,Y}`, `bossIntroCamTarget{X,Y}`, `_bossChoiceScreen`, `_bossChoiceFrame`, `_bossChoiceFocus`, `_bossChoiceGpPrev`, `isPlayerDying`, `playerDeathTimer` — now live on `runState`. ~107 bare references rewritten via the acorn-tokenizer script. Twelve local declarations removed (the loose `_bossChoiceGpConsumed` debounce flag stays local — it's never accessed outside `game.js` and was not in the Phase 6 schema). Existing `defineProperty` bridges for `isPlayerDying` and `playerDeathTimer` auto-redirected through the rename. One new bridge added for `bossDeathTimer` so TestingGrounds.js's `bossDeathTimer = 0` reset in `spawnBoss()` continues to land on the canonical state. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#11 — RunState carve, Phase 5 (story / objective / weather migrated)**. Thirteen module-scope fields covering story playback, objective tracking, and weather state — `currentStoryEvent`, `currentObjective`, `isStoryOpen`, `currentBiomeType`, `currentWeather`, `currentWeather2`, `weatherTimer`, `weatherDuration`, `weatherDuration2`, `weatherParticles`, `_weatherFlash`, `_weatherBolts`, `activeMutators` — now live on the `runState` container. ~288 bare references in `game.js` rewritten to `runState.X` via the acorn-tokenizer rename script. The 13 local `let`/`var` declarations deleted. Nine pre-existing `defineProperty` bridge entries (`activeMutators`, `weatherParticles`, `currentWeather`, `currentObjective`, `currentBiomeType`, `isStoryOpen`, `weatherDuration`, `_weatherFlash`, `_weatherBolts`) auto-redirected to `runState.X` by the rename. One new bridge added for `currentStoryEvent` (DLC `MazeUI.js` writes `window.currentStoryEvent = …`). The remaining three fields (`currentWeather2`, `weatherTimer`, `weatherDuration2`) are game.js-internal, so no bridges needed. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#11 — RunState carve, Phase 4 (mode flags migrated)**. Fourteen module-scope mode flags in `game.js` — `isCoopMode`, `isAICompanionMode`, `isEvilMode`, `isDailyMode`, `isWeeklyMode`, `isTutorialMode`, `isOnlineMode`, `isOnlineHost`, `isOnlineGuest`, `isVersusMode`, `isWorkshopMode`, `isTestingMode`, `isChaosShuffleMode`, `isSpeedrunMode` — now live on the `runState` container. `isPhotoMode` stays Camera-owned (not part of this migration). ~303 bare references rewritten to `runState.X` via the same acorn-tokenizer rename script from Phase 3 with two heuristic extensions: skip `get IDENT() {…}` / `set IDENT() {…}` accessor method names, and skip function/arrow parameter declarations by detecting the enclosing `(` and checking whether it's preceded by `function` (decl) or followed by `) =>` (arrow). The 14 local `let` declarations deleted. Eight new `defineProperty` bridge entries added for flags that previously had only ad-hoc `window.X = …` plain assignments (`isCoopMode`, `isAICompanionMode`, `isOnlineHost`, `isOnlineGuest`, `isVersusMode`, `isWorkshopMode`, `isTestingMode`, `isSpeedrunMode`) — these are now read/written through `runState.X` so the scattered `window.X = bool` flips throughout `startGame()` / `startOnlineMatch()` / mode-resets continue working and stay coherent with the canonical state. One object-shorthand site (`isStoryBossWave(..., { isDailyMode, isWeeklyMode })`) had been mis-rewritten to invalid `{ runState.isDailyMode, runState.isWeeklyMode }` and was hand-corrected to explicit form. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#11 — RunState carve, Phase 3 (run-lifecycle scalars migrated)**. Eleven module-scope scalars in `game.js` — `wave`, `score`, `frame`, `gameRunning`, `gamePaused`, `isLevelingUp`, `isShopping`, `bossActive`, `enemiesKilledInWave`, `coopZoom`, `_hitStopFrames` — now live on the `runState` container. Unlike Phase 2 arrays (stable identity via mutate-in-place), scalars are by-value primitives so they required a real rename: ~278 bare references in `game.js` rewritten to `runState.X` via an acorn-tokenizer-based one-shot script that walks the token stream and skips property access (`obj.IDENT`), object-literal keys (`{IDENT:`), and variable declarations (`let/var/const IDENT`). The eleven local `let`/`var` declarations deleted. The existing `Object.defineProperties(window, …)` bridge block — already in place from earlier `defineProperty`-mirror passes — now reads `get: () => runState.X` and writes `set: v => { runState.X = v; }`, so every DLC + leaf module that goes through `window.X` keeps working with no consumer change. `_hitStopFrames` added to the bridge to back `core/postProcess.js`'s `globalThis._hitStopFrames` read. Three follow-up fixes after the rename: a function parameter `function _getStoryArcLabel(wave, hero)` had been rewritten to `function _getStoryArcLabel(runState.wave, hero)` and was reverted (the script's "skip declarations" heuristic didn't cover param positions); two object-shorthand sites `{… , score, … }` and `{ wave, …}` mis-rewrote to `{… , runState.score, …}` and were expanded to `score: runState.score` / `wave: runState.wave` explicit form; the `GAME_API` accessor block had six `get/set IDENT()` method names mis-rewritten to `get runState.IDENT()` (invalid syntax) and was hand-corrected back to the legal accessor form while the bodies stayed as `return runState.IDENT;` / `runState.IDENT = v;`. Build green, 83/83 Vitest, lint 95/95 baseline.

### Changed
- **#11 — RunState carve, Phase 2 (entity arrays migrated)**. The 11 module-scope entity arrays in `game.js` — `enemies`, `projectiles`, `particles`, `floatingTexts`, `meleeAttacks`, `holyMasks`, `goldDrops`, `cardDrops`, `memoryShards`, `powerUps`, `companions` — now live on the `runState` container created via `createRunState()` from Phase 1. `game.js` keeps the existing bare-name idiom (~330 references in this one file) via a destructured `const { … } = runState;` block, so every existing read/mutate site is unchanged. Array identity is now stable for the lifetime of the session: all 33 in-game `arr = []` reassignments + TutorialMode.js's 4 `window.arr = []` reset sites converted to mutate-in-place via `arr.length = 0`. Four sites that did `arr = arr.filter(...)` / `arr = src.map(...)` routed through a new `_replaceArrInPlace(target, src)` helper so they also preserve the ref. Seven now-stale `defineProperty` bridge entries (`enemies`, `projectiles`, `particles`, `floatingTexts`, `companions`, `holyMasks`, `goldDrops`) dropped — the one-time `window.X = X` exports at init bind directly to the permanent runState refs, so DLC + leaf modules reading `window.X` (or `globalThis.X` in `core/*.js`) keep working with no code change. Sprinkled `if (window._world) window._world.X = X` resyncs (~15 sites) are now idempotent no-ops; left in place for Phase 8 cleanup. 83/83 Vitest, lint 95/95 baseline, build green.

### Added
- **#11 — RunState carve, Phase 1 (schema + factory)**. New `createRunState()` factory in `RunState.js` defines a ~70-field container covering the run-scoped subset of `game.js`'s ~540 module-scope globals: entity arrays (Phase 2), run-lifecycle scalars (Phase 3), mode flags (Phase 4), story/objective/weather (Phase 5), combat/cinematic (Phase 6), and players + revival + stats (Phase 7). Full JSDoc `@typedef RunState` annotates every field; defaults mirror `game.js`'s initial values byte-for-byte so later phases can flip `let foo = X` into `runState.foo` without behavior drift. Factory mirrored on `window.RunState.createRunState` for DLC access. No `game.js` consumers are wired up yet — this phase only lands the schema so later phases (tracked in `tasks/active-work.md`) migrate one slice at a time. Unblocks #173 phase 10 (update-half leaf-module extraction) and #5 (ECS).
- **#35 — Modern post-processing stack (WebGL fragment shader)**. New `core/postProcess.js` leaf module renders a single full-screen post-pass after `_drawGameplayPost()` each gameplay frame. An overlay `<canvas id="postFxCanvas">` (`position:absolute`, `pointer-events:none`, `z-index:2`) sits between the 2D `gameCanvas` and `#ui-layer`; each frame the 2D canvas is uploaded as a texture (`gl.texImage2D(TEXTURE_2D, …, gameCanvas)`) and re-presented through a single fragment shader that applies: (1) **bloom** — 8-tap ring sample at ~1.5 px radius, luminance excess above 0.75 threshold accumulated and added back at 0.6 strength so bright projectiles / hero auras gain a soft glow halo; (2) **chromatic aberration** — R/B sample offset scaled by `_hitStopFrames / 12 * 0.012`, so the existing hit-stop window from #39 doubles as the chromatic trigger and decays in lockstep; (3) **vignette** — radial smoothstep darken keyed to `(1 - player.hp/player.maxHp) * 0.7`, pulsing at 6 Hz once HP drops below 30 %; (4) **per-biome color grade** — vec3 multiplier table covering fire / water / ice / plant / metal / black biomes. HTML HUD (`#ui-layer`, z-index 5) sits above the overlay so HUD text and icons never go through the shader and stay crisp. New `postFX: true` setting in `Config.js` Visuals block plus a Post-FX toggle row in the Options panel (mirrors the Screen Shake row pattern) and an `'postFX': 'opt-postfx-btn'` entry in `UI/Options.js` buttonMap. Auto-disabled under `reducedMotion` (same gate the weather flash already uses). Server-safe: every code path guards on `_hasDOM` + `_glReady`, so the module imports cleanly into the server simulation parity test (loader.js's no-op canvas can't get a real WebGL context — `_glFailed` latches and `renderPostFX()` becomes a no-op). WebGL context-loss / -restore handlers wired. Photo mode (#51) works for free since the shader samples the canvas each frame, so the frozen scene re-renders correctly while the camera pans.

### Changed
- **Loading screen redesign — Elemental Ring**. Replaced the generic `<h1>LOADING</h1>` + yellow spinning circle + "Loading Assets..." monospace text in `game.html` with a branded loading screen themed to the game's five-element identity. New layout: centered `logo.png` with a soft glow pulse, a 220 px SVG progress ring split into five colored arc segments (Fire `#ff5e5e`, Water `#5ec8ff`, Ice `#bfe9ff`, Plant `#6cd06c`, Metal `#bdbdbd`) that fill clockwise as load progresses, a numeric `XX%` readout dead-center inside the ring, five hero element pips below the ring that desaturate by default and light up + pulse once their arc completes, a rotating tip line (15 gameplay hints — each verified against the codebase so claims like dash mechanics, combo thresholds, mutator effects, weather types, and revive-in-co-op are accurate; cycles every 4.4 s with crossfade), 28 drifting elemental particles in the background tinted to each element color, a soft radial vignette + grain, and a small `vX.Y.Z` pill in the bottom-right corner. Responsive: ring scales down to 180 px under 540 px wide; honors `prefers-reduced-motion`. Progress is driven by real signals — new `window.reportLoadingProgress(pct)` API (monotonic, never moves backward) consumed by an inline IIFE driver in `game.html` that runs before the deferred `main.js` module so the ring renders at first paint. Wiring: `loadGame()` resolves → 20 %; `DLCManager.init()` gained an `{ onProgress }` option that fires per-DLC settlement, driving the ring from 20 % → 95 % across enabled DLCs; `_launchMenu()` in `game.js` reports 1.0 and calls a new `window.__loadingScreenCleanup()` to stop the tip + creep timers before the existing 0.5 s fade-out. An idle creep (capped at 88 %) covers the gap between paint and the first real signal so the ring never sits frozen at 0 %. Existing fade transition into the press-start screen unchanged.

### Changed
- **#194 — DLC dynamic-import migration + window-shim retirement (closes #171 Phase 2)**. Every DLC `.js` file now imports its renderer dependencies explicitly instead of relying on the global pollution from the per-file `window.X = X` shims. `dlc/DLCManager.js`'s `loadScript` switched from `await import(/* @vite-ignore */ url)` to a static `import.meta.glob('./*/*.js')` manifest — Vite now statically discovers every DLC file at build time, follows each one's transitive imports, and emits 59 code-split chunks under `dist/assets/<DlcFile>-<hash>.js` (one per hero / biome / boss module). Each chunk is fetched only when `DLCManager.loadScript` is called for the matching DLC, preserving the lazy-load contract from #29 P3. `vite.config.mjs` dropped `dlc/` from `STATIC_DIRS` because the bundler now owns the DLC output; the prior raw-copy step would have duplicated those files at stale paths. After every DLC and renderer file gained the explicit imports it actually needs, the per-file `window.X = X` shims went: `AltarData.js`, `Boss.js`, `Companion.js`, `Enemy.js`, `MemoryShard.js`, `MemoryStories.js`, `Player.js`, `shared/world.js`, `Entities/Particle.js`, `Entities/Projectile.js`, `Entities/FloatingText.js`, `Entities/GoldDrop.js`, plus the trailing `drawHeroSprite`/`shadeColor`/`cachedRadial` block in `Utils.js`. Twelve shim lines removed. Remaining intentional shims: `window.chaosState` (deliberate extension point for DLCs to mutate run-time chaos state), `window.bumpDamageSource` (game.js helper referenced bare-name by Player.js — keeping it inline avoids a circular import). Main bundle dropped from ~862 KB → ~842 KB after tree-shake. 83/83 Vitest, lint cap moved to 96 (was 95) to absorb a single new unused-vars warning from a vestigial `WindBosses` import in `dlc/waker_of_winds/index.js`.

### Changed
- **#171 — ESM migration completion, Phase 1: single-entry renderer bundle**. New `main.js` is the sole renderer entry; it imports every previously-listed module in the same declaration order via side-effect-only imports (each module's body still runs in original order, preserving the legacy load semantics where many files set `window.X = X` shims for cross-file global access). `game.html` collapsed from 70 separate `<script type="module">` tags to one `<script type="module" src="main.js">`. Vite/Rollup now sees the entire renderer as one dependency graph rooted at `main.js` — bundles to a single `dist/assets/main-<hash>.js` chunk (~862 KB / ~245 KB gz), tree-shakes unreachable exports, and cuts the production boot waterfall from 70 sequential module fetches to one. `vite.config.mjs` simplified: dropped the `collectClassicScripts()` post-build copy step (the entries it used to enumerate are now in the bundler graph) and the `_CLASSIC_WARN_RE` `onwarn` filter (no classic-script warnings to suppress anymore). Phase 2 initial pass removed 8 dead `window.X = X` shims after auditing each for DLC + bare-name renderer consumers: `MeleeSwipe`, `OnlineTestBot` (class only — the singleton stays), `InfoDialogueManager` (class only), `MenuBackground`, `CompletionMenu` (Museum.js gained an explicit import), `mulberry32` (×2 redundant assignments in Utils.js — game.js + tests already import it), `clearGradientCache` (zero consumers anywhere), `applyOneHandedScheme` (UI/Options.js gained an explicit import; dropped a duplicate inline fallback branch). Build / dev server / 83/83 Vitest / lint warning cap (95) all unchanged. **Full Phase 2 — removing the remaining ~12 shims (Particle, Projectile, FloatingText, Player, Boss, Enemy, Companion, MemoryShard, MEMORY_STORIES, ALTAR_TREE, etc.) — gated on DLC ESM migration (new follow-up #194)**: 80+ DLC files reference Entity/Manager classes by bare name (e.g. `new Particle(…)`, `new FloatingText(…)`) which resolves only because the renderer modules still set `window.X = X`. Removing those shims now would silently break every DLC.

### Changed
- **#177 — applyDamage pipeline coverage extended to hero-specific paths**. The `applyDamage(target, dmg, opts)` helper landed under #18 with five cookie-cutter sites (acid-fog ×2, lava, explosive-mutator, exploder-elite). This pass extends it to eight more hero-specific damage paths so they also get free coverage of `target.isInvincible`, `target.customOnDamage` shield hooks, optional floating-text + sfx, and the `recordPlayerDamage` / `currentRunStats.damageTaken` bookkeeping when the target is the player: **Thornmail reflect** in `game.js` (drops the bare `enemy.hp -= 20`, keeps the manual REFLECT pop + explosion); **LoveHero Heartbreak burst** (`cl1` mutation, 1.5× rangeDmg on charm expiry); **TimeHero delayed lightning** (AOE secondary hits on 130 px radius at `_delayedLightnings` resolve); **TimeHero Burning Moment** (`ct3` mutation, fire DOT every 45 frames on time-slowed enemies); **LightningHero ground-strike AOE secondary** (radius hits when the bolt misses its primary target); **AirHero Hurricane** (passive 220 px aura tick every 10 frames); **AirHero Tempest Aura** (ambient 150 px effect-ring DOT); **SpiritHero Bell reflect** (300 px AOE on damage taken). High-frequency DOT sites pass `noFloatText: true` to avoid spamming numbers — the existing particle/explosion visuals already convey the damage — and pass `sfx: null` so the per-tick `damage` SFX doesn't fire on every enemy in the radius. Burst sites (Heartbreak) keep the floating number. Each DLC site reads `applyDamage` off `window` with a typeof guard + bare-`hp -=` fallback so the DLC still works when loaded server-side (`window` is undefined under Node's `require()` path).

### Changed
- **#188 — Lint warning baseline cleanup (371 → 95, 74 % reduction)**. The flat ESLint config from #14 left a 371-warning floor catalogued but otherwise allowed to drift. Cleanup pass: (1) `eslint . --fix` cleared 152 auto-fixable cases (mostly `prefer-const`, formatting); (2) `no-unused-vars` rule extended with `caughtErrorsIgnorePattern: '^(_|e|err)$'` and `destructuredArrayIgnorePattern: '^_'` so the project's longstanding `catch (e) { /* swallow */ }` idiom no longer floods the warning channel; (3) new override block for `dlc/**/*.js` and `Biomes.js` accepts the canonical callback signature names (`player, arena, enemies, projectiles, wave, pl, ar, b, …`) — these are required by cross-DLC contracts and renaming the unused half to `_pl`/`_arena` would obscure the contract for new DLC authors; (4) `no-var` rule turned off project-wide — the ~200 legacy `var` declarations are the chosen idiom for module-scope DLC-exposed variables paired with `defineProperty(window, name, {get/set})` bridges, and flagging each one swamps the warning channel for zero behavioral upside; (5) deleted 8 truly-unused imports from `game.js` (`SHAKE_PRESETS`, `triggerScreenShake`, `triggerVibration`, `togglePhotoMode`, `spawnEnemy`, `spawnBoss`, `enemiesNeededForWave`, plus an unused `path` local); (6) added explicit `window.openAltar`, `window.shuffleHero`, `window.chooseUpgrade` exports so the lint sees the cross-file references (the functions were already used from Altar.js / ChaosMode.js / inline `onclick` handlers via the script-tag global pollution). `npm run lint` now runs with `--max-warnings 95` so any new code that introduces a regression breaks CI — the prior pattern of warnings drifting up silently is closed.

### Added
- **#192 — Boss intro skip on re-encounters**. The boss intro cinematic shipped in `e386a79` now supports a skip for bosses the player has already seen. New per-save flag `saveData.global.bossesSeen` records each boss-id on first cinematic; on the second encounter onward, pressing **Escape** (keyboard) or **B / Circle** (controller) ends the cinematic immediately. First encounters always play in full so new players never miss the reveal. A small `Press ESC or B to skip` hint fades in once the banner is fully visible and only on skippable runs. Implemented in `game.js`: new `bossIntroSkippable` runtime flag set at the cinematic-start branch after reading the previous `bossesSeen[storyBossId]` state, then stamped; reset alongside `bossIntroTimer` at the same two sites that already cleared the cinematic state. Keyboard skip wires through the existing `inputManager.onKeyDown` ahead of the pause handler so the same key doesn't also toggle pause. Controller skip lives at the top of the `handleGamepadMenu` B-button block so it runs even though no `uiState` matches during gameplay.

### Security
- **#182 — Admin login audit log**. Every `POST /api/admin/login` attempt is now persisted to a new SQLite table `admin_login_attempts` (`id`, `ts`, `ip`, `user_agent`, `success`, `reason`). All five outcome paths are logged: `success` (reason `null`), `bad_password`, `no_password`, `verify_error`, `rate_limited`. To make the rate-limit case loggable, `rateLimit({ ... })` in `server/server.js` gained an optional `onReject(req)` callback fired before the 429 response; the existing 16 call sites that don't pass `onReject` are unaffected (default no-op). New helper `_logAdminLoginAttempt(req, success, reason)` uses a prepared INSERT and runs a rolling prune to the most recent 10 000 rows every 100 inserts. New admin-only endpoint `GET /api/admin/login-attempts?success=true|false&limit=N` (max 500 rows) returns `{ attempts, total, last24h }` where `last24h` aggregates successes, bad-password, rate-limited, no-password, verify-error counts plus distinct IP count over the last 24 h. Admin dashboard Technical tab gains a four-card summary row (Admin Logins / Bad Password / Rate-Limited / Unique IPs, all over 24 h) and a "Admin Login Attempts" table showing time / result pill / reason pill / IP / truncated user-agent. End-to-end verified: success / bad / empty / rate-limited / read flows all produce expected rows. Pairs with #181 (fail-secure JWT_SECRET) and the 715f03e bcrypt+JWT hardening pass — completes the admin auth posture.

### Security
- **#181 — JWT_SECRET fail-secure on production boot**. `server/server.js` now exits 1 with a `[security] FATAL` message when `NODE_ENV=production` and `JWT_SECRET` is still the placeholder default `'change-this-secret-in-production'`. The previous behavior only logged a warning, letting the server boot and sign session/admin JWTs with a public-knowledge secret. Mirrors the fail-secure pattern already in place for `ADMIN_PASSWORD_HASH` (admin-auth hardening pass). Dev / test paths unchanged — same warning is logged once at boot but the process proceeds so local workflows don't need a JWT_SECRET in every shell. Boot paths verified manually: prod without JWT_SECRET → FATAL exit; prod with JWT_SECRET → boots clean; dev without JWT_SECRET → warning, proceeds.

### Changed
- **#190 — Upgrade `better-sqlite3` from `^9.4.3` to `^12.10.0` for Node 24 prebuilt binaries**. The v9 series shipped prebuilts only up to `node-v115`; on Node 24 (NAPI v137) the install used `--ignore-scripts` as a workaround during the admin-hardening pass, leaving the JS layer in place but no native binding — `require('better-sqlite3')` failed at runtime. v12.10.0 ships official prebuilds for Node 20/22/23/24/25/26, so `npm install` in `server/` no longer needs to compile from source. API is backward-compatible for our usage (`Database`, `db.exec`, `db.prepare(...).get/all/run/iterate/pluck`, `db.transaction`); smoke-tested via in-memory CRUD and full server boot. No application code changes required.

### Closed — already shipped
- **#189 — `no-foreach-splice` rule violations**. The 8 instances surfaced when the rule was first wired up (#13) have all been fixed since; current `npx eslint .` reports 0 occurrences of the `forEach(...) + arr.splice(index)` pattern. Verified by running the rule against a synthesized positive case to confirm it still fires. Item closed in `tasks/improvements-2026-05-10.md`.

### Security
- **Admin dashboard hardening — bcrypt + JWT session + fail-secure boot**. Replaces the plaintext-password-as-bearer scheme used by every `/api/admin/*` endpoint. New extracted module `server/adminAuth.js` exposes `resolveAdminHash`, `signAdminToken`, `verifyAdminToken` so the logic is unit-testable without booting Express. `server/server.js` boot now resolves admin credentials via `ADMIN_PASSWORD_HASH` (bcrypt) only — the plain `ADMIN_PASSWORD` env is no longer honored; when set, a loud warning is logged and the value is ignored. In production with no hash configured, the process exits 1 (`[admin] FATAL: ADMIN_PASSWORD_HASH not set`). In development with no hash, an ephemeral random password is generated via `crypto.randomBytes(12)` and the plaintext is printed once to the boot log so the operator can paste it into the dashboard. New `POST /api/admin/login` rate-limited 5 attempts / 900 s per IP via the shared `makeRateLimiter` token bucket; on success returns `{ token, expiresAt }` where `token` is a JWT carrying `{ kind: 'admin' }` with an 8 h TTL. `requireAdmin` rewritten to verify the JWT (rejects non-admin kinds with 403 — a stolen player/game-session JWT signed with the same secret can no longer escalate), and the password no longer traverses the network on every poll. Additional boot warning fires when `JWT_SECRET` is still the placeholder default in production. `server/dashboard.html` updated: `tryLogin()` now POSTs to `/api/admin/login` and stores `{ token, expiresAt }` in `sessionStorage` under `admin_session` (legacy `admin_token` key wiped on every boot); `fetchAll()` sends the JWT as Bearer, watches for 401/403 on any poll, and auto-logs-out when one is seen; expired tokens trigger logout before the next refresh. New CLI helper `scripts/admin-hash.js` (`npm run admin-hash <password>`) prints a `ADMIN_PASSWORD_HASH=…` line ready for `.env` (refuses passwords under 8 chars, uses bcrypt cost 10 to match the existing user-register hash cost). New `tests/adminAuth.test.js` (18 assertions): covers `verifyAdminToken` (missing header / non-Bearer / garbage token / wrong-signature / expired / non-admin kind / no kind / valid admin), `signAdminToken` (kind + TTL math + default), `resolveAdminHash` (hash path / legacy warning / production fail-secure / dev ephemeral), and the shared 5-per-15-min token bucket (capacity, per-IP isolation, refill).

### Added
- **#3 — JSDoc schema typedefs for save data, hero stats, world object, and game config**. New `types/schemas.js` centralises JSDoc `@typedef` definitions for the four cross-cutting shapes called out in the improvement list — `SaveData` (per-hero slots, `GlobalSaveStats`, `MetaUpgrades`, story/altar/chaos/tutorial sub-bags, schema `version`), `HeroStats` + `BaseHeroStats` + `StatBreakdown` (output of `window.getHeroStats`), `GameConfig` (mirrors `defaultConfig` in `Config.js` — audio sliders, accessibility flags, key/gamepad bindings, telemetry/cloud-save metadata, HUD layout overrides), `WorldState` + `ArenaState` + `ArenaCamera` + `ArenaObstacle` + `BiomeZone` (partial — full RunState carve tracked under #11), `GameplayConstants` (mirrors `GAMEPLAY` in `Constants.js`). File is runtime-free (`export {}`); typedefs are reached via `import('./types/schemas.js').Name` from anywhere. New `jsconfig.json` sets `allowJs: true` + `checkJs: false` so VSCode/tsserver picks up the typedefs for IntelliSense across `Managers/`, `Entities/`, `UI/`, plus key root files, without flooding the project with type errors on existing code; opt-in per file with `// @ts-check`. `@type` references wired at the schema-defining sites: `Constants.js` (`BASE_HERO_STATS`, `GAMEPLAY`), `Config.js` (`defaultConfig`), `game.js` (`defaultSaveData`), `Player.js` (`getHeroStats` `@param` + `@returns`), `Arena.js` (`camera` / `obstacles` / `biomeZones` field annotations), `Managers/SaveManager.js` (full `@param`/`@returns` on `encodeSaveData`, `decodeSaveData`, `saveGame`, `loadGame`, `_migrate`, and the `MIGRATIONS[]` array shape). Hovering any of these in VSCode now shows the full schema; renaming fields surfaces in the IntelliSense view across call sites. Prevents the regression class cited in the original ticket (e.g. `saveData[player.type].prestige` misuses).
- **TLS / HTTPS setup scripts for the server**. New `scripts/server-tls-letsencrypt.sh` (production: certbot standalone or webroot, takes `SERVER_HOST` + `EMAIL` env vars, optional `MODE`/`WEBROOT`/`STAGING`, writes resolved cert + key paths into `server/.env`), `scripts/server-tls-selfsigned.sh` (local/LAN: openssl with SAN entries for hostname + 127.0.0.1 + optional `SAN_IP` list, configurable `DAYS`/`KEY_BITS`/`OUT_DIR`, paths into `server/.env`), `scripts/server-tls-renew.sh` (cron/systemd hook around `certbot renew --deploy-hook 'systemctl restart $SERVICE'`, configurable `SERVICE`/`POST_HOOK`), `scripts/server-start.sh` (env-loading wrapper that validates TLS files and prints the resolved `http(s)://host:port` + `ws(s)://…/ws` URLs before exec'ing node), `scripts/server-systemd.service.example` (hardened sample unit with `ProtectSystem=strict` + `CAP_NET_BIND_SERVICE` for port-443 binding without root), and `scripts/server-setup.md` (end-to-end recipe). All scripts are env-driven (`SERVER_HOST`, `PORT`, `ALLOWED_WS_ORIGINS`, `TLS_CERT_PATH`, `TLS_KEY_PATH`, `TLS_CA_PATH`, `JWT_SECRET`, `ADMIN_PASSWORD`); `server/.env.example` rewritten to document every knob with inline comments. No `server.js` changes required — TLS plumbing was already in place at `server/server.js:904-925`.

### Added
- **#98 — Telemetry / analytics opt-in pipeline**. Lightweight in-house analytics so balance decisions can be data-driven. New `Managers/TelemetryManager.js` mirrors the `CrashReporter` shape: in-memory ring buffer, batched POST to `/api/telemetry` (flush every 30 s or at 20 events, `navigator.sendBeacon` on `pagehide`/`visibilitychange`), self rate-limit (60 events/min), strict event whitelist (`run_start`, `wave_completed`, `level_up`, `run_end`) and field whitelist (`hero`, `mode`, `biome`, `wave`, `timeSec`, `outcome`, `upgradePicked`, `deathSource`, `dailySeed`) so PII can't leak by accident. Three new `gameConfig` keys: `telemetryEnabled` (default `false` — opt-in), `telemetryConsentSeen` (drives first-launch modal), `telemetryInstanceId` (random UUID generated lazily on first opt-in via `crypto.randomUUID()` with RFC-4122 fallback — no account linkage). First-launch consent modal (`#telemetry-consent-modal`, z-index 9600) with Enable / Decline / "Ask me later" buttons; "Ask me later" leaves `consentSeen` false so the prompt returns next boot. Options row "Anonymous Analytics" under Crash Reports; toggling from Options auto-flips `consentSeen` so the modal doesn't re-prompt. Run lifecycle hooks wired in `game.js`: `run_start` at the bottom of `startGame()` before `advanceWave()`, `wave_completed` at the top of `advanceWave()` (skipped on the wave 0→1 transition since no wave actually played), `level_up` at the top of `chooseUpgrade()` with the picked id, `run_end` at the top of `gameOver()` with `outcome`/`wave`/`totalTimeSec`/`deathSource` plus an immediate `flush()`. Server adds `telemetry_events` SQLite table (indexed on `(event, ts)` and `(hero, event)`) and three endpoints: `POST /api/telemetry` (unauthenticated, IP rate-limited 60/10min, 32 KB payload cap, 50-event batch cap, single prepared-statement transaction, rolling prune at 500k rows), `GET /api/admin/telemetry/summary` (per-hero runs/wins/win-rate/avg-end-wave/avg-time, dropoff map keyed by hero with `{wave, reached, pct}` points, top-30 upgrade picks, top-20 death causes), `GET /api/admin/telemetry/raw?event=…&limit=…` (per-event spot-checks, 500-row cap). Admin dashboard gets a new **Analytics** tab between Balance and Events: 4-card summary, per-hero run-stats table (with colour-coded win-rate cells), drop-off curve canvas (per-hero coloured polylines on a 0–wave-50 x-axis with 25% gridlines and a legend), top-upgrades and top-death-causes tables. New `tests/telemetryManager.test.js` (Vitest, 13 assertions) covers opt-in gate (3 cases), event whitelist (3 cases including PII strip), batching auto-flush + manual flush + empty-buffer no-op + sendBeacon path + disabled-mid-buffer drop, and the 60/min client-side rate limit.
- **#126 — Music ducking / sidechain compression for SFX cut-through**. `Managers/AudioManager.js` inserts a new `_musicGain` `GainNode` between the existing `_musicFilter` (lowpass) and `destination` in the music-bus Web Audio graph. Two duck modes share the gain stage: `_duckMusicPulse(holdMs, depth)` schedules an attack→hold→release envelope (50 ms attack, ~140 ms hold, 240 ms release, default 0.5 depth) for one-shot high-impact SFX; `_duckMusicHold(depth)` is reference-counted for held ducks during voice lines (0.7–0.8 depth, restored on `onended`/`onerror`/safety-timeout via an idempotent `_restored` flag). Pulse triggers are curated: `wave_completed`, `achievement_unlocked`, `challenge_success`/`_fail`, all `boss_makuta_*`, `boss_tank_phase2`, `boss_summoner_*`, `boss_rhino_charge`, `boss_stomp`, `death`, `twin_event`, `weather_thunder_crack`, `pickup_card`/`_mask`, and any `level_up*` SFX (per-attack / per-damage SFX excluded so the bus doesn't pump every frame). New static `AudioManager.DUCK_TRIGGER_KEYS` Set + `DUCK_TRIGGER_PREFIX` regex drive `_shouldDuckOnSfx`. Hold-duck supersedes pulse-duck so a voice line in progress isn't interrupted. `playVoice` and `playHeroExclamation` now duck at the graph level (cleaner than the old per-track `HTMLAudio.volume` writes, which fought `updateSettings()`'s `_baseVolume × _categoryMultiplier` reapply); legacy per-track volume dip kept as fallback when no `AudioContext` is available. `_routeActiveMusic()` helper extracted from `setPauseFilter` ensures every currently-playing music track is routed through the graph before each duck. Self-restoring envelopes guarantee the bus returns to unity even if `onended` doesn't fire (failed file load / cross-origin).
- **#146 — Nightly headless test harness**. New `scripts/nightly-headless.js` drives N (default 100) authoritative `GameSession` simulations directly (no WS/server boot) with random bot inputs matching the `test-arena.js` / `OnlineTestBot.js` wire shape, auto-resolves level-ups, and monkey-patches `Date.now()` to a per-tick virtual clock so wave spawn pacing advances under tight-loop ticking. Captures per-run final wave / score / HP, max enemy + projectile load, and tick wall-time p50/p95/p99; aggregates across runs into `nightly-report.json`. Exits non-zero if any run throws. New npm script `test:headless` (env knobs `RUNS`, `TICKS`, `SEED`, `MODES`, `REPORT`, `QUIET`). New GitHub Actions workflow `.github/workflows/nightly.yml` runs the harness daily at 04:17 UTC (and on `workflow_dispatch` with overridable runs/ticks/seed inputs), uploads the JSON report as a 30-day artifact. Local 100-run smoke (1800 ticks each): 1.5 s wall, ~100k ticks/sec, p99 ≈ 0.12 ms, peak ~11 enemies / 43 projectiles, 0 crashes. `nightly-report.json` added to `.gitignore`.

### Added
- **Reliquary biome scenery expanded with cathedral fixtures and ritual elements**. `dlc/radiance_of_ruin/ReliquaryBiome.js` now precomputes and renders: 6 hanging chandeliers (chain from ceiling, ring of 6 candle orbs with glow halos using `lighter` composite, central pendant teardrop, gentle sway + flicker), 22 floor candles (cream tapers + drip highlights + flickering teardrop flames with radial glow), 4 hanging tapestry banners between arches (gradient cloth + gold trim + eye sigil + tassels, with subtle sway), 7 eye-of-providence floor medallions (triangle frame + ellipse eye + iris + 8 outer dots, slow blink pulse), 4 large sunburst inlays (16 alternating-length rays + central disc with halo), 8 edge mask shrines (alcove + arched top + stylised mask glyph + paired candles at base), a large central rose window above the arches (12 stained-glass petals in alternating hues + outer ring + central bulb + soft halo), and a rising gold ember particle stream spawned from candles. Banners + medallions + sunbursts + shrines + rose window render in `drawBackground` (entities draw on top); chandeliers + candles + embers render in `draw` for live animation.
- **Dreamspace biome ambience expanded with more dream iconography**. `dlc/radiance_of_ruin/DreamspaceBiome.js` now precomputes and renders: 36 constellation lines connecting nearby `bgStars` pairs (subtle pulsing alpha), 5–7 large crescent moons (full disc + offset bg-colored cutout to form the bite, soft hue halo, gentle pulse), 6 large nebula color blooms (purple/blue gradients rendered behind stars with `lighter` composite), and an occasional drifting "Z" sleep puff that rises and rotates over ~6 seconds. Crescents + nebulae + constellations live in `drawBackground` so entities draw on top; Z puffs live in `draw` for animated foreground.

### Removed
- **Floating clock-face shards removed from Dreamspace biome**. The watches read as a Time-biome copy rather than dream iconography. Dropped `clockShards` field, its generation block, its drift/spin tick in `update`, and its render block in `draw` from `DreamspaceBiome.js`.
- **Crimson Greenhouse biome scenery expanded with more vines, thorns, and floral elements**. `dlc/radiance_of_ruin/CrimsonGreenhouseBiome.js` now adds: 18 hanging vines suspended from the glass roof (sway with `Math.sin` over time, 5 alternating leaves down the stem, terminal red flower), 10 climbing edge-vines (5 per left/right wall with leaves + tip rose), 24 scattered floor rose buds (4-petal layout with phase-pulsing soft halo + small green base leaf), 10 briar patches (7–11 quadratic-curve thorn spokes around a central bud + red thorn dots on the spoke ring), an additional falling-leaf particle stream (slower vy, larger size, dark green/red hues, drawn with a central vein) alongside the existing rose petals, and bumped the edge vine silhouette count from 14 → 26 with each vine now layered (dark + highlight stroke), studded with bezier-positioned ellipse leaves with veins, and red thorn nubs along the path. Particle render branches on `kind` for petal vs. leaf shape.

### Added
- **Full Altar of Mastery convergence coverage for Radiance of Ruin heroes**. Each of Light/Thorn/Dream now has a Prestige-5 convergence with every other hero in the convergence pool (fire, water, ice, plant, metal, earth, lightning, gravity, void, air, spirit, chance, sound, poison, time, love, psycho, mirror, smoke) plus the other two Ruin heroes. Expanded `dlc/radiance_of_ruin/index.js` `ruinMutations` from 12 entries (4 per hero) to 60 entries (19 per hero + 3 intra-Ruin + trio). All new entries use the existing `cv_ror_` ID prefix and the standard `{ id, req, type:'mutation', name, desc }` shape registered via `ALTAR_TREE.convergence.push`. Manual entry's Convergence Mutations section updated to reflect the broader coverage with new representative highlights.

### Added
- **In-game Manual entry for Radiance of Ruin DLC**. New `ruin` tab in `Tutorial.js` `Manual.content` covering all three heroes (Light, Thorn, Dream) — resource mechanics (Mask Integrity / Blood Bond / Lucidity), specials (Revelation, Crimson Garden, Dreamscape), ultimates (The Unveiling, The Reckoning, The Long Sleep), per-hero skill tree highlights, Altar of Mastery nodes, all 13 convergence mutations, the three biomes (Reliquary, Crimson Greenhouse, Dreamspace), and what's NOT included (no story mode / new enemies / cards / music). Added `btn-tutorial-ruin` button in `game.html` sidebar and DLC-active toggle in `Manual.open()` matching the existing pattern.

### Added
- **#169 — Configurable HUD layout**. Combo display, minimap, bottom player HUD (HP/XP/MELEE + buffs + special), and the P2 HUD (local + online co-op) can each be repositioned. New `Managers/HUDLayout.js` reads per-element `{ left, top }` overrides from `gameConfig.hudLayout`, applies them as inline styles after DOM ready, and exposes an edit mode (Options → "Customize HUD" → EDIT). Edit overlay dims the screen, force-shows the editable elements so they're moveable from menus too, and supports three input methods: (1) mouse — drag any highlighted element, Shift snaps to viewport edges; (2) keyboard — Tab cycles focus, Space grabs/drops, arrows nudge (Shift = 4× step), Esc cancels, Enter saves; (3) controller — D-pad / LB-RB cycles focus, A grabs (orange pulse outline), D-pad / left stick nudges in move mode, B cancels, Start saves, Y resets. Initial inline styles captured once per element so Reset preserves HTML-authored positions like the minimap's `top:14px; right:14px`. Toolbar + backdrop sit outside `#ui-layer` so their z-index escapes the layer's stacking context and paints above `#menu-overlay`; during edit mode `#ui-layer` is also raised to `z-index: 9000` so the editables themselves rise above the main menu.

### Fixed
- **Combo display overlapped the minimap when both were visible**. `#combo-display` lived inside `#hud-top-left`'s flex row, so high-combo scales pushed it horizontally into the top-right minimap region. Extracted into its own positioned element (`position: fixed; top: 70px; left: 20px` by default) so the two HUD widgets never share screen real estate. Now also moveable via #169.

### Added
- **`LORE.md` canonical lore compendium** at the project root. Synthesizes world cosmology, the Five Friends' origins and trials, the Black Hero, antagonists (Makuta, Green Goblin, Entropy Mage), the Museum/Memory Shard meta-narrative, the Altar of Mastery, and per-DLC story arcs (Champions of Chaos, Faith of Fortune, Waker of Winds, Symphony of Sickness — Poison & Sound, Echos of Eternity, Disciples of Deception, Radiance of Ruin) sourced from `MemoryStories.js`, `Museum.js`, `EvilHeroes.js`, and the `dlc/*/Story.js` scripts.
- **DLC: Radiance of Ruin character pack** implemented. Three resource-driven heroes: **Light** (Mask Integrity drain — Revelation reveal-sphere special, The Unveiling ult, Aurum Burn passive trail, Civilian Form lockout when gauge bottoms), **Thorn** (Blood Bond HP-cost attacks, Bleed-stacking ranged + melee, Crimson Garden pulsing Blood Rose, The Reckoning arena-wide vine erupt), **Dream** (Lucidity gauge, Drowsy melee debuff, Dreamscape pocket, The Long Sleep arena dream + 5s execute window, Lucid Step phase save once per wave). Three biomes (`light` Reliquary with healing Light Shafts, `thorn` Crimson Greenhouse with Bloom Patches + Mother Rose, `dream` Dreamspace with teleporting Dream Pockets) added to the standard DLC biome pool. New `dlc/radiance_of_ruin/` directory with hero JS, biome JS, `index.js` manifest registering hero stats, HERO_LOGIC, ALTAR_TREE entries (3 hero trees + 13 cross-hero convergences with `cv_ror_` prefix), 10 achievements, SFX manifest, and voice exclamation paths + subtitle texts.
- **DLC plan: Radiance of Ruin character pack** (`tasks/dlc-radiance-of-ruin.md`). Second and final character pack: Light (Radiance / Mask Integrity drain), Thorn (Blood Bond HP-cost), Dream (Lucidity gauge). Three biomes (Reliquary, Crimson Greenhouse, Dreamspace), 10 achievements, full SFX + voice line manifest, `index.js` outline.

### Changed
- **DLCManager registry, Wave.js DLC biome pool, AudioManager level-up hero list, and Museum hero color map** all updated to include `light`, `thorn`, `dream` so Radiance of Ruin appears in the DLC toggle UI, biomes roll in non-story standard runs, level-up SFX volumes match other heroes, and museum portraits render with the correct color.
- **Reliquary and Crimson Greenhouse biome backgrounds expanded with unique scenery**. Reliquary now renders a 300px marble floor-tile grid with diamond gold inlays, six vaulted cathedral arches along top + bottom edges (each with a stained-glass medallion), warm god-rays streaming down through each arch with subtle horizontal sway, and five colored stained-glass puddles on the floor (orange/red/blue/gold). Crimson Greenhouse renders a cracked greenhouse-glass roof along the top (diamond/triangular lead-frame lattice with branching crack lines), fourteen tangled vine silhouettes growing inward from all four edges (each ending with thorn-nub punctuation), four pulsing red glowing veins crisscrossing the floor, and five irregular blood-pool splotches with radial gradients.
- **Light hero character-select tile renders as metallic gold via gradient**. Previously the tile background was a flat `#f1c40f` yellow. Now uses `linear-gradient(135deg, #fff5b8 0%, #ffd633 25%, #f1c40f 55%, #b8860b 85%, #8b6914 100%)` with an inset gold glow + outer warm glow box-shadow. The hero name text uses a clipped gradient fill (`-webkit-text-fill-color: transparent` over the same gold-to-bronze gradient) for a stamped-metal look.
- **Unique projectile sprites for Light, Thorn, and Dream**. `Entities/Projectile.js` now branches on `type === 'light' | 'thorn' | 'dream'` before the generic fallback. **Light** fires a sun-mote: hot white core inside a gold halo, four-step comet trail using lighter composite, and a faint cross-shaped lens flare. **Thorn** fires a barbed crimson shard: elongated diamond body with two side barbs, dark red gradient fill, blood-red glow, and three soft red-mist puff trail. **Dream** fires a Whisper: curving violet quadratic arc with a brighter inner streak, leading star-spark, five fading dotted trail points along a sine wave, and twin slow-blinking micro-stars. Uses `cachedRadial` for stable performance.
- **Audio generation scripts updated for Radiance of Ruin**. `scripts/voice_ids.txt` now lists `light`/`thorn`/`dream` (empty IDs — fill in to enable voice generation). `scripts/generate_hero_exclamations.js` adds all 13 exclamation lines per hero, DLC output dirs under `dlc/radiance_of_ruin/audio/voices/{hero}/`, and per-hero voice-setting overrides (light: fractured-reverent, thorn: gaunt-hungry, dream: soft half-asleep). `scripts/generate_levelup_sfx.js` adds `level_up_{light,thorn,dream}` entries (2s each, elementally-themed prompts). `scripts/generate_boss_sfx.js` adds Light/Thorn/Dream ability SFX (attack/melee/dash/special + `unveiling_activate`/`civilian_collapse`/`reckoning_activate`/`lifebloom_heal`/`long_sleep_activate`/`lucid_step_phase`) wired to `--boss light|thorn|dream` filter.

### Changed
- **Main menu "Online 2-Player" button renamed to "Online"**. Tutorial copy updated to match.
- **Trap visual redesign**. All six trap types redrawn with modern canvas techniques: gradient fills, shadow glows, layered geometry, and smooth animations. Spikes: metallic gradient bodies with glowing red tips and a warning pulse before activation. Slow: purple hexagonal web zone with pulsing concentric rings. Conveyor: dark industrial steel plate with animated amber arrows and belt grooves. Turret: hexagonal armored shell, gradient barrel with muzzle charge glow, and glowing sensor eye. Laser: layered soft-glow beam with octagonal emitter and spinning dashed energy ring. Teleporter: multi-arm vortex arcs with radiant core and depth-layered portal rings.

### Changed
- **SLOW trap start wave + visual**. SLOW trap no longer spawns from wave 1; now appears from wave 3 onwards (waves 1-2 are trap-free unless `trapOverride` is set, e.g. via custom map). Visual simplified to a single soft purple radial gradient — hex web pattern and pulsing concentric rings removed.

### Fixed
- **Radiance of Ruin: BiomeZones in new biomes rendered with a red outlined box**. `Arena.BiomeZone.draw()` had no case for the RoR zone types `LIGHT_SHAFT`, `BLOOM_PATCH`, or `DREAM_POCKET`; the per-type branch fell through and the trailing `fillRect`/`strokeRect` inherited whatever fill/stroke was last set on the ctx — usually the red from a prior LAVA-style zone — producing a stamped red rectangle over the floor. Added an early-return case mirroring the existing `FRACTURE`/`SMOG_POCKET` precedent so the zones are drawn solely by the biome's `draw()` method.
- **Radiance of Ruin: default grey stone obstacles still appeared in Reliquary, Crimson Greenhouse, and Dreamspace at wave 15+**. The `ownsObstacles` flag suppressed the default layout pass, but the trap-generation loop (TURRET / LASER_BEAM) still pushed `new Obstacle(...)` with `biomeType = null`, drawing as the default grey stone. Added a complementary `noTraps` flag on the biome — Arena.generate skips trap generation entirely when set. All three RoR biomes set it (the plan calls Reliquary "sacral, not hostile", Crimson Greenhouse uses Bloom Patches as the hazard, Dreamspace uses Dream Pockets). Trap-spawned obstacles also now inherit `biomeType` so other biomes get themed turret/laser plinths instead of grey stone.
- **Radiance of Ruin: `TypeError: Cannot read properties of undefined (reading '_lightRevealed')` in `player.onKill` at `LightHero.js:249`**. Some base-game kill paths call `player.onKill()` without passing the enemy argument; the Light + Thorn wrappers read `enemy._lightRevealed` / `enemy._thornBleedStacks` unconditionally and threw. Added enemy null-guards on both wrappers. The resulting `masterFrame` crash was what produced the red error outlines on Reliquary biome elements (CrashReporter rendering frame errors visually).
- **Radiance of Ruin: Light Mask and Thorn HP drained on every shoot/melee *call*, not on actual fire**. `Player.shoot` / `Player.melee` return early when `rangeCooldown`/`meleeCooldown > 0`, but the hero wrappers deducted Integrity/HP before calling `origShoot/origMelee`. Held-fire input loops invoked the wrapper every frame, depleting the Mask in under a second and dropping Thorn to 1 HP within a couple seconds. Wrappers now run `origShoot/origMelee` first and only deduct resource + apply Bleed/SFX when the projectile count or melee cooldown actually changed. Same pattern applied to Dream's Lucidity-gain hook (previously farmed Lucidity from no-op shoot calls).
- **Default Arena obstacles appeared on top of biome-placed obstacles in Reliquary, Crimson Greenhouse, and Dreamspace**. `Arena.generate()` always ran the layout obstacle pass *after* `BIOME_LOGIC[biomeType].generate()`, so RoR biomes ended up with 14–29 obstacles when only the biome's 10–14 were intended. Biomes now expose `ownsObstacles = true` flag; Arena checks it and skips the default layout pass entirely. All three RoR biomes set the flag.
- **Radiance of Ruin biomes (Reliquary, Crimson Greenhouse, Dreamspace) overlaid the arena and hid the player/enemies on game start**. The biomes defined only `draw()` (no `drawBackground`), so `Arena.draw()` invoked it as the bg-layer fallback AND `game.js` called it again after entities — drawing the opaque bg fill *over* the player and enemies on the second pass. Split each biome into `drawBackground()` (opaque fill, player/biome radial glow, background stars) and `draw()` (translucent overlays only: light shafts, bloom patches, dream pockets, particles, mother rose, civilian vignette). Matches the base-game biome pattern in `Biomes.js`.
- **Battle theme played in online lobby and global lobby**. `AudioManager.update()` didn't recognize `ONLINE_LOBBY`, `GLOBAL_LOBBY`, or `GLOBAL_LOBBY_MENU` `uiState` values, so it fell through to the default battle music branch. Added `ONLINE_LOBBY` to `menuStates` (plays menu music) and routed `GLOBAL_LOBBY` / `GLOBAL_LOBBY_MENU` to the museum track.
- **Custom map biome overridden by hero type in workshop mode**. `advanceWave()` biome-rotation block ran for all modes except Versus, forcing `currentBiomeType` to the player's hero type on wave 1 and a random biome on subsequent waves. Workshop mode is now excluded so the map's configured biome persists.
- **Custom map layout reverted to random on every wave**. `advanceWave()` unconditionally called `arena.generate()`, overwriting the custom layout loaded by `startGame('WORKSHOP')`. Workshop mode now calls `arena.generateFromMap(pendingCustomMap)` instead, preserving obstacles, biome zones, and traps every wave.
- **Hero projectiles render as plain circles in multiplayer**. Server snapshot omitted the `type` field from per-projectile first-appearance data; clients initialized ghost projectiles with `type = ''`, falling through to the generic circle fallback. `type` is now included in the server snapshot and applied on the client alongside the other static fields.
- **DLC modules fail to load in packaged/dev Electron app**. `DLCManager.loadScript` built a root-relative URL (`/dlc/...`) which Electron's `file://` renderer resolved to `file:///dlc/...` (filesystem root) instead of the app directory. Fixed by resolving against `document.baseURI` via `new URL(rel, document.baseURI).href`, which works correctly under both `file://` and `http://`.
- **Map Workshop notice board in global lobby**. A clickable world-space board is placed at the top-centre of the gallery room. Walking within 120 px shows a `[E] / click · Map Workshop` HUD prompt; pressing E or clicking the board opens the workshop panel. The board glows blue when the player is nearby. The `[M]` global shortcut still works from anywhere.
- **Game stuck on "Loading Assets" after first server login**. The save-conflict modal (`cloud-conflict-modal`) had `z-index: 4000`, beneath the loading screen (`z-index: 9999`). Users who logged in once and restarted would hit a conflict check on startup, see the modal hidden behind the loading overlay, and be unable to proceed. Modal z-index raised to `10000`.
- **CrashReporter URL construction** — bare IP/hostname in `gameConfig.serverUrl` was treated as a relative path, causing crash reports to POST to `localhost:5173/<ip>/api/crash`. `_baseUrl()` now mirrors `CloudSaveManager`: adds `http://` scheme and `:3001` port when no scheme is present.
- **InputManager crash on synthetic key events** — `e.key` is undefined on some browser-generated `keydown`/`keyup` events, causing `TypeError: Cannot read properties of undefined (reading 'toLowerCase')`. Added `if (!e.key) return` guard to both listeners.
- **`SaveManager` not defined in CloudSaveManager** — `SaveManager` was never exposed on `window`, so `CloudSaveManager._applyCloudSave()` (and other methods) threw `ReferenceError: SaveManager is not defined` at startup. Added `window.SaveManager = SaveManager` at the bottom of `SaveManager.js`.

### Added
- **Three server-seeded example maps**. The server now seeds three official maps into `custom_maps` on startup under the system author **5 Freunde Team** (`user_id = 0`): **Volcanic Crucible** (fire, 8 waves, lava pools + Tank boss), **Cryo Gauntlet** (ice, 5 waves, slow/laser/turret traps with a teleporter pair + Hydra boss), and **Magnetic Coliseum** (metal, 10 waves, 2400×1800 arena with magnet zones and dark-energy strips + Rhino boss). They appear in the Community / Newest tabs like any uploaded map, can be played and liked by anyone, and cannot be deleted (the delete endpoint requires `row.user_id === req.user.id` and no real account has id 0). Seed lives in `server/exampleMaps.js`; idempotent re-runs skip existing rows by `(user_id, name)`.
- **Technical tab in admin dashboard**. New "Technical" tab shows crash reports from `GET /api/admin/crashes`: summary cards (total, last 24 h, unique stacks), sortable table with time/kind/message/version/hero/wave columns, and expandable detail rows with full stack trace, breadcrumbs, and run context.
- **Controller support for cloud save-conflict modal and custom maps panel**. Both UIs now register as proper `uiState` values (`CLOUD_CONFLICT`, `CUSTOM_MAPS`), wired into `UIManager.getFocusables()`. D-Pad navigates between buttons, A confirms, B cancels (picks local save / closes panel). Gamepad hint added to both UIs.
- **Map editor ships as a separate executable**. The installed game directory now contains `MapEditor.bat` (Windows) or `MapEditor.sh` (macOS/Linux), both of which launch the main binary with the `--map-editor` flag. The editor window opens at 1400×900, non-fullscreen. Electron main (`index.js`) detects `--map-editor` via `process.argv` and calls `createMapEditorWindow()` instead of `createWindow()`.
- **Maps saved to user data directory**. Electron sets `APP_MAPS_PATH` to `<userData>/maps/` on startup. The map editor writes maps there via `fs.writeFileSync` when running in Electron; in a browser it falls back to JSON download. New `Managers/MapManager.js` provides `listMaps()`, `loadMap()`, `saveMap()`, `deleteMap()`, and a `defaultWaveConfig` getter — used by both the editor and the in-game Custom Maps panel.
- **Map editor "My Maps" sidebar**. When running in Electron, the map editor shows a left-panel list of all saved maps sorted by modification time. Each row has a Load button and a delete (🗑) button. The list refreshes after every save or delete.
- **Wave configuration in map editor**. New right-panel "Wave Config" section lets authors set: wave count (0 = unlimited/survival), enemies per wave, base spawn rate and per-wave decay, enemy pool (11 checkboxes: BASIC SHOOTER BRUTE SPEEDSTER SWARM SUMMONER GHOST SNIPER BOMBER TOXIC SHIELDER), and boss type (Tank / Speedster / Summoner / Nova / Rhino / Hydra / Random / No Boss). Map format bumped to v2; v1 maps load with defaults.
- **"Custom Maps" button in main menu**. Opens `CustomMapsPanel` — a new overlay with three tabs: "💾 My Maps" (reads from `MapManager`), "⭐ Community" (popular server maps), and "🕐 Newest" (newest server maps). Local maps have Play, Upload (requires login), and Delete actions. Server maps have Play, Like, and Leaderboard actions. New `UI/CustomMapsPanel.js`; accessible via `window.openCustomMaps()`.
- **Workshop waveConfig applied in-game**. `game.js`: `isWorkshopMode` promoted to module-level; `startGame('WORKSHOP')` sets `window._customEnemiesPerWave` from the map's `waveConfig`. `advanceWave()` checks `waveConfig.waveCount` and calls `gameOver(true)` when the configured wave count is reached. Spawn loop applies `spawnRateBase` / `spawnRateDecayPerWave` and picks enemy types from `enemyPool` when set. Boss spawn respects `bossType` (named type, `random`, or `none`). `Wave.js` `enemiesNeededForWave()` reads `window._customEnemiesPerWave` when set.

- **Community Map Workshop**. Players can publish custom maps from the map editor to the server via the new "Upload" button (reads auth token from game config in `localStorage`). In the global lobby, press **[M]** to open the Map Workshop panel — a browsable overlay with "Popular", "Newest", and "My Maps" tabs. Each map card shows the biome, author, play count, and a like toggle. Clicking **Play** fetches the map data, loads it into a new solo run via `Arena.generateFromMap()`, and submits the resulting score to a per-map leaderboard. New server infrastructure: `custom_maps`, `custom_map_scores`, and `custom_map_likes` SQLite tables; REST endpoints `POST/GET /api/maps`, `GET/DELETE /api/maps/:id`, `POST /api/maps/:id/like`, `GET/POST /api/maps/:id/leaderboard` and `/api/maps/:id/score`. Limits: 64 KB max map payload, 20 maps per user, 500 scores retained per map.
- **Map editor** (`#102`). Standalone `map-editor.html` tool with a canvas-based drag-and-drop layout builder. Supports placing obstacles (rect, any biome theme), biome zones (LAVA / ICE / MUD / WATER / MAGNET / DARK_ENERGY), and traps (SLOW / CONVEYOR / SPIKE / TURRET / LASER_BEAM / TELEPORTER with auto-pair linking). Features: per-element property panel, grid snap, layer visibility toggles, undo/redo (Ctrl+Z/Y), keyboard shortcuts (S/O/Z/T/G/Del), rubber-band drawing, right-click delete, spawn-safe-zone indicator, and Export / Copy JSON. Exported JSON is consumed by the new `Arena.generateFromMap(mapData)` method, which reconstructs obstacles, biome zones, traps, and teleporter pairs from the map data.

### Added
- **Reconnect grace window** (`#83`). When a player disconnects mid-game the server no longer immediately fires `PARTNER_DISCONNECTED`. Instead, a `PARTNER_RECONNECTING { timeoutSec: 30 }` message is sent to the remaining player, pausing the game with a 30-second countdown overlay. If the disconnected player reconnects within the window the game resumes normally; if not, `PARTNER_DISCONNECTED` fires and the run is aborted. Hard lobby cleanup still runs after 90 s to preserve the leaderboard submission window.
- **WebRTC P2P voice chat** (`#84`). New `Managers/VoiceChatManager.js` manages mic capture, `RTCPeerConnection` lifecycle, remote audio playback, and mute toggling. Signaling (`WEBRTC_OFFER` / `WEBRTC_ANSWER` / `WEBRTC_ICE`) is relayed through the existing WebSocket lobby. The host creates the offer automatically when `voiceChatManager.start('host')` is called; the guest responds via the received offer. STUN-only ICE (Google public server). `VOICE_MUTE` WS message relays mute state to partner. New `NetworkManager` methods: `sendFriendRequest`, `respondFriendRequest`, `fetchFriends`, `removeFriend`.
- **Friends list** (`#85`). New `friendships` SQLite table (`requester_id`, `addressee_id`, `status`). REST API: `GET /api/friends`, `POST /api/friends/request`, `POST /api/friends/respond`, `DELETE /api/friends/:userId`. Mutual-request auto-accept: sending a request to someone who already sent you one immediately accepts both sides.
- **Server-side world events** (`#96`). New `world_events` SQLite table stores timed events (type, label, multiplier, starts_at, ends_at). Public `GET /api/events` returns currently active events. Admin CRUD: `GET/POST /api/admin/events`, `DELETE /api/admin/events/:id`. New admin dashboard **Events** tab with a create form and event list. New `Managers/WorldEventsManager.js` polls `/api/events` every 5 minutes and exposes `getXpMultiplier()` / `getBanner()`. XP kill reward in `game.js` multiplied by active `xp_boost` event multiplier.
- **Hero balance dashboard** (`#161`). New admin endpoint `GET /api/admin/hero-balance` returns per-hero run count, pick rate, win rate, average wave, and average score. New admin dashboard **Balance** tab displays the data as a sortable table with colour-coded win rate.

### Changed
- **Cloud save conflict modal enhanced** (`#86`). The conflict UI now decodes both blobs client-side before showing the dialog and displays a one-line summary for each side: wave reached, heroes unlocked, total kills, and meta upgrade points. Users can make an informed choice rather than relying solely on timestamps.

### Fixed
- **Speedrun unlock/PB now follows story-completion group logic**. `isSpeedrunUnlocked` checks the full sibling group (base heroes share, gravity↔void share, spirit↔chance share) so beating story with any grouped hero unlocks speedrun for all. Speedrun PB update mirrors the same groups — base-hero speedrun win now propagates the best time to all five base heroes.
- **TimeHero crash on game start** (`#103`). `saveData` could be `undefined` when `_w` had no `saveData` property, causing `TypeError: Cannot read properties of undefined (reading 'altar')` at `TimeHero.init`. Fixed by defaulting `saveData` to `{}` via optional chaining.
- **Lag compensation — hit registration** (`#91`). Player projectile vs. enemy collision radius on the server expanded by 18 px (`ROLLBACK_PX`). At typical enemy speeds (~8 px/tick) this covers ~2 server ticks (≈ 66 ms) of positional error introduced by network latency, eliminating most "I hit them but no damage" cases without affecting game balance meaningfully.

### Added
- **Boss intro cinematic** (`#46`). When any `BOSS_FIGHT` wave triggers, a 2.5-second canvas cinematic plays before gameplay resumes. Camera eases from the player's position to the boss location while zooming from 1.0× to 1.45×, holds on the boss, then zooms back out. A "BOSS FIGHT" label and the boss name fade in at the 0.22 mark with red glow and white text, flanked by decorative rule lines. An impact flash fires at the start; a vignette deepens throughout; the final 12% fades to black. Gameplay (enemy AI, player input, physics) is fully frozen during the cinematic — the boss is visible but inert. Works for all boss types including DLC heroes. The old `event-text` "BOSS: NAME" overlay is replaced by the cinematic banner.

### Changed
- **Save editor extended with all recent schema fields**. Heroes tab now shows all 14 DLC heroes (earth, lightning, gravity, void, spirit, chance, sound, poison, air, time, love, psycho, mirror, smoke) alongside the 6 base heroes, each with `highScore`, `maxWinPrestige`, `storyCompleted` checkbox, and `bestSpeedrunSec` (speedrun PB) fields. "Max All Heroes" and "Set All Prestige" bulk actions cover DLC heroes too. Currency tab renamed to "Currency & Stats" and extended with all 12 global stat fields (`totalKills`, `maxWave`, `totalBosses`, `totalDamage`, `maxCombo`, `totalGames`, `totalDeaths`, `totalVoidGoldSpent`, `daily_wins`, `weekly_wins`, `onlineGamesPlayed`, `onlineMaxWave`). New "Story & Misc" tab exposes story mode enabled toggle, unlocked chapter list, tutorial seen/completed flags, and daily/weekly last-completed timestamps. `ensureDefaults` and `collectAllValues` updated to round-trip all new fields.

### Added
- **Hero walk and fire animations** (`#37`). `drawHeroSprite` gains an optional `anim` param (`lean`, `fireRaise`). Walk: sprite shears laterally (local-space `ctx.transform` shear, coefficient 0.10) based on `_lean` — computed each frame as the projection of movement input onto the axis perpendicular to aim, smoothed with a 0.15 lerp toward target and ×0.85 decay on idle. Effect: hero visibly leans into strafing/circular movement. Fire raise: nozzle translates −28 % radius on perpendicular axis for 8 frames after each shot, eased via `sin(timer/8 * π)`. DLC heroes using `customDraw` or no projectile fire are unaffected. Museum and DLC `drawHeroSprite` callsites work with zero-anim defaults.

### Fixed
- **Museum player avatar color for Mirror, Smoke, Psycho heroes** — they fell through to default yellow. Added color entries: Marine Blue, Slate Gray, Teal.

### Added
- **Story Speedrun mode**. Unlocks per hero once that hero's story is beaten (base heroes fire/water/ice/plant/metal share one unlock; DLC heroes unlock individually). A gold "⏱ Story Speedrun" button appears in the main menu below Story Mode when the selected hero qualifies. Solo only — hidden in co-op and online modes. During a speedrun run: story chapter modals are skipped (no screen, no audio) but all gameplay effects are preserved (boss spawns, companion joins, forced enemy types, hero swaps, objective waves). A live `mm:ss.t` HUD timer runs from run start; split toasts pop every 10 cleared waves (arc boundaries) and on the final victory wave showing the delta vs previous split. Best final time is saved per hero (`saveData[hero].bestSpeedrunSec`) and shown on the hero card in the main menu. Times are submitted to a new `speedrun_scores` server table via `POST /api/speedrun` (rate-limited, plausibility-checked at 4 s/wave minimum). The global leaderboard in the Museum gains a second **⏱ SPEEDRUN** tab (switch with Q / E); the tab shows all-hero times sorted fastest-first with the signed-in player's row highlighted in gold. Implementation: `isSpeedrunMode` flag + `startSpeedrunGame()` in `game.js`; `_finishStoryEvent()` extracted from `closeStory()` as shared post-modal mechanic runner; `openStory()` fast-path skips modal for speedrun; `_updateSpeedrunHud()` hooked into RAF tick; `_recordSpeedrunSplit()` pushes to `currentRunStats.splits[]` (new field in `RunState.js`); `storyCompleted` + `bestSpeedrunSec` fields in `defaultSaveData`; server table + endpoints in `server/server.js` + `server/anticheat.js`.

### Changed
- **Mindscape biome — center motif differentiated from Hall of Mirrors**. The Mindscape (Psycho biome) previously rendered a 24-spoke mandala fracture pattern + concentric pulsing rings emanating from arena center — visually too close to the Hall of Mirrors biome's "rotating prismatic rays + bright core" central motif (both = radial-from-center light pattern). Replaced the two center elements with non-radial, mind-themed alternatives that read as distinct at a glance. **EEG brainwave traces** ([draw section 2](dlc/disciples_of_deception/MindscapeBiome.js)) — 5 horizontal jagged sine + noise waveforms scrolling left across the arena, stacked at varied y-positions (12% → 88% of arena height), each with independent amp / freq / hue / scroll speed. Each trace samples its waveform every 16 px (cheap, still smooth) and renders a faint chromatic-aberration echo line shifted down + back 0.4 rad. Random "thought spike" jolts boost amplitude for ~30 frames; jolt chance + scroll speed roughly double while the player is in Hysteria so the screen visibly destabilises during the mode. **Drifting mind glyphs** ([draw section 3](dlc/disciples_of_deception/MindscapeBiome.js)) — `?`, `!`, `Ψ`, `∞`, `◊`, `∴`, `※`, `⊗` symbols spawn at the bottom edge and drift upward with light horizontal jitter, slow rotation, fade-in / fade-out alpha envelope, and a faint white inner highlight. Spawn rate is gated (max 40 active) and tripled during Hysteria. State arrays (`this.eegLines`, `this.mindGlyphs`) replace the prior `this.fracturePts`; constructor + `generate` + `update` adjusted accordingly. All other Mindscape elements (vignette, rorschach inkblots, screen-tear bands, vertical chromatic stripes, particles, obstacles) intentionally unchanged.

### Added
- **Thematic projectile visuals for Smoke / Mirror / Psycho heroes**. The three Disciples of Deception heroes were previously falling through to the generic fallback branch in `Entities/Projectile.js#draw` (plain coloured disc using `this.color`). Each now has a unique rendering path keyed off the hero `type`. **Smoke** — dark wispy puff: pulsing slate-gray inner core (`cachedRadial`-keyed `proj-smoke-core:${r}`) wrapped in a smoky radial aura (`proj-smoke-aura:${r}`, slate → black falloff), two trailing wisp circles offset behind direction of travel with a small `sin/cos` jitter, plus a subtle rotating swirl arc. Rotates to velocity angle so the trail always lags correctly. **Mirror** — angular glass shard: elongated 4-point diamond (tip 2.2r forward, back -1.4r, sides ±0.8r) filled with a glassy white→marine-blue `cachedRadial` (`proj-mirror-body:${r}`), dark `#0e2e44` edge stroke, white sheen stripe across the long axis, and a 1-pixel `hsl((t*0.1)%360, 80%, 70%)` refraction tick that cycles through the full hue wheel (prismatic shimmer). Rotates to angle. Faint cyan `shadowBlur` for the glass glow. **Psycho** — erratic teal-magenta psychic bolt: pulsing teal core (`proj-psycho-core:${r}`, white→teal→dark-teal gradient) with `1abc9c` shadow, surrounded by a teal-to-purple aura (`proj-psycho-aura:${r}`), 3 jagged spark arcs that orbit at `t * 0.01` with `sin(t*0.05+i)` chaos noise on the mid-points, and a magenta counter-rotating dashed ring (`setLineDash([3,4])`, `rotate(-t * 0.006)`). All three radii / colour-bucket keys use `cachedRadial` so per-frame gradient cost stays at lookup-tier (consistent with P4 sweep), and they all sit before the fallback branch so enemy projectiles + types without a dedicated draw still hit the generic path. No gameplay change — purely visual.

### Changed
- **Performance pass P10 — phase-timing instrumentation, gating decision for #24 + #30**. P10 was billed in the plan as *speculative* — `#24` (Web Worker AI via `SharedArrayBuffer`) and `#30` (WebGL/Pixi backend toggle) only worth the multi-day / multi-month rewrite if profiling at the end of P1–P9 shows main-thread CPU still bound. P1–P9 already shipped substantive cuts (spatial hash inversion, pool expansion, gradient cache, offscreen obstacles, DLC lazy primitives, particle sprite cache, Web Audio SFX, snapshot deltas) so without numbers a rewrite call is guessing. This pass ships the measurement infrastructure to gate that decision. **`_phaseTimes`** ring buffers (120-frame window) + `_recordPhase(name, ms)` + `_phaseP50/P99(name)` helpers. **`masterFrame`** wrapped in `try / finally` so the `frameWork` phase covers every return path (museum skip, game-over early return) and the previously-incorrect `_updateDebugOverlay(deltaTime)` (passed the rAF delta, not the work cost) gets a real work-time measurement. **Enemy phase** wrapped around the `for (let eIndex = enemies.length - 1; ...)` block — this is the #24 candidate (200+ enemies × steering / target / collision per tick). F1 overlay gains two lines: `Frame work: p50 X.X / p99 X.X` and `Enemies ph: p50 X.X / p99 X.X`. **Gating thresholds** (per plan): if `enemies` p99 ≥ 4ms at wave 30+ → #24 worker pass justified; if `frameWork` p99 ≥ 8ms after subtracting `enemies` → #30 WebGL pass justified. Otherwise stop here. Overhead when overlay is off: two `performance.now()` calls + two array writes per frame (~µs). Build green, 58/58 Vitest + 80/80 parity.

- **Performance pass P9 — snapshot position deltas** (#32). Server snapshots send entity positions as `dx, dy` deltas from the last sent value once an entity is known, instead of full `x, y` absolutes every tick. Typical mid-game deltas are -10..+10 px → 2–3 char JSON tokens vs 4–5 char absolutes (`1234.5` → `5`). On wave 30+ with ~80 enemies + ~150 projectiles, that's ~30–40 chars saved per entity × 230 entities = ~7–9 KB saved per snapshot before `permessage-deflate` (#158) — deflate is more efficient on smaller inputs too, so the wire savings compound. **Server** (`server/simulation/GameSession.js`): `_lastSentEnemyXY` + `_lastSentProjXY` Maps track the rounded value sent to clients per entity id; `_snapshotsSinceKeyframe` counter + `_KEYFRAME_INTERVAL = 30` (1s at 30Hz) force a full `x, y` keyframe to prevent rounding drift over time. First sight of any entity always emits absolute. **Client** (`game.js _onlineApplySnapshot`): position decoder branches on `ed.x !== undefined` (absolute → use as-is + store `_lastSnapX/Y`) vs `ed.dx !== undefined` (delta → add to `_lastSnapX/Y`, store sum). Same path for projectiles. Player snapshots (`p1`/`p2`) intentionally stay absolute — only 2 per snapshot, marginal saving + simpler. parityTest #6 already verifies static-field delta encoding (maxHp/color first-sight-only); position delta is invisible to it since the tests check absolute `snap.p2.x` values which aren't delta-encoded. Build green, 58/58 Vitest + 80/80 parity.

- **Performance pass P8 — Web Audio fast path for hot SFX** (#34). `play(key)` was always cloning the underlying `HTMLAudioElement` (`track.cloneNode()` + `.play()`) — ~0.5–1ms per shot on Chrome (clone + decode-cache hit + buffer setup), which at peak combat (~30–50 SFX/sec) costs ~20ms/sec. New path: shared `AudioContext` (the same instance already powering the #167 pause low-pass filter) + `_sfxBuffers` Map<key, AudioBuffer> populated at boot via `fetch().then(arrayBuffer).then(decodeAudioData)`. Boot prewarm covers the 12 hot SFX from the #15 tier (`damage`, `death`, `dash`, `melee_all`, `enemy_damage`, `level_up`, `attack_fire/water/ice/plant/metal/black`); other SFX continue on the cloneNode path. `play()` now tries `_playBuffer(key, track.volume)` first — creates `AudioBufferSourceNode` + per-shot `GainNode` + `start(0)` (~0.01ms per shot, 50× cheaper than cloneNode); falls back to cloneNode when the buffer isn't decoded yet (first wave or two) or when there's no `AudioContext` (server / very old browser). Prewarm is fire-and-forget on a `setTimeout(_, 0)` so constructor finishes before any decode landings hit manager state; failed decodes are logged + retryable. AudioContext stays suspended until first user gesture (browser autoplay policy); `_playBuffer` calls `ctx.resume()` lazily on first shot. The existing per-track `volume` (with category multiplier baked in via `updateSettings`) feeds the `GainNode` so behaviour matches the cloneNode path exactly. Build green, 58/58 Vitest + 80/80 parity.

- **Performance pass P7 — particle sprite cache** (#25, #26). `Particle.draw()` was the hottest per-frame draw call (`ctx.save` / `globalAlpha` / `fillStyle` / `beginPath` / `arc` / `fill` / `ctx.restore` — 7 ops × up to 300 particles × 60 fps = ~126 k canvas ops/sec). Replaced with a per-color sprite cache: `Particle._spriteCache` lazily builds an 8×8 offscreen canvas with a 3-radius disc rendered in the requested color on first use; subsequent draws of that color hit `ctx.drawImage(sprite, x-4, y-4)` after one `globalAlpha` set — 2 ops vs 7. Cache bounded to 64 entries with FIFO eviction so a runaway DLC adding random `rgba(…)` colors can't grow it unbounded; typical real-world usage stays around 20–30 unique colors (hero theme palette + biome accents). Server-side / no-document path falls back to the original immediate-mode arc draw. masterLoop resets `ctx.globalAlpha = 1` once after the particle sweep rather than wrapping each draw in `save` / `restore`. Explicit alpha-bucket batching (#26 follow-up) deferred — sprite cache captures the dominant win (eliminates `fillStyle` thrash + `arc` path build cost); enemy-glyph + pickup-icon atlas (#25 follow-up) deferred since per-frame enemy draws are already complex shape-by-shape rendering that won't trivially atlas. Build green, 58/58 Vitest + 80/80 parity.

- **Performance pass P6 — DLC on-demand infrastructure** (#29 Phase 3). Three additive primitives in `dlc/DLCManager.js` set up lazy load without changing default behaviour: **(1)** `availableDLCs[id].heroes` arrays added for all 8 DLCs (`['earth']`, `['lightning']`, `['gravity', 'void']`, `['air', 'wind']`, `['spirit', 'chance']`, `['sound', 'poison']`, `['time', 'love']`, `['psycho', 'mirror', 'smoke']`) so consumers can reverse-map hero → owning DLC without loading the module. **(2)** New `getHeroOwnerDLC(heroType)` builds + caches a `_heroOwnerIndex` reverse map; returns `null` for base heroes (fire/water/ice/plant/metal/black). **(3)** New `ensureDLCLoaded(id)` — idempotent memoized load via `_loadPromises` Map. Concurrent callers share the same in-flight promise; resolved promises stay cached so subsequent calls are O(1); failed loads clear themselves so a retry can run. `init()` now routes its parallel eager load through `ensureDLCLoaded` so the eager + on-demand paths use the same memoization. **Wiring:** [`UI/MainMenu.js`](UI/MainMenu.js) hero-card `onmouseenter` prefetches the owning DLC in the background — by the time the player clicks Start, the load is already resolved (or in-flight, sharing the promise). Hovering a base hero is a no-op (null owner short-circuits). **Deferred:** the full Phase 3 lazy-load (skip eager `init()`, render hero select from manifest before DLC loads, show loading spinner on hero click) requires hardening Achievements / Collection / Story menus against partially-registered DLC content; primitives shipped this pass are the foundation for that follow-up. Build green, 58/58 Vitest + 80/80 parity.

- **Performance pass P5 — offscreen static obstacle layer** (#21). Obstacles never move; their per-frame draw is ~25 canvas ops each (stone gradient + block lines + cracks + pits + 4 bevel polygons + outer stroke), and a typical arena has 8–15 obstacles → 200–375 ops/frame just for static geometry. Now baked once into an offscreen `<canvas>` at `Arena.generate()` and blitted per frame via a single `drawImage`. Implementation: `Arena._staticObstacleCanvas` + `_staticObstacleFingerprint` + `_rebuildStaticObstacleLayer()` + `_staticObstacleLayerDirty()`. Fingerprint = `${obstacles.length}:${biomeType}:${width}x${height}` so DLC mutations (PoisonBiome flask spawn/consume, turret/laser obstacle pushes) auto-rebake on the next `draw()` call without explicit invalidation API. Server-side guard: `typeof document === 'undefined'` short-circuits to null canvas → linear fallback (server simulation never invokes `draw()`, but the guard keeps the code import-safe). Biome zones and DLC `drawBackground` hooks intentionally NOT baked — they carry `Date.now()`-driven alpha pulses (LAVA, STORM, DARK_ENERGY) and animated decals (Mindscape, Smog Quarter fogs) that would freeze in the bake. The bake covers obstacles only; everything else continues to draw live. Build green, 58/58 Vitest + 80/80 parity.

- **What's New modal renders Markdown** (#165). The in-game changelog dialog now parses the CHANGELOG slice through a tiny inline Markdown renderer instead of dumping raw text via `textContent`. Supported: `##` / `###` headings, `- ` bullet lists, `**bold**`, `*italic*`, `` `code` ``, `[label](url)` links. Source HTML is escaped before any transform so contributor-authored content can't inject markup. New `_renderMarkdownLite(md)` helper in `game.js`; `#whatsnew-body` swaps from `textContent` to `innerHTML` and drops `white-space:pre-wrap`. New CSS rules (`.wn-h2`, `.wn-h3`, `.wn-ul`, `.wn-li`, `.wn-p`, `.wn-strong`, `.wn-em`, `.wn-code`, `.wn-link`) style the rendered tree, with high-contrast overrides matching the rest of the modal sweep.

- **Performance pass P4 — gradient cache sweep** (#22). 15 additional `createRadialGradient` sites converted to `Utils.cachedRadial` across base entities + 7 DLC files: `Entities/HolyMask.js` (golden mask overlay), `Entities/GoldDrop.js` (outer glow — keyed per tier × 3), `Player.js` (transform halo — keyed by hero color × radius), `dlc/waker_of_winds/WindBosses.js` (turbine core, vortex body, eye glow — keyed by phase + color tints), `dlc/waker_of_winds/WindEnemies.js` (core — burst flag), `dlc/champions_of_chaos/ChaosEnemies.js` (void body + chaos eye), `dlc/champions_of_chaos/index.js` (boss void body + boss eye — phase-keyed), `dlc/champions_of_chaos/GravityHero.js` (gravity well core), `dlc/disciples_of_deception/SmokeHero.js` (smog aura), `dlc/echos_of_eternity/TimeHero.js` (time-shadow body), `dlc/rise_of_the_rock/EarthHero.js` (obsidian transform aura). Cache key convention: `{class}:{purpose}[:variant][:rBucket]`. Total converted now ~18 of ~169 sites; conversion is incremental and bounded by which sites have static stops + (0,0)-centered geometry. Sites skipped this pass: those with per-frame dynamic alpha (`pulse`, `fade`, `life/maxLife` ramps — caching freezes the animation), those positioned at non-origin centers where the offset is the lighting cue (e.g. Boss.js `(-r*0.28, -r*0.28, ...)` upper-left highlight on sphere bodies), and the entire `Biomes.js` set where seeded scatter positions don't repeat per frame. Long-tail deferred for follow-up profiling-driven passes; helper API is the same `cachedRadial(ctx, key, r0, r1, stops)` already in use by `Entities/Projectile.js` (Time + Love hero arrows). Build green, 58/58 Vitest + 80/80 parity.

- **Performance pass P3 — pool expansion** (#20). Three new entity pools land alongside the existing `Particle` / `FloatingText` ones, killing per-spawn GC churn on the hottest paths. **`Projectile`** (POOL_MAX = 256) — 85 `new Projectile(` call sites converted to `Projectile.acquire(` across base (`Player.js`, `Boss.js`, `Enemy.js`, `Arena.js`, `EvilHeroes.js`) and 8 DLCs (Echos of Eternity, Symphony of Sickness, Rise of the Rock, Waker of Winds, Faith of Fortune, Disciples of Deception, Champions of Chaos, Tournament of Thunder). **Strict reset** in `acquire`: every constructor field is reapplied (incl. crit radius re-derivation so `radius *= 1.5` doesn't compound on reuse), plus every known caller-attached optional field is cleared back to defaults — `shooterType`, `onHit`, `isWildfire`, `isCryo`, `_ghost`, `_id`, `_loveHeartArrow`, `_loveHeartBolt`, `outlineColor`, `ownerIsPlayer`, `dead`. **`MeleeSwipe`** (POOL_MAX = 64) — 1 spawn site (`Player.js`); `hitList` cleared in-place to keep hidden class stable. **`GoldDrop`** (POOL_MAX = 64) — 5 spawn sites (3× game.js, 3× `ChanceHero.js`); value + tier re-rolled per acquire so reused drops don't carry stale loot stats. **Release-before-splice** wired at every consumption site: 9 sites in `game.js` (life expiry, PvP dead, 2P versus dead, arena collision, out-of-bounds, lifted enemy-projectile pre-pass dodge/hit/p2-hit, inner enemy sweep ghost/onHit/immune/explosive/pierce-end), 3 DLC sites (Time Boss absorb + spirit-orb damage, MirrorHero shatter). New `tests/entityPools.test.js` adds 10 Vitest assertions covering: constructor-equivalence on cold acquire, optional-field clearing on reuse, crit-radius non-compound, ice-vs-other pierce defaults, POOL_MAX bound, MeleeSwipe `hitList` reset, GoldDrop value re-roll. Build green, 58/58 Vitest (10 new) + 80/80 parity.

- **Performance pass P2 — spatial hash Phase 2** (#19). Full O(N×M) → O(N×k) inversion of the per-enemy projectile collision sweep. Three changes: **(a) enemy-projectile → player(s) lifted out of the per-enemy inner loop** into a pre-pass before the enemies array iteration. The branch was independent of any single enemy (only player position matters), but was being re-checked N times redundantly inside the old `projectiles.forEach` inside `enemies.forEach` block; now it runs exactly once per frame. Preserves all dodge/immune/co-op/transform/p2-down logic. **(b) New `_projectileSpatialHash`** (128 px cells) rebuilt each frame alongside `_enemySpatialHash`. The per-enemy inner sweep now iterates `queryProjectilesNear(enemy.x, enemy.y, enemy.radius + 60)` candidates (~3–10 per query at wave 30+) instead of the full projectiles array. `pIndex` for splice is resolved lazily via `projectiles.indexOf(proj)` only when a hit consumes the projectile, keeping the cost proportional to hits (rare) rather than to iterations (every frame). **(c) Small-N bypass** — `_SPATIAL_HASH_MIN = 30`. Rebuild + map overhead exceeds the savings when entity counts are low, so per-frame `_enemyHashActive` / `_projectileHashActive` flags flip off and the new `queryEnemiesNear` / `queryProjectilesNear` helpers transparently fall back to the full array. All three pre-existing AOE-radius sites (explosive projectile arena impact, explosive projectile vs nearby enemies, SWARM-4 chain explosion) routed through the same helpers so bypass works uniformly. F1 debug overlay gains per-hash `{buckets, maxBucket}` lines (replaces the stale `window._spatialHash` reference left over from #148). Build green, 48/48 Vitest + 80/80 parity.

- **Performance pass P1 — cheap wins** (#23, #27, #28, #33). **#27 off-camera culling**: particles + floating texts now compute `arena.camera` AABB margins (`64` near, `128` far) once per masterLoop sweep. Anything past the far margin is released to its pool + spliced immediately (saves both update and draw); anything in the on-screen band gets `update() + draw()` as before; anything between the two bands still updates (so it re-enters cleanly) but skips draw. Cuts per-frame `arc/fill` GPU calls at high entity counts where bosses scatter particles across the arena and only a fraction land in-view. **#28 30 Hz throttle for biome-zone collision**: `enemy._zoneRefreshAt` caches the per-enemy `biomeSpeedMod` for 4 frames. At wave 30+ (200 enemies × ~10 zones = 2k AABB tests/frame) this collapses to ~500/frame. Refresh also fires every `frame % 60 === 0` so the LAVA DoT tick window never gets skipped — `MUD`/`ICE`/`WATER` speed-mod staleness is invisible (zones are 200–800 px, enemies move 3–5 px/frame). **#23 frame-budget HUD**: confirmed F1 overlay already prints p50 + p99 from the 120-frame ring buffer (`_frameTimes`) added by #148 — no new work needed; backlog entry promoted to closed. **#33 WS compression**: closed in favour of #158 — `permessage-deflate` shipped on the WebSocket already covers what bitpacked-delta compression would have given. Net effect: zero gameplay-visible change, measurable frame-time tail compression at wave 30+. Build green, 48/48 Vitest + 80/80 parity.

- **#4 GameContext — session 5 of 5 (final multi-writer flip)**. `gameConfig` + `saveData` now owned by GameContext via `_ownedDescriptor` (sessions 1-4 added the helper + flipped 3 single-writer props). Writer audit: `gameConfig` has a single init writer in `Config.js` + 5 mutation sites (all `Object.assign(gameConfig, …)` or deep-prop `gameConfig.X = …`, so object identity is preserved); `saveData` has whole-object reassignment from 5 sites — init declaration + `loadGame` result + `loadGame` fallback + `importSave` callback in `game.js`, and the `CloudSaveManager.syncOnStartup` merge writer. All swapped to `window.gameContext.X = …` via the setter, which re-defines the legacy `window.X` as a reverse-alias getter pointing at the backing field. UI / DLC bare-reads (`window.gameConfig.X`, `window.saveData.X`) keep working unchanged. Entity arrays (`enemies` / `projectiles` / `wave` / `arena` / `_world`) intentionally stay delegated — flip is coupled to the #11 RunState carve which is a separate multi-session arc. Build green, 48/48 Vitest + 80/80 parity.

- **#4 GameContext — session 4 of 5 (first ownership flip)**. Introduced `_ownedDescriptor(backingKey, winAlias)` helper: GameContext owns the value on an internal backing field (`_canvasBacking`, `_ctxBacking`, `_defaultSaveDataBacking`); on assignment the setter also re-defines `window[winAlias]` as an `Object.defineProperty` getter pointing at the same backing field. DLC bare-reads (`window.canvas` / `window.ctx` / `window._defaultSaveData`) still work — the legacy global is now a reverse alias. Flipped the 3 single-writer properties: `canvas`, `ctx`, `defaultSaveData`. `game.js` writers swapped to `window.gameContext.canvas = canvas` / `.ctx = ctx` / `.defaultSaveData = defaultSaveData`. Multi-writer properties (`gameConfig` reassigned by `loadConfig`, `saveData` written by ~12 sites, entity arrays `enemies`/`projectiles`/`wave`/`arena` mutated every frame) deferred to session 5 once their writer surface is audited. Build green, 48/48 Vitest + 80/80 parity.

- **#4 GameContext — session 3 of 5 (registries API + base UI sweep)**. `GameContext.registries` gains convenience methods that collapse the long `if (window.HERO_LOGIC && window.HERO_LOGIC[t] && window.HERO_LOGIC[t].fn)` guard chain: `getBiome(id)` / `getHero(type)` / `getEnemy(type)` / `getDLC(key)` return entry-or-null; `callHero(type, methodName, ...args)` invokes the method and returns its result or `undefined`; `listDLCs()` returns enabled keys. Base UI readers migrated to the new API: `UI/LevelUp.js` (4 sites — `getCustomLevelUpOptions` / `modifyUpgradeOption` / `applyUpgrade` / upgrade-pool lookup), `UI/Shop.js` (1 — `permUpgrades`), `UI/SkillTree.js` (2 — `getSkillTreeWeights` / `getSkillNodeDetails`), `UI/MainMenu.js` (1 — `DLC_REGISTRY` iteration). 29 DLC files with 53 raw `HERO_LOGIC[…]` / `BIOME_LOGIC[…]` reads inside `dlc/` deferred — DLCs load server-side via `require()` through `server/simulation/loader.js`, so a DLC sweep must validate the stub layer. Migration is incremental from here.

- **#4 GameContext — session 2 of 5 (UI `saveData` reader sweep)**. Migrated all `window.saveData` reads in `UI/Achievements.js` (1 site), `UI/Collection.js` (6 sites), `UI/HeroDetails.js` (6 sites), `UI/Statistics.js` (11 sites), `UI/SkillTree.js` (5 sites) to `window.gameContext.saveData`. Underlying object reference unchanged (getter delegates to `window.saveData`) — pure forward-compat for session 4 ownership flip. Tuning numbers (`ENEMIES_PER_WAVE`, `BIOME_OBSTACLE_DENSITY`) already cleanly imported via `Constants.js` ESM exports; no migration needed. Entity classes (Player/Enemy/Boss) + DLC files deferred to session 3 since they're consumed by server simulation via `require()` and stub layer; surface needs audit before sweep. Build green, tests still 48/48 Vitest + 80/80 parity.

- **#4 GameContext — session 1 of 5 (foundation)**. New `GameContext.js` singleton — a getter-only view onto the ~15 cross-cutting globals (`canvas`, `ctx`, `gameConfig`, `saveData`, `defaultSaveData`, `wave`, `arena`, `enemies`, `projectiles`, `world`, `player`, `player2`, `enemiesPerWave`, `biomeObstacleDensity`, `platform`, plus `registries.{biomes,heroes,enemies,dlcs}`). Each property is `Object.defineProperty` accessor that delegates to `window.X` so the underlying value stays where it always was — readers can adopt today without breaking any writer. Loaded as third ESM module in `game.html` (after `Platform.js`, before `Constants.js`). Three proof migrations: `Managers/InputManager.js` (`_getBindings()` + `_getGamepadBindings()`), `Managers/AudioManager.js` (`_categoryMultiplier`), `Managers/CrashReporter.js` (Electron flag in crash payload). `ARCHITECTURE.md` gains a new `GameContext` section documenting the 5-session migration arc. Sessions 2-3 add saveData / tuning / registries readers; session 4 flips ownership (`GameContext` becomes source of truth, `window.X` becomes alias) — risky step, couples with #11 RunState carve; session 5 drops redundant `window.X` shims where no DLC bare-reads. Every change in sessions 1-3 is additive.

- **Pass H step 1 — architecture follow-through** (#9, #15, #17, #7). **#9 `Platform.js`** centralises Electron detection + `fs` / `path` / `saveFilePath` / `configFilePath` access. Replaces duplicated `typeof process !== 'undefined' && process.versions.electron` checks in `Config.js` and `game.js` (saveFilePath wiring), and unblocks the same pattern in `Managers/SaveManager.js` / `Managers/CrashReporter.js` / `dlc/DLCManager.js` for follow-up passes. Loaded as the first ESM module in `game.html` so all later modules can read `window.Platform`. **#15 `AudioManager` lazy load** — switched the constructor's eager-fetch behaviour (~80 parallel HTTP requests on cold boot) to a three-tier `preload` policy: `'auto'` for music + the 12 hot-path SFX (`damage`, `death`, `dash`, `melee_all`, `enemy_damage`, `level_up`, `attack_*`), `'metadata'` for boss / pickup / weather SFX (metadata fetched up-front so duration is known, audio data deferred to first play), `'none'` for voice memories / story stings. Cold-boot audio bandwidth drops ~70 % without first-play hitch on common gameplay sounds. **#17 `structuredClone` sweep** — replaced every remaining `JSON.parse(JSON.stringify(...))` deep-clone outside the explicit fallback in `Player.js:1427`: `Config.js` (4 sites: gameConfig init, load-fallback, one-handed-scheme presets), `Managers/SaveManager.js` (fresh-save init), `game.js` (saveData fresh init + savedRun `buffs` / `runBuffs` snapshots). 7 sites swapped; `structuredClone` skips the JSON serialise/parse round-trip and handles `Map` / `Date` / typed-arrays if a DLC ever adds one. **#7 `BiomeRegistry`** — new `window.BiomeRegistry` namespace with `register(id, impl)` / `list()` / `get(id)` / `validate(id, impl)`. Required-method check on `generate()` (warns + skips if missing); recommended-method check on `update()` / `draw()` / `drawObstacle()` (info-level log if absent). Base biomes in `Biomes.js` migrated to `BiomeRegistry.register(...)`. DLC biomes still hit `window.BIOME_LOGIC[id] = …` directly (legacy path retained) — registry is additive, not breaking. Catches silent missing-method failures at registration time instead of mid-run.

- **game.js split — 5 new modules** (#1). First end-to-end pass at decomposing the 8379-line `game.js` into focused ES modules. Five new files extracted and wired through `import` from `game.js`; window shims preserved for DLC back-compat. **`Camera.js`** owns screen-shake state + `SHAKE_PRESETS` table, `shake(type)`, `triggerScreenShake`, `triggerVibration`, `triggerImpact`, `applyScreenShake(ctx)` helper (replaces the inline shake-apply block in masterLoop), and the photo-mode subsystem (`togglePhotoMode`, `tickPhotoMode`, `isPhotoMode`, `_PHOTO_HIDE_IDS`, F2/Esc keydown listener). Module-private `_shakeIntensity`/`_shakeDuration`/`_photoMode`/`_photoCam` state no longer leaks into game.js scope. **`Spawner.js`** owns `createExplosion(x,y,color,count)` + `spawnLevelUpAura(x,y,color)` particle bursts plus new thin wrappers `spawnEnemy(elite, forcedType)` / `spawnBoss(id)` that funnel `new Enemy/Boss` through a single chokepoint. Reaches `window.enemies` / `window.particles` (now mirrored from game.js module-scope arrays) so all callers see the same live references. **`Wave.js`** owns pure helpers: `enemiesNeededForWave(wave)`, `isWaveCleared(wave, killed)` (replaces three inline `enemiesKilledInWave >= ENEMIES_PER_WAVE * wave` checks at the objective + boss-trigger sites), `buildBiomePool(isStoryRun, hero)`, `getDLCBiomePool()`, `pickRandomBiome(types)`, `pickSeededBiome(wave, seed)` (online-co-op deterministic pick), `isStoryBossWave(wave, saveData, modes)` (MAKUTA at 50/100). `advanceWave()` body itself stays in game.js for this pass — entangled with 30+ run-scoped variables — but its biome-roll + Makuta-gate blocks now route through the new helpers and the function emits `wave:advance` on the EventBus on every increment. **`RunState.js`** owns `createRunStats({ startTime })` factory + `resetRunStats(rs)` + pure helpers (`bumpDamageSource`, `logUpgradePick`, `logKeyMoment`). Both `currentRunStats` initializations in `game.js` (module-scope default + `_resetGameState`) now use the factory. **`GameLoop.js`** owns the rAF + fixed-timestep harness: `createGameLoop({ targetFps, onRafTick, onFrame })` returns a `{ start, stop, frameDelay }` controller. Per-rAF callback runs `audioManager.update()`; the gated `onFrame(dt, ts)` callback drives the (still in-game.js) `masterFrame(dt, ts)` body once per fixed 60 Hz tick. `lastTime` drift correction + `cancelAnimationFrame` on stop live inside the harness. **Net effect**: ~210 lines moved out of game.js (8379 → 8169); each new module is self-contained (no implicit globals beyond well-known DLC shims) and importable from tests. Future passes can grow RunState into a real container for the ~540 module-scope globals (#11) and migrate more bodies into Spawner/Wave/Camera incrementally. Build green, 80/80 parity tests + 38 Vitest assertions still pass.

### Added
- **Pass C step 2 — minimap, photo mode, contributor docs** (#170, #51, #154). **#170 Minimap**: new `#minimap` 180×135 DOM canvas top-right; renders arena bounds, camera viewport rectangle, obstacles (brown), enemies (red), boss (yellow, larger), objective marker (cyan), player(s) (green / blue). Throttled to ~15 Hz to minimize DOM-canvas write cost. Hidden when `gameConfig.minimapEnabled = false` (new "Minimap" toggle row in Options → Visuals; defaults ON). **#51 Photo mode (live)**: `F2` toggles. Hides all 14 HUD overlays via `visibility: hidden` (`ui-layer`, `pause-screen`, `minimap`, `debug-overlay`, `hud-top-left`, `bottom-ui`, `p2-hud`, `notification-area`, `weather-display`, `objective-display`, `combo-display`, `chaos-challenge-hud`, `online-name-bar`, `hero-subtitle`, `weather-bar-wrap`), suppresses `arena.updateCamera` calls (manual control stays put), and runs `_photoModeTick()` every frame to pan with Arrow keys / WASD (Shift = slow, Ctrl = fast). Camera clamped to arena bounds. World keeps running — this is a "live" / cinematic mode rather than a hard pause; a true freeze-pause path is a future refinement (would require carving updates from draws inside the main loop). Esc / F2 exits. **#154 Contributor docs**: new [`CONTRIBUTING.md`](CONTRIBUTING.md) (setup, commands, PR checklist, commit convention, code conventions, file-tree cheat sheet) and [`ARCHITECTURE.md`](ARCHITECTURE.md) (high-level client/server split, build pipeline, module-by-module map of `game.js` / `Entities/` / `Managers/` / `UI/` / `dlc/`, save-data shape, server endpoints + networking notes, CI summary, backlog pointer).

### Added
- **Pass C step 1 — gameplay constants + damage pipeline** (#16, #18). **#16 Constants extraction**: new `GAMEPLAY` block in `Constants.js` consolidates 13 previously-inline magic numbers — `MAX_PARTICLES` (300), `MAX_FLOATING_TEXTS` (80), `DEFAULT_MELEE_RADIUS` (80), `DEFAULT_DASH_COOLDOWN` (180), `SERVER_VIEW_HALF_W/H` (1000/580), `INTERP_DELAY_MS` (100), `HITSTOP_HIT/CRIT_MELEE/CRIT_SHOT/BOSS_KILL` (2/5/4/12), `BOSS_DEATH_FRAMES` (180), `COMBO_TIMEOUT_FRAMES` (240), `SAVE_BACKUP_SLOTS` (5), `PAUSE_UPGRADE_CHIPS` (24), `PAUSE_CARD_CHIPS` (12). Exported as `GAMEPLAY` named export + `window.GAMEPLAY` shim. `game.js` (MAX_PARTICLES + 5 boss-death sites + floating-text cap + 3 hit-stop call sites), `Player.js` (combo timeout), and `Managers/SaveManager.js` (backup slot count) all reference the constants now. Balance tweaks no longer require grepping through 7000+ lines. **#18 Centralized damage pipeline**: new `applyDamage(target, dmg, opts)` helper in `game.js` consolidates the per-site boilerplate (invincibility check, `damageReduction` multiplier, `customOnDamage` shield hook, `recordPlayerDamage` stamp, run-stat accounting, combo reset, `damage` SFX, floating-text damage number, optional shake). Options: `label`/`color`/`size`/`prefix`/`sfx`/`noFloatText`/`noReduction`/`shake`. Wired at five cookie-cutter damage sites (acid-fog DOT — both primary + stacked weather, lava biome zone, explosive-mutator burst, exploder elite suicide). Each site dropped 4–6 lines of duplicated bookkeeping. Hero/biome-specific paths (chaos hooks, Earth momentum loss, Void realm shift, Thornmail reflect, Mirror Shield) stay site-local for now — pipeline is in place for incremental migration.

### Changed
- **Smog Quarter + Hall of Mirrors biomes — thematic backgrounds**. Replaced the flat single-color tints on Smoke + Mirror hero biomes with multi-layer composites (matches the Mindscape "crazier background" treatment). **Smog Quarter** (`SmogQuarterBiome.draw`) — sodium-light vertical gradient (smog horizon), diagonal sewer-grate cross-hatch floor, 10–14 dark soot-pool ellipses, 7–11 distant smokestack silhouettes anchored to top/bottom edges each with a rising plume, 6 wind-drifting fog banks (radial blobs, wrap around arena), 2 oscillating sodium-yellow searchlight cones from opposite corners, existing smog wisps, rising orange embers with flicker + glow (`lighter` composite), and a final vignette darkening edges. New state: `t`, `chimneys`, `sootPools`, `fogBanks`, `embers`, `searchlights`, `gratePhase` (chimneys/pools/searchlights generated once per arena; fog/embers updated each frame). **Hall of Mirrors** (`HallOfMirrorsBiome.draw`) — polished radial marble gradient (marine→black), camera-clipped diamond mirror-tile tessellation with per-tile shimmer, 6–9 distant fracture-crack clusters (each a radial-line burst with branch lines and a soft starburst centre), concentric reflection ripples spawning from arena centre every ~2.5s, 6-color rotating prismatic rays from centre, the existing horizontal prismatic sweep (slightly richer gradient), player ghost-echo silhouettes projected onto far walls (~every 0.6s, faint connecting line), chromatic edge aberration on left/right margins, plus existing edge-glint particles. New state: `t`, `ripples`, `rippleTimer`, `fractures`, `echoes`, `echoTimer`, `tileSize`, `prismAngle`. Both biomes also got proper `getHeroTheme('smoke' | 'mirror')` entries so the base bg fill matches the new palette instead of falling back to default gray.

### Added
- **Pass B — feel + recovery sweep** (#38, #40, #149, #159, #166, #167, #168). Seven items, independently shippable. **#38 Camera-shake taxonomy**: new `SHAKE_PRESETS` table + `shake(type)` helper in `game.js` so common impact events (`hitSmall`/`hit`/`hitBig`/`stomp`/`explode`/`boss`/`weather`) draw from one tuned set of `(intensity, duration)` values instead of magic numbers per call site. **#40 Damage-number polish**: `FloatingText` adds a `_detectCrit(text, size)` heuristic (text contains `'!'` or `size >= 25`); crit floats get arc trajectory (`(±5, -4)` initial velocity with extra gravity), starting scale ~1.6× shrinking to 1× over life, alternating white flash every 4 frames, and a 4-px outline. Pool `acquire()` recomputes crit on reuse. **#149 Cheat console**: tilde / backtick toggles a `#cheat-console` input overlay. Verbs: `give gold <n>`, `give xp <n>`, `set wave <n>`, `spawn boss <id>`, `kill`, `killall`, `god`, `heal`, `clear`, `help`. Echoes commands + responses to a 160-px log. Suppressed when typing into other inputs. **#159 Pause menu rework**: pause modal now shows an 8-cell run-stats grid (Wave / Level / Gold / Time / Kills / Dmg Dealt / Dmg Taken / Max Combo), the last 24 upgrades picked this run (most recent first, chip-styled), and up to 12 owned collection cards. `window.renderPauseMenu()` is called from `togglePause()` whenever the game pauses. CSS adds `.pause-stats-grid`, `.pause-stat-cell`, `.pause-section-title`, `.pu-chip`, `.pc-chip`. **#166 Crash recovery**: new `localStorage['5FreundeRunActive']` sentinel set on every `saveRunState()` and cleared on `clearSavedRun()` / clean exit (`beforeunload` only clears when `gameRunning === false`). At launch, after the menu paints, `_maybeOfferCrashRecovery()` checks for the sentinel; if a `saveData.savedRun` also exists, it prompts the user to restore — Yes calls `continueRun()`, No clears the sentinel. **#167 Pause music low-pass**: lazy Web Audio graph built on first pause (`AudioContext` + `BiquadFilter(lowpass, Q=0.8)`). `setPauseFilter(true)` ramps cutoff from open (22050 Hz) → 500 Hz over 180 ms; resume ramps back. Each music `Audio` element is plumbed through `createMediaElementSource` once and cached in a `WeakMap`. **#168 Death feedback**: new `recordPlayerDamage(target, label, dmg)` helper stamps `player._lastDamageSource = {label, dmg, time}` at every meaningful damage-take site: enemy contact (pass `enemy.subType`), projectile (`proj.shooterType`), acid fog (`'ACID FOG'`), lava (`'LAVA'`), bomber suicide (`'BOMBER'`), exploder elite (`'EXPLODER'`), explosive-mutator burst (`'EXPLOSION'`), and boss stomp (`this.name || 'BOSS'`). `gameOver()` reads the stamp and writes "Defeated by {label} — {dmg} dmg" into a new `#go-death-cause` line under the GAME OVER title (cleared on victory).
- **Controller button remap** (#131 gamepad parity) — gameplay actions (`shoot`, `melee`, `dash`, `special`, `pause`) now read XInput button indices from `gameConfig.gamepadBindings` instead of hardcoded literals. Defaults preserve the existing behaviour (`shoot:[7,5]`, `melee:[6,2]`, `dash:[1,4,0]`, `special:[3]`, `pause:[9]`); triggers still trip on `value > 0.15`. New `InputManager.isGamepadAction(gp, action)` helper drives `Player.js`, `Entities/PlayerController.js`, and `CoopGamepadController.js` (legacy literal checks kept as fallback). New "Customize Buttons" row in Options opens `#gamepad-remap-modal`; clicking a row primes `InputManager.beginGamepadRemap(action)`, which runs a `requestAnimationFrame` poll over `navigator.getGamepads()` and captures the first edge-triggered button press (snapshots currently-held buttons up front so a stuck button can't auto-bind). Esc cancels. Options gamepad menu nav is suppressed while a remap capture is in flight so the captured press doesn't double as menu input. Bindings persist through the existing `Config.js` disk + localStorage path and deep-merge with defaults.

### Fixed
- **`ReferenceError: isLevelingUp is not defined` in `Player.levelUp`** — `Player.js:173` does a bare read + write of `isLevelingUp`. `isLevelingUp` is a module-scoped `var` in `game.js` and was not exposed via the `Object.defineProperties(window, ...)` block, so cross-module ESM resolution threw on both the read at line 173 and the assignment at line 174. Added `isLevelingUp` + `isShopping` to the defineProperties block in game.js (same getter/setter pattern as `wave`/`bossActive`/etc.); bare reads/writes from any other ESM module now route through the window accessor.
- **Void biome "REALITY ERR_CONNECTION_RESET" teleport puts player into obstacles** — `FracturedBiome.update()` was offsetting `player.x/y` by ±200 every 5s with no collision check. Player landed inside void-biome code-block obstacles regularly and got stuck. Fixed: now tries up to 12 candidate offsets, clamps each to arena bounds, and skips any that fail `arena.checkCollision(cx, cy, radius)`. If all 12 collide the teleport is skipped silently (the notification only fires on a successful relocation).

### Changed
- **Audio Music / Sound-FX ON/OFF toggles removed** — the per-channel sliders added in Pass A already cover the off case at 0%, so the two redundant toggle rows are dropped. `gameConfig.musicEnabled` / `sfxEnabled` keys are kept and now derived inside `AudioManager.updateSettings()` from the corresponding volume value (`> 0` = on); every consumer that reads `musicEnabled` / `sfxEnabled` stays unchanged.
- **Mindscape biome (Psycho hero) — denser, "crazier" background**. Replaced the flat teal-tint draw with a 7-layer composite: (1) pulsing radial vignette tinted teal→deep-magenta, (2) 24-spoke mandala fracture lines from arena centre with sin-wobble jitter, (3) 5 concentric pulsing rings expanding outward, (4) 6 rorschach inkblot blobs (`filter: blur(8px)`, alternating teal/red) drifting + wrapping around arena bounds, (5) random horizontal screen-tear bands (6× denser during Hysteria) with a magenta echo line, (6) faint vertical chromatic stripes alternating teal/magenta with sin-phase displacement, (7) the existing dust particle field on top. New state in `update()`: `t`, `pulsePhase`, `tearBands`, `inkBlots`, `fracturePts`. All effects use composite operations + soft alphas so the canvas-clip and biome obstacles remain readable.
- **Options menu — full controller navigation pass**. Panel is now scrollable (`max-height: 88vh; overflow-y: auto;`) with thin scrollbar styling so the now-much-longer accessibility + audio + server sections fit on small screens. New gamepad shortcuts inside the OPTIONS state: **Left / Right** on any cycle-button (`opt-colorblind-btn` / `opt-fontscale-btn` / `opt-aimassist-btn` / `opt-onehand-btn` / the four volume buttons) cycles the value — Right advances one step, Left rotates backward N-1 steps to land on the previous value. **LB / RB** jump focus to the previous / next `.options-section`, scrolling the first focusable button in that section into view. Existing D-pad spatial nav + A/B select/back retained. New footer hint inside the panel documents the bindings. Module-level `_lastOptLB` / `_lastOptRB` flags handle edge-triggered LB/RB so holding the bumper doesn't repeat-jump every frame.

### Added
- **Pass A — high-leverage bounded sweep** (#140, #165, #127, #128, #39, #89, #22, #148, #20). Nine items, independently shippable. **#140 Save backups + restore UI**: `SaveManager` now ring-buffers the previous encoded blob into one of 5 numbered slots (`<savePath>.bak1`…`bak5` on disk, `5FreundeSave.bak1`…`bak5`+`.ts` keys in localStorage) before every overwrite — oldest-by-mtime slot is reused first. New `SaveManager.listBackups()` + `SaveManager.restoreBackup(slot)` API. New "Restore Backup" entry under the main-menu system column opens a modal listing each slot with timestamp + size; clicking RESTORE confirms, rotates the *current* save into a fresh slot, swaps the chosen blob in, verifies HMAC, then reloads. **#165 In-game changelog modal**: `vite.config.mjs` copies `CHANGELOG.md` into `dist/` on build; `_maybeShowWhatsNew()` runs after the loader hides, fetches `CHANGELOG.md`, parses the most recent `## [...]` sections newer than `gameConfig.lastSeenVersion`, and renders them into the new `#whatsnew-modal`. Closing the modal stamps `lastSeenVersion = APP_VERSION` so the same content doesn't re-show. **#127 Per-channel audio sliders**: `gameConfig.musicVolume` / `sfxVolume` / `voiceVolume` / `uiVolume` (0…1, default 100% except music 50%). `AudioManager._baseVolume` snapshot taken at construction; `updateSettings()` recomputes `track.volume = base * categoryMultiplier`; `_categoryOf(key)` routes `level_up_*`, `pickup_*`, `achievement_*`, `wave_completed`, `challenge_*` to the UI bucket and everything else to SFX. `playVoice` + `playHeroExclamation` apply `voiceVolume` directly to the per-shot `Audio` object. New cycle buttons (0/25/50/75/100%) under the Audio section in Options. **#128 Subtitle wire-through**: audit confirmed all 16 `playHeroExclamation` call sites flow through `_showSubtitle()` (gated on `gameConfig.subtitlesEnabled`). Added ARIA mirror — `_showSubtitle` also pushes `"{Hero}: {text}"` to `window.a11yAnnounce` so screen-reader users get the same coverage as the on-screen subtitle. **#39 Hit-stop**: existing `triggerHitStop(frames)` infrastructure (and the `_isHitStopped` skip in `masterLoop` Projectile + Enemy update loops) extended to fire at every boss-death init site (4 production paths: Evil Mode wave clear, boss in normal mode, boss in co-op host path, boss in versus path). 12-frame freeze on boss kill. `triggerHitStop` exposed via `window` for DLC consumers. **#89 WS origin / CSRF check**: `ALLOWED_WS_ORIGINS` env var (comma-separated) feeds a `verifyClient` callback on the `ws.Server` that rejects upgrade requests whose Origin header isn't allowlisted (native/Electron clients with no Origin pass through; the JWT in the query string remains the authn proof). Same allowlist also restricts CORS for the HTTP API. Empty allowlist = dev-mode wildcard so local testing is unaffected. **#22 Gradient cache helper**: new `cachedRadial(ctx, key, r0, r1, stops)` in `Utils.js` returns a cached `CanvasGradient`; key includes radius so different-radius callers don't share a gradient. Soft cap 512, LRU drop oldest insertion. Converted 3 hot per-frame projectile draws (`time` aura, `time` core, `love` heart) — incremental conversion path for the remaining ~107 sites. **#148 F1 debug overlay**: new `#debug-overlay` div toggled by F1 displays FPS / p50+p99 frame time (rolling 120-frame buffer) / wave / player position / enemy+projectile+particle+floatingText counts / spatial-hash cell count / current hit-stop frames. DOM writes throttled to 5 Hz. `SpatialHash.cellCount()` added for the cell stat. **#20 Object pools**: `Particle._pool` (cap 256) + `FloatingText._pool` (cap 128) static pools with `acquire()`/`release()` API. Every `new Particle(...)` and `new FloatingText(...)` call site in the codebase (game.js + Player + Enemy + Boss + Companion + Arena + EvilHeroes + 14 DLC files) converted to `.acquire()`. The masterLoop splice paths now call `Particle.release(p)` / `FloatingText.release(ft)` before splicing dead instances, so retired objects re-enter the pool instead of going to GC. Tests stay 80/80 parity + 48/48 Vitest; bundle 738 KB → 746 KB (+8 KB for new modal HTML/JS + backup logic + debug overlay code).

### Added
- **Accessibility pass** (#131–#139, except #134 which already shipped) — eight accessibility items landed in one sweep. **#131 Remappable keys**: `gameConfig.keyBindings` stores per-action key arrays (`moveUp/Down/Left/Right`, `shoot`, `melee`, `dash`, `special`, `pause`); `InputManager.isAction(action)` and `InputManager.eventMatches(action, e)` resolve against the live config. `Player.js`, `Entities/PlayerController.js`, and `game.js`' `onKeyDown` route input through these helpers (legacy `e.code` literals kept as fallbacks). A new "Customize Keys" modal (`#remap-modal`) lists every action; clicking a row primes `InputManager.beginRemap(action)`, the next valid keydown stores the binding (Esc cancels). **#132 Hold-to-fire toggle**: `gameConfig.holdToFireToggle`; when on, the left-mouse `mousedown` flips `mouse.leftDown` sticky and `mouseup` is ignored — single click toggles continuous fire. **#133 Aim assist slider**: `gameConfig.aimAssist` (0…1) blends `player.aimAngle` toward the nearest enemy via shortest-arc lerp every frame in `Player.update()`. Options UI cycles 0% / 25% / 50% / 75% / 100%. **#135 High-contrast UI**: `gameConfig.highContrast` toggles `body.high-contrast`; CSS rules force black-on-white panels + 2px borders on options/pause/levelup/shop/gameover screens (canvas/world unaffected). **#136 Font size scaling**: `gameConfig.fontScale` (0.8 / 1.0 / 1.25 / 1.5) sets `--ui-font-scale` on `:root`; HUD labels, options text, screen titles, and damage-number font sizes in `Entities/FloatingText.js` multiply by it. **#137 Screen reader hints**: `gameConfig.screenReaderHints` toggles `window.a11yAnnounce(msg)`; `UIManager.setUIState` announces state transitions ("Main menu", "Paused", "Level up — choose an upgrade", etc.) into a hidden `#a11y-live-region` (`aria-live="polite"`). **#138 One-handed control schemes**: `gameConfig.oneHandedScheme` ('off' / 'leftHand' / 'rightHand'); `window.applyOneHandedScheme(scheme)` overwrites `keyBindings` from `ONE_HANDED_PRESETS` (left-hand consolidates everything around WASD + Shift/Ctrl/Q; right-hand maps to arrows + IJKL + numpad-area keys). User-customised remaps can still be applied on top. **#139 Pause-on-tab-switch + controller disconnect**: `gameConfig.pauseOnFocusLoss` (default ON) and `gameConfig.pauseOnGamepadDisconnect` (default ON); `InputManager` listens to `document.visibilitychange` + `window.blur` (calls `togglePause()` if a run is active) and `gamepaddisconnected` (only pauses if `player.usingGamepad` was true). All new settings live under a new "Accessibility" section in the Options screen, persist via the existing Config.js disk + localStorage path, and deep-merge with defaults so old configs keep working.

### Fixed
- **Player.js dead `window.TutorialMode` guard** — `Player.dash()` and `Player.useSpecial()` both gated their tutorial event hooks behind `if (window.TutorialMode) TutorialMode.onDash/onAbility()`. `window.TutorialMode` was never set anywhere (TutorialMode.js doesn't self-expose, game.js doesn't expose it either), so the guard always failed and tutorial dash/ability progress events never fired — meaning the dash and ability tutorial objectives were impossible to complete. Fix: `Player.js` now `import { TutorialMode } from './TutorialMode.js'` directly (matches the Phase 10 explicit-imports pattern from MeleeSwipe), and the `window.TutorialMode` guard is dropped.
- **Player.js dead `window._syncSoundBiomeMusic` guard** — `Player.levelUp()` calls `window._syncSoundBiomeMusic()` to refresh Sound hero music after a level-up. The function exists in `game.js` but was never exposed to `window`, so the `typeof window._syncSoundBiomeMusic === 'function'` guard always failed and the music never updated. Added `window._syncSoundBiomeMusic = _syncSoundBiomeMusic` in the window-exports block.
- **Gray enemies / wrong enemy subtypes + DLC write-back** — `var wave`, `var enemiesKilledInWave`, `var bossActive`, and `var isPlayerDying` in `game.js` are declared with `// Exposed for DLC` comments, but ESM module-level `var` does not auto-assign to `window` (unlike classic scripts). `Enemy.js` reads bare `wave` → `window.wave` (never set) → fallback 1 → all subtypes resolve as BASIC → all enemies render gray. DLC files (`TutorialMode.js`, `AirHero.js`) wrote back to `window.wave`/`window.bossActive`/`window.enemiesKilledInWave` but those writes were silently discarded (game.js module vars never updated). Fixed with `Object.defineProperties` on `window` inside `game.js`, defining getter/setter pairs for all four vars: getters return the live module variable; setters update it in place. Both read and write directions now work with zero per-mutation sync boilerplate. No changes to DLC or TutorialMode files needed.
- **`waveTimer` ESM write-back** — `scripts/VersusTest.js` writes `waveTimer = 999999` to disable wave progression in versus mode, but in ESM `waveTimer` is module-scoped in `game.js` and never reaches the global scope. Added `waveTimer` to the second `Object.defineProperties` block so the bare write resolves to a window setter and updates the live module variable.
- **DLC bare-global vars not on window** — `companions`, `projectiles`, `particles`, `currentObjective`, `currentWeather`, `activeMutators`, `isDailyMode`, `isWeeklyMode`, `isChaosShuffleMode` were module-scoped `let`/`var` in `game.js` and never exposed to `window`. DLC files (`ChaosMode.js`, `WindBosses.js`, `Enemy.js`, `Biomes.js`, `AirHero.js`) read them as bare globals guarded by `typeof` checks — all resolved `'undefined'`, silently disabling companion spawning, boss projectile firing, objective-defense targeting, weather-conditional biome behavior, and story-mode detection. Added a second `Object.defineProperties` block (same getter/setter pattern as the DLC-var block) for all nine variables. Also exposed `getCoopTarget` via `window.getCoopTarget` for `Boss.js`/`Enemy.js` co-op targeting.
- **TutorialMode / EvilMode bare function globals** — `showNotification`, `openStory`, `advanceWave`, `checkAchievements`, `saveGame`, and `gameOver` were module-scoped in `game.js` and never exposed to `window`. `TutorialMode.js`, `EvilMode.js`, `Boss.js`, `TestingGrounds.js`, and `Museum.js` all call these as bare identifiers guarded by `typeof X === 'function'` checks — all resolved `false` and the calls were silently skipped. Added 6 `window.X = X` assignments. Also, `Object.defineProperty` setters/getters on `window.wave` etc. (see next entry) incidentally fixed `TutorialMode._forceBoss` — bare `wave` now resolves via the global getter instead of falling back to 1.
- **Enemy draw crash — player is undefined** — `window.player` was set once at module init (when `player` was still `undefined`), but `startGame`, the chaos-shuffle hero swap, and the hero-change path each reassigned the module-level `player` variable without syncing `window.player`. Enemy.js (and any bare-`player` global consumer) saw `undefined`, causing `TypeError: Cannot read properties of undefined (reading 'y')` on every run. Added `window.player = player` after each of the three reassignment sites. Same stale-reference fix applied to `window.meleeAttacks` (reset in startGame without syncing the window pointer).
- **Options "Show Intro Screens" label** — option was present but labeled "Intro Screens"; renamed to "Show Intro Screens" so ON/OFF meaning is unambiguous.
- **ReferenceError on game start: masksDroppedInWave / waveTimer not defined** — two game-state variables were used as bare identifiers in `game.js` but never declared. `masksDroppedInWave` (holy-mask drop cap, reset unconditionally in `startGame` and `advanceWave`) crashed every run; `waveTimer` (versus-mode sentinel) crashed only in versus mode. Added `let` declarations for both near the wave-state block. Root cause: ESM strict mode forbids implicit globals; classic-script mode silently created them on `window`.
- **Options toggles broken** — `Config.js:loadConfig()` was reassigning `gameConfig` to a new object, leaving `window.gameConfig` pointing to the old one. Calls to `toggleSetting` mutated the new object while `updateOptionButtons` read from the stale old one, so buttons always reflected defaults and appeared unresponsive. Fixed by using `Object.assign` to mutate the existing object in place.
- **startTestingGrounds ReferenceError** — function was omitted from the first window-exposure pass because it is the second call in a compound onclick (`document.getElementById(...); startTestingGrounds()`), which the initial grep missed. Added `window.startTestingGrounds = startTestingGrounds` to the window block in game.js.
- **ESM onclick ReferenceErrors** — game.js functions called from HTML `onclick` attributes (`skipTutorialPrompt`, `acceptTutorialPrompt`, `startGame`, `initMenu`, `continueRun`, `checkNewGame`, `confirmNewGame`, `closeConfirmDialog`, `quitGame`, `exitToDesktop`, `closeGame`, `togglePause`, `toggleCoopMode`, `toggleLobbyMenu`, `openDLCMenu`, `closeDLCMenu`, `startDailyChallenge`, `startWeeklyChallenge`, `confirmDailyStart`, `closeDailyInfo`, `closeStory`, `quitGlobalLobby`, `startOnlineTestArena`, `exportSave`, `importSave`) were no longer reachable as globals after the Phase 8b/2 ESM migration. Added a `window.X = X` block at the end of game.js for all 25 functions. Also added `window.versusMenu = versusMenu` in `UI/VersusMenu.js` — the instance was module-scoped but HTML uses `versusMenu.selectOnlineMode(...)` directly.

### Changed
- **ESM Phase 10 step 3 — redundant window guards pruned in game.js** — removed `if (window.createExplosion)` guards (2 sites) before `createExplosion(...)` calls, `if (window.TestingGrounds)` guards (4 sites) before `TestingGrounds.X(...)` calls, and `&& TutorialMode` truthy checks (5 sites) inside `if (isTutorialMode && TutorialMode) TutorialMode.X(...)`. All three identifiers are statically imported at the top of `game.js`, so the runtime guards were dead code that obscured intent. Also replaced two `window.player` and one `window.HERO_LOGIC['chance'].updateBigGamble(window.player)` reads from inside `game.js` with the local `player` variable for consistency.

### Added
- **`window.GAME_API` — formal DLC integration contract** — new namespace exposed at the bottom of `game.js` that groups every DLC-consumable game state, mode flag, entity array, helper function, and registry behind a single stable entry point. Uses getter/setter pairs so reads/writes stay live with module variables. Categories: live game state (`wave`, `bossActive`, `currentObjective`, …), mode flags (`isCoopMode`, `isOnlineMode`, …), entity arrays (`enemies`, `projectiles`, …), core actors (`player`, `player2`, `arena`), helpers (`showNotification`, `createExplosion`, `advanceWave`, `triggerImpact`, …), DLC registries (`HERO_LOGIC`, `ENEMY_LOGIC`, `BIOME_LOGIC`), and world context. Carries `GAME_API.version` matching `APP_VERSION` for future deprecation surfacing. Existing DLCs keep working unchanged; new DLCs should prefer `GAME_API.X` for forward compatibility.
- **CI guard — `// Exposed for DLC` window-binding check** — new `test.yml` step extracts every `let|var|const` declaration in `game.js` tagged with `// Exposed for ...` and verifies each one has a corresponding `window.X =`, `Object.defineProperty(window, 'X', ...)`, or `X: { get:` (defineProperties block) entry. Fails the build if any tagged var has no window binding. Catches the exact ESM regression class that produced gray enemies (module-scoped `var wave` with no explicit window export).
- **getHeroStats memoization** (#31) — `Player.js` keeps a `Map` cache of computed hero-stat blocks keyed by `(type, prestige:unlocked:level, JSON(metaUpgrades), achievementsList)`. On cache hit the function returns a `structuredClone` of the stored block — every level-up + spawn skips the skill-tree iteration, achievement-bonus loop, DLC `applySkillNode` hook, and full meta-upgrade math. Cache auto-invalidates whenever any input slice mutates (key strings change). New `window.invalidateHeroStatsCache()` exposed for explicit busts (DLC toggles, save imports). No behavioral change — parityTest 80/80 + 48/48 Vitest unchanged.
- **ESM Phase 10 step 2 — Player.js + EvilMode.js explicit imports** (#4 / #2 follow-up) — `Player.js` now imports `MeleeSwipe` directly from `./Entities/MeleeSwipe.js` (replaces bare-identifier fallback to `window.MeleeSwipe`). `EvilMode.js` now imports `HolyMask` from `./Entities/HolyMask.js`. With the bare-identifier consumers gone, the two corresponding `window.X = X` shims dropped. `HumanController` shim intentionally kept — server simulation provides its own stub via `global.HumanController` (real class has gamepad/keyboard dependencies that wouldn't load server-side), so Player.js cannot `import` it without breaking the parityTest. Bundle: 719.31 KB → 719.21 KB. Tests still 80/80 parity + 48/48 Vitest.
- **ESM Phase 10 — window-shim cleanup pass 1** (#4 / #2 follow-up) — game.js no longer reads cross-module symbols via a `window` destructuring bridge; explicit `import` statements at the top of the file pull in `Player`, `Enemy`, `Boss`, `Arena`, `Companion`, every `Entities/*`, every consumed `Managers/*` (class + singleton), `EvilMode`, `TutorialMode`, `TestingGrounds`, `CoopGamepadController`, `Altar`, `Manual`, `MenuBackground`, AI controllers, `onlineLobby` + `infoDialogueManager` singletons, `DLCManager`, `MEMORY_STORIES`, and the chaos helpers it calls bare. Dropped 30 now-redundant `window.X = X` class/singleton shims across `Managers/` (UIManager, InputManager, SaveManager, SpatialHash, EventBus, CrashReporter, StoryManager, PROCEDURAL_TEMPLATES, AudioManager-class, IntroManager-class, NetworkManager-class), `Entities/` (CardDrop, PowerUp, NetworkInputController, RecordingInputController, PlayerController, CompanionAIController), `UI/` (LevelUpUI, MainMenuUI, OptionsUI, HeroDetailsUI, ShopUI-class, SHOP_POOL, CompletionUI, StatisticsUI, AchievementsUI, CollectionUI, SkillTreeUI, OnlineLobbyUI, VersusMenuUI, BIOME_META), and top-level (EvilMode, TutorialMode, CoopGamepadController). Replaced `window.TutorialMode` / `window.CoopGamepadController` truthy guards in game.js with the bare identifiers (always defined via import now). Singletons (`audioManager`, `introManager`, `networkManager`, `eventBus`, `onlineLobby`, `infoDialogueManager`, every UI singleton like `levelUpUI`/`shopUI`/etc.) keep their `window.X` assignments — HTML `onclick` handlers and DLC index files still reach them through globals. Kept shims whose only consumers are *non-importing* modules: `MeleeSwipe` (Player.js bare-reads `new MeleeSwipe(...)`), `HolyMask` (EvilMode.js bare-reads `new HolyMask(...)`), `HumanController` (Player.js bare-reads `new HumanController(0)`), `AIController` (Faith of Fortune DLC bare-reads `new AIController(...)`), plus everything DLCs reference bare (`Projectile`, `FloatingText`, `Particle`, `Boss`, `Altar`, `Player`, `MEMORY_STORIES`, `Enemy`, `MemoryShard`, `DLCManager`, `Arena`, `Companion`, `GoldDrop`, `Manual`, `CompletionMenu`, `MenuBackground`, `STORY_EVENTS`, `TestingGrounds`, `CloudSaveManager`). Bundle: 722 KB → 719 KB. Tests still 80/80 parity + 48/48 Vitest. Lint stays at 328 warnings, 0 errors.
- **ESM Phase 8b/2 — game.js migrated** (#2 / #1 follow-up) — `game.js` (7469 lines) is now an ES module. `<script defer src="game.js">` in `game.html` becomes `<script type="module" src="game.js">`. A `window`-destructuring bridge block at the top of the file pulls in every cross-module symbol used as a bare identifier (`Player`, `Enemy`, `Boss`, `Arena`, `Companion`, all entity types, manager singletons + classes, mode/scene classes, AI controllers, lobby singleton, DLC manager class, MEMORY_STORIES, plus the chaos helpers game.js invokes directly). Mutable registries (`BIOME_LOGIC`, `HERO_LOGIC`, `ENEMY_LOGIC`, `DLC_REGISTRY`, `chaosState`) and UI singletons (`levelUpUI`, `shopUI`, etc.) keep their `window.` prefix at call sites so DLC-time extension stays observable. Every `window.X = X` shim later in the file is retained — DLC code (loaded via dynamic `import()` since Phase 9) still relies on globals. game.js was the last classic-script file in the project; with it migrated, Phase 10 (window-shim cleanup) is now tractable. Bundle: 602 KB → 722 KB (game.js is now a real ESM chunk, no longer a raw classic-script asset). Tests still 80/80 parity + 48/48 Vitest. Lint stays at 328 warnings, 0 errors.
- **ESM Phase 8b/1 — ChaosMode migrated** (#2 / #1 follow-up) — `ChaosMode.js` is now an ES module. Its 10 top-level mutable `let`s (`chaosShuffleOptions`, `chaosSelectionIndex`, `heroAffection`, `lostHeroes`, `nextWaveIsNemesis`, etc.) consolidated into a single `state` object exposed as `window.chaosState`. Internal code uses `state.X`; game.js (still classic) mutates the same object via `window.chaosState.X` — both sides hit the same memory, no divergence. `game.js` updated at 3 call sites (chaos gamble UI, run save/restore, nemesis-boss spawn) to read/write through `window.chaosState`. ChaosMode top-level functions (`spawnChaosCompanion`, `openChaosGamble`, `confirmChaosGamble`, `updateChaosGambleUI`, `manageAffection`, `generateChaosObjective`, `updateChaosObjective`, `checkChaosEvent`, `completeChaosObjective`, `getAvailableChaosBosses`) all gained `window.X` shims. Bundle: 589 KB → 602 KB. Tests still 80/80 + 48/48.
- **ESM Phase 9 — DLC loader** (#29 / #2 follow-up) — `dlc/DLCManager.js` is now an ES module. The classic `<script>` injection in `loadScript()` was replaced with native dynamic `import()`. Browser treats every dynamically-loaded DLC file as an ES module, but the files themselves needed no changes: their existing `window.X = X` shims keep cross-file references working, and the server-side simulation still `require()`s DLC hero/biome files unchanged (Node 24 treats files without `export` as CommonJS, side effects run via `global.window = global`). Vite static-asset mirror keeps `dist/dlc/` byte-for-byte identical to source. Bundle: 584 KB → 589 KB (DLCManager rolled in). Tests still green.
- **ESM Phase 8a — EventBus module** (#1 foundation) — new `Managers/EventBus.js` (~80 lines): synchronous pub/sub for cross-module signalling. Naming convention `domain:event` documented for the upcoming game.js split (`wave:start`, `player:level_up`, `boss:spawn`, etc.). Errors from one handler don't abort siblings. 10 Vitest cases covering `on`/`off`/`once`/`emit`/`clear`/`stats` semantics + throw isolation + self-unsubscribe-during-emit. Test count: 38 → 48.
- **ESM Phase 7 — UI + auxiliary game modules** (#2 follow-up) — 32 more files migrated to ES modules: `Config`, `AltarData`, `Biomes`, `Museum` (+ `MuseumEntity`, `MUSEUM_DIALOGUES`), `Altar` (+ `altarUI`, `ALTAR_HERO_META`, `ALTAR_TIER_LABELS`), `GlobalLobbyScene` (+ `GLOBAL_EMOTES`, `HERO_COLORS`, `getHeroColor`), `Tutorial`, `MemoryStories`, `MemoryShard`, `CompletionMenu`, `EvilHeroes`, `EvilMode`, `TutorialMode`, `TestingGrounds`, `CoopGamepadController`, `OnlineTestBot`, `shared/world.js`, `scripts/VersusTest.js`, and all 14 files under `UI/` (`HeroDetails`, `Completion`, `Collection`, `SkillTree`, `Achievements`, `Statistics`, `Shop`, `LevelUp`, `Options`, `OnlineLobby`, `VersusMenu`, `InfoDialogueManager`, `MainMenu`, `MenuBackground`). `ChaosMode.js` deferred — shares mutable `let`-declared state with game.js, will land with the Phase 8 game.js split. Bundle: 208 KB → 584 KB (70 modules transformed). ESLint config gains a `scripts/VersusTest.js` override for browser-ESM (sibling Node scripts stay CommonJS).
- **ESM Phase 6 — Entities + core game classes** (#2 follow-up) — `Player`, `Enemy`, `Boss`, `Companion`, `Arena` (+ internal `BiomeZone`/`Obstacle`/`Trap`), and every file under `Entities/` (`Projectile`, `MeleeSwipe`, `Particle`, `FloatingText`, `GoldDrop`, `CardDrop`, `HolyMask`, `PowerUp`, `PlayerController` + `HumanController`/`AIController`/`CompanionAIController`, `NetworkInputController` + `RecordingInputController`) are now native ES modules with explicit `export` lists. Old UMD `module.exports` footers replaced with ESM exports. `window.X = X` shims retained so classic-script callers (game.js, DLCs, biomes) keep resolving identifiers as bare globals. `game.html` marks all 15 files as `<script type="module">`. Bundle: 98 KB → 208 KB.
- **Server-side ESM interop** — `server/simulation/loader.js` now uses `loadClass(path, name)` helper that detects `__esModule` namespace objects (returned by Node 22.12+ `require()` of ESM modules) and unwraps to `.default` or named export. Server simulation still loads Player/Enemy/Arena/etc. via the same `require()` calls, transparently consuming the new ESM exports.
- **ESM Phase 5 — Managers** (#2 follow-up) — every file under `Managers/` is now a native ES module: `CrashReporter`, `StoryManager`, `IntroManager`, `InputManager`, `UIManager`, `AudioManager`, `CloudSaveManager`, `NetworkManager` (plus `SaveManager` + `SpatialHash` from Phase 1). Each exports its class/instance and retains `window.X = X` shims so classic-script callers (game.js, DLC files, entities) see no behaviour change. `game.html` marks all `Managers/*.js` as `<script type="module">`. Vite bundle grew from 24 KB → 98 KB as more code rolled into the build graph. parityTest 80/80 + Vitest 38/38 still green.
- **Build pipeline** (#2) — Vite 8 introduced as the dev server + production bundler. `npm run dev` boots Vite at `http://localhost:5173` with full-page reload on file save and CSS HMR for `main.css` (no reload, live style swap). `npm run build` produces a fingerprinted bundle in `dist/`. `npm start` (electron-forge) loads `dist/game.html` when present, falls back to source `game.html` otherwise — keeps the existing dev flow working unchanged. `vite-plugin-electron-renderer` keeps renderer `require('fs')` calls (Config.js, SaveManager.js, DLCManager.js, game.js) working post-bundle.
- **ESM leaf-file migration** (#2 → #1/#3 unlock) — `Utils.js`, `Managers/SpatialHash.js`, `Constants.js`, and `Managers/SaveManager.js` are now native ES modules with explicit `export` lists. Each retains its `window.X = X` shim so classic-script callers see no behavior change. `game.html` marks these four files as `<script type="module">`; every other script tag now carries `defer` so module + classic execute in document order after parsing. Inline `<script>` blocks that read `APP_VERSION` are wrapped in `DOMContentLoaded` listeners to honour the new ordering. This is the first phase of the longer ESM migration (Managers, Entities, UI, game.js, DLCs in future sessions).
- **ESLint + Prettier + custom lint rule** (#13, #14) — flat ESLint 9 config in `eslint.config.mjs` with warnings-only policy (zero errors, lint never blocks). New local plugin `eslint-plugin-5freunde/` with one rule `no-foreach-splice` that detects the `arr.forEach((e, i) => arr.splice(i, 1))` index-skip bug class — the audit pass on 2026-05-10 found 11 of these; this run found 8 more. `.prettierrc` documents project style (4-space, single quotes, 120-col). `npm run lint` / `npm run lint:fix` scripts.
- **Vitest unit-test harness** (#12) — `tests/` directory with 38 tests across 6 files: `saveManager.test.js` (schema migration invariants), `mulberry32.test.js` (determinism + uniformity), `spatialHash.test.js` (broad-phase query correctness), `hermiteInterp.test.js` (snapshot interpolation edge cases), `plausibility.test.js` (leaderboard anti-cheat caps), `rateLimit.test.js` (token-bucket refill semantics). New `server/anticheat.js` extracts the plausibility + rate-limit helpers from `server.js` so they're unit-testable without booting the WS server. `npm test` runs Vitest then the existing `server/simulation/parityTest.js` (80/80 assertions passing after the AirHero stub fix below).
- **GitHub Actions CI** — `.github/workflows/test.yml` renamed to a `CI` workflow that runs on every push/PR: dependency install, the existing legacy-globals lint, ESLint, Vite production build, and the full test suite (Vitest + parity).

### Fixed
- **parityTest level-up flow typo** — `server/simulation/GameSession.js:211` used `this.world.player` / `this.world.player2` instead of `this._world.player(2)`. The level-up choice handler crashed every test run since the World refactor. Caught by the Vitest harness pass.
- **AirHero `Cannot convert object to primitive value` on server tick** — `dlc/waker_of_winds/AirHero.js:777` reads `ctx.canvas.width` for HUD layout. The server-side `_noopCtx` Proxy in `server/simulation/loader.js` returned itself for every property access including `Symbol.toPrimitive`, so arithmetic operations on stub fetches (`screenW / 2`) crashed. Added explicit `Symbol.toPrimitive` / `valueOf` / `toString` handlers on the Proxy that yield `0` / `''`. AirHero tick now runs cleanly server-side.
- **Removed accidental devDep** — `install@^0.13.0` was sitting in `devDependencies` from an earlier mistype (per improvements #153). Dropped during the npm install pass.

### Changed
- **`server/server.js` anti-cheat helpers** — `plausibilityReject` and the token-bucket logic now live in `server/anticheat.js`. `server.js` imports them as a thin wrapper that adds the Express middleware shape. No behavior change.

- **End-of-run breakdown** (#160) — victory + game-over screens now render a third section below the existing Run Performance / Final Build columns:
  - **Damage by Source** — proportional bars showing projectile / melee / special / DoT contributions. Each damage site (`bumpDamageSource`) buckets into its source so future weapon types add a row without code changes.
  - **Upgrades Picked** — full timeline `W{wave} {title} {time}` of every level-up choice the local player made. Hero-specific upgrade titles are looked up via `HERO_LOGIC[type].upgradePool` when present.
  - **Key Moments** — boss kills + level-ups timestamped per wave. Future hooks (near-deaths, special-form activations) can push into `currentRunStats.keyMoments`.
- **Reduced Motion accessibility mode** (#134) — new Options toggle (`gameConfig.reducedMotion`) that disables camera shake, weather particle overlays, and the pulsing red low-HP vignette. Static vignette + damage numbers remain so combat feedback stays intact. Helper `isReducedMotion()` is exposed for future renderers to gate on.
- **Colorblind correction modes** (#50) — new Options control cycling through Deuteranopia / Protanopia / Tritanopia daltonization filters, applied via SVG `<feColorMatrix>` on the canvas element. Off by default. The filter definitions live in `game.html` (`#cb-*`) so they apply without any draw-call changes.
- **Crash reporting** (#97) — new `Managers/CrashReporter.js`. Captures `window.onerror`, `unhandledrejection`, and a 30-entry breadcrumb ring of `console.warn`/`error`. Posts coalesced reports (1 per stack per minute) to `POST /api/crash`. Opt-out via Options → "Crash Reports". Server endpoint appends JSONL to `data/crashes.jsonl`; admin dashboard sees last 200 entries at `GET /api/admin/crashes`. No PII — username stripped, IP pseudonymised before storage.
- **Daily seed leaderboard tagging** (#61) — daily/weekly runs now stamp their `dailySeed` (YYYYMMDD / YYYYWW) on leaderboard POSTs. New server column `scores.daily_seed`; `GET /api/leaderboard?dailySeed=20260510` returns per-seed leaderboards. Mutator RNG was replaced from a Math.sin-based seed (biased) to `mulberry32` (uniform). New `getSeededRng()` / `initSeededRng()` globals give future arena/drop code an opt-in seeded RNG bound to today's seed so all players on the same day share identical rolls.
- **Save schema versioning + migrations** (#10) — `SaveManager.SCHEMA_VERSION` + `MIGRATIONS` table. Each load reads `data.version`, runs every applicable migration in sequence, and writes a one-shot pre-migration backup (`save_data.json.pre_v{X}_to_v{Y}.bak` on disk, separate localStorage key on web) before the first write. Saves without a version field are treated as v0. `defaultSaveData.version` is now stamped on every write.
- **Online anti-cheat session tokens** (#87) — server issues a per-user signed session token at `START_ONLINE_GAME`. The client returns it on `POST /api/leaderboard`; the server clamps the client-claimed score downward to its own authoritative `wave`/`score` and marks the entry `verified=1`. Unverified submissions go through a heuristic plausibility check (wave/score/time ratios) and are tagged `verified=0`. New scores column `verified`. `GET /api/leaderboard?verified=1` filters to server-confirmed entries.
- **Rate limiting on auth + leaderboard + crash endpoints** (#88) — new lightweight token-bucket middleware (no external dependency). Per-IP limits: `/api/register` 5/hour, `/api/login` 10/minute, `/api/leaderboard` POST 10/hour, `/api/crash` 20/10min. Buckets are swept every 5 minutes to bound memory.
- **HTTPS / WSS support** (#90) — when `TLS_CERT_PATH` + `TLS_KEY_PATH` env vars are set, the server boots `https.createServer` with the supplied cert (and optional `TLS_CA_PATH` chain) and the WebSocket upgrades to WSS automatically. Falls back to plain HTTP for local dev. The client's `serverUrl` regex (`http→ws`) already handles the `https→wss` transition.
- **WebSocket permessage-deflate compression** (#158) — enabled on the server's `ws.Server` with conservative zlib settings (level 3, 256-byte threshold so PING/INPUT skip compression). Cuts snapshot bandwidth ~50–70% on internet links.
- **Cubic-Hermite snapshot interpolation** (#155) — client-side `_onlineInterpBuf` was a linear lerp; now a Catmull-Rom cubic Hermite with tangents derived from neighboring snapshots. Smoother during dash transitions where linear interp produced a visible corner at each snapshot boundary. Boundary samples (first/last in buffer) still fall back to linear so the edges stay well-defined.
- **Variable server tick rate** (#156) — GameSession nominal tick stays at 30 Hz (33 ms) but drops to 20 Hz (50 ms) when `enemies + projectiles > 180`, with 140-entity hysteresis on the way back up. `TICK_FRAMES` is now per-tick instance state so simulated game speed stays constant across tick-rate changes (sub-step count scales). The tick loop is now self-rescheduling (`setTimeout` chain) rather than `setInterval`.
- **MTU-aware snapshot chunking** (#157) — snapshots with more than 100 entities are split across 2–3 `SNAPSHOT` messages tagged with `chunk: {seq, idx, of}`. Client `_onlineHandleSnapshot` buffers chunks by `seq`, reassembles when all parts arrive, and evicts stale partials after 500 ms. Small snapshots ship unchanged (backward-compatible — the chunk field is optional).
- **Live authoritative score relay** — GameSession now calls an optional `onTickStats` callback every tick, letting server.js keep `_sessionScores` fresh so leaderboard verification works even if the client posts before the WS `GAME_OVER` message lands.
- **Spatial hash infrastructure** (#19) — new `Managers/SpatialHash.js` (uniform-grid broad phase, 128 px cells). Rebuilt once per frame from the enemy list and queried by AOE radius scans (explosive projectile splash, wall-hit explosions, swarm self-destruct). Full nested projectile-vs-enemy loop inversion is left for a follow-up pass.
- **DLC parallel load** (#29) — `DLCManager.init` now awaits enabled DLC loads via `Promise.all` instead of sequential awaits. Each DLC ships ~50 KB of scripts; on 5+ enabled DLCs this cuts startup wait by hundreds of milliseconds on internet-hosted assets.
- **Online mode version gate** — opening Online mode now checks whether the running version is the latest. If a newer version exists, a blocking dialog is shown with a "Download Latest Patch" button; the lobby cannot be entered until the player updates.
- **DLC: Disciples of Deception — level-up SFX generation** —
  - `scripts/generate_levelup_sfx.js` extended with 3 hero entries (`level_up_psycho`, `level_up_mirror`, `level_up_smoke`), each with an archetype-appropriate ElevenLabs prompt: Psycho gets glitching teal synapse crackles + dissonant rising chorus; Mirror gets crystalline ascension + prismatic refraction; Smoke gets industrial bass drone + smoky pulse fading into a hiss. Generate per hero with `node scripts/generate_levelup_sfx.js --hero=psycho` (or `mirror` / `smoke`); dry-run verified.
  - `Managers/AudioManager.js` — registered the 3 new tracks (`level_up_psycho`, `level_up_mirror`, `level_up_smoke`) pointing at `audio/sounds/level_up_<hero>.wav`, and added the hero keys to the volume-config array so they play at the same 0.85 volume as base/DLC level-up SFX. `UI/LevelUp.js` already resolves the right track via `level_up_${player.type}`, so no additional wiring needed.
- **DLC: Disciples of Deception — Altar of Mastery integration** — the 3 Disciples heroes are now fully wired into the Altar UI and convergence system:
  - 3 hero altar trees (3 nodes each, RUNE I/II/III) keyed in `ALTAR_TREE['psycho'|'mirror'|'smoke']`. RUNE I shaves 10% off cooldowns; RUNE II buffs the hero's signature mechanic (Hysteria Gain / Reflect Damage / Cloud Radius); RUNE III is a unique upgrade ("Bleeding Edge", "Glasshouse", "Suffocation").
  - 13 cross-hero **convergences** added to `ALTAR_TREE.convergence` (IDs `cv_dod_*`): 4 each for Psycho (Fire / Ice / Lightning / Time), Mirror (Water / Metal / Lightning / Air), Smoke (Fire / Poison / Plant / Earth), plus 1 pack-wide trio (`cv_dod_trio`: Master of Deception — confused/blinded/reflected enemies take +25 % damage from all sources).
  - In-combat hooks wired for the simpler convergences: Burning Mind (DoT on confused), Mental Loop (+50 % Hysteria duration + 30 % slower confused walk), Plate Glass (-30 % damage taken while Mirror Shield is active), Hydroflect (full Water knockback on reflected projectiles), Smoke Bomb (60-dmg fire burst on cloud spawn), Toxic Smog (poison DoT in clouds + cloud-life +1 s), Spore Cloud (1 HP/0.5 s self-heal in own cloud), Dust Cloud (enemies in clouds tagged `_smokeRangedLock`). Vulnerability tag (`_dodVulnerable`) applied for the trio mutation.
  - `ALTAR_HERO_META` (in `Altar.js`) now includes Psycho / Mirror / Smoke entries with icon + RGB so the altar selector renders them with proper styling instead of falling back to the `?` placeholder.
- **DLC: Disciples of Deception — Ultimate forms** — every Disciples hero now has a unique level-10 transformation, presented in the level-up screen alongside a regular upgrade option:
  - **Psycho — DELIRIUM**: 10 s duration. Permanent confusion on every enemy in range, +50 % movement speed, every projectile splits into 5 erratic shots (vs. 3 in normal Hysteria). Teal screen-edge vignette.
  - **Mirror — REFRACTION**: lasts until you take damage. Mirror Shield is permanently active (any projectile within 70 px is reflected at 150 % damage and heals 5 HP). Four mirror panels orbit the player at 70 px radius.
  - **Smoke — INK STORM**: 10 s duration. A 200 px ink cloud follows the player and continuously slows + DoTs nearby enemies (4 dmg every 0.5 s). Every enemy in the arena is permanently blinded for the duration.
- **DLC: Disciples of Deception — manual entry** — Game Guide now has a "Disciples of Deception" tab (auto-shown when the DLC is enabled). Covers all three heroes, their core mechanics, Ultimate forms, the three new biomes, and what's intentionally not in the pack (no story, enemies, cards, or music).
- **DLC: Disciples of Deception** — first character-only DLC. Adds three deception-themed heroes:
  - **Psycho** (Teal `#1abc9c`) — chaos disruptor with a Hysteria Gauge that fills from damage and kills. When full, enters a 6 s Hysteria Mode (+40 % move speed, projectiles split into 3, nearby enemies confused). Special: **Mind Fracture**, a bouncing psychic bolt that confuses up to 4 targets.
  - **Mirror** (Marine Blue `#1a5276`) — reactive defender. Special **Mirror Shield** (3 s) reflects enemy projectiles back at 150 % damage and heals 5 HP per reflect; melee hits during shield trigger a point-blank burst. Also fires a **Shatter** shard that splits into 6 fragments on impact.
  - **Smoke** (Slate Gray `#5a5a6e`) — control specialist. Each dash drops a smoke cloud (radius 60, 4 s, max 3 simultaneous) that slows enemies 40 % and disrupts targeting. Special: **Blackout**, a radius-120 burst that damages and blinds enemies for 3 s.
  - Each hero ships with: stats, custom upgrade pool, permanent altar upgrades, skill tree weights, dedicated SFX (attack, melee, dash, special, mechanic-specific), all 13 standard voice exclamations, and 3 achievements.
  - Three new biomes registered to `window.BIOME_LOGIC` and the `dlcBiomes` random pool: **The Mindscape** (glitch-step obstacles, teal sparks, Hysteria-aware density), **The Hall of Mirrors** (parallel mirror panels, 8 s prismatic floor sweep, edge glints), **The Smog Quarter** (dense concrete blocks, 3-4 smog pockets that stack with Smoke's clouds, 20 s wind-direction shifts, persistent gray haze).
  - Main-menu wiring: heroes auto-appear in Standard/Versus/Online via `BASE_HERO_STATS`. The green main-menu primary button reads "▶ Standard Mode" and routes to `checkNewGame('STANDARD')` when one of the three heroes is selected (since the DLC has `noStoryMode: true`); other DLC heroes still route to story mode as before.
- **Online Sim mode in Testing Grounds** — pressing `D` in the main menu now shows a mode chooser: "Local" (existing) or "Online Sim". Online Sim auto-connects a bot second player, runs the full online lobby handshake, and starts the game through the server-authoritative simulation path. No second human needed.
- **test-arena.js** — standalone solo online-mode test script (Node.js); boots two bots against the running server. Usage: `node test-arena.js [hostHero] [botHero] [mode]`.
- **Online: story chapter sync** — in online co-op story mode, the continue button now waits for both players to click before closing the story screen. Local player sees "Waiting for partner…"; if partner clicks first the button reads "CONTINUE → (partner ready)".
- **Online: Maze of Time (EoE DLC) sync** — the Maze of Time map now opens for both players when the host has a Time or Love hero. The host sees the interactive map and picks the next node; the guest sees the full map in read-only spectator mode with a "Waiting for host to choose path…" message and no clickable controls. On host confirmation the selected node is relayed via the server (`MAZE_NODE_SELECTED`) so both clients enter the same wave with the same biome, enemy pool, and modifiers. Host-only actions (`initForRun`, `completeNode`) do not touch the guest's local maze save data.

### Fixed
- **Regression follow-up to the audit pass** (2026-05-10):
  - **`shadeColor` could emit `'#XXYYNaN'`** for `#RGB` shorthand or unparseable input, making `addColorStop` throw `'#8408NaN' could not be parsed as a color` and crashing the game loop mid-frame. The crash sat upstream of the spawn block, so the visible symptom was "no enemies spawn for any hero" plus a per-frame Spirit/Standard-mode color SyntaxError. `shadeColor` now expands `#RGB` to `#RRGGBB`, validates the parsed channels, and returns the input color unchanged on invalid input.
  - **PoisonHero `saveData.altar` access guarded** in `dlc/symphony_of_sickness/PoisonHero.js` (lines 32 and 218). The DOT-tick path runs every 30 frames during normal play and would throw `Cannot read properties of undefined (reading 'altar')` when `saveData` was undefined for a frame. Now guarded with `typeof saveData !== 'undefined' && saveData?.altar?.active`.
- **Codebase audit pass — bugs, inconsistencies, and perf issues across core, DLC, networking, and UI** (2026-05-10):
  - **Player.js altar cooldown reductions were dead code**: the `f1/w1/i1/p1/m1` altar cooldown bonuses were nested inside the `else if (this.type === 'chance')` branch in `setupSpecial()`, so `this.type === 'fire'` etc. could never match. Hoisted the altar block out of the chance branch so the bonuses now apply to the heroes they target.
  - **`forEach + arr.splice(index)` index-skip bug across hot game loops** in `game.js` — affected `memoryShards`, `goldDrops`, `cardDrops`, `holyMasks`, `powerUps`, `projectiles`, `meleeAttacks`, `particles`, `floatingTexts`, and the main `enemies` loop. Each splice during forEach skipped the next element, leaving "dead" entities alive for an extra frame and silently dropping ~half of pending removals on bursty deaths. Converted all of these to backwards `for`-loops; inner `return` calls became `continue`.
  - **Companion synergy beam drawn during update phase** (`Companion.js`): `performSynergy()` was called from `update()` and called `ctx.save()/stroke()/restore()` directly, mixing logic and render phases. Split into target-finding (logic) + state (`_lastTarget`, `_lastTargetTimer`) consumed by `draw()`. Also added a `typeof enemies` guard to avoid a ReferenceError if the companion outlives the run state.
  - **`Boss.js` crashed for DLC heroes** without a `saveData` entry — `saveData[player.type].prestige` now uses optional chaining + nullish fallback to `0`.
  - **Echos of Eternity TimeBoss projectiles malformed** (`dlc/echos_of_eternity/TimeBosses.js`): `spawnProj` passed `vx, vy` as separate scalars to `new Projectile(...)`, but the constructor expects a single `velocity` object as the third arg, with `radius`/`type`/`knockback`/`isEnemy` after it. Every TimeBoss burst attack was producing projectiles with `velocity = vx (number)`, `damage = vy`, `color = damage`, etc. — likely the cause of bizarre boss attack behavior. Now wraps `{x: vx, y: vy}` and reorders the trailing args correctly.
  - **Faith of Fortune ChanceHero — multiple gameplay bugs**:
    - Windfall buff multiplied `player.speedMultiplier *= 1.5` *every frame*; the dead code block (with a "if we multiply every frame it explodes" comment) was kept and never returned. Removed; rely on the existing `player.buffs.tempSpeed = 0.5` path.
    - `BAD` and `DIAMOND` outcomes used `player.speedMultiplier *= 0.5; setTimeout(() => player.speedMultiplier /= 0.5)` and `player.damageMultiplier *= 2; setTimeout(/= 2)` — the divide-by-multiply-restore was correct only in isolation; if a second proc landed within 5 s the timeouts compounded and never restored. Now snapshots the prior value and restores to it.
    - `SpiritHero.shootMantra` onHit closure referenced bare `e.fireTicks/e.x/e.y` but the parameter is named `enemy` — every Mantra hit threw `ReferenceError` when the SacredFlame or HolyWater convergences were active. Replaced `e` with `enemy` throughout the closure (and used safe `vx/vy` defaults).
    - `JACKPOT` slot outcome `enemies.forEach(e => e.hp -= 7777)` skipped `player.onKill`, breaking XP/gold/kill counters on screen-wipes. Now invokes `onKill` per dead enemy.
    - Spirit Tranquility set `e.speedMultiplier = 0.5` on every nearby enemy and never restored it — enemies stayed slowed forever after leaving range. Now tags enemies with a base-speed snapshot and a frame stamp; restores the original speed once the tag goes stale.
    - `startStoryDuel` did `enemies = []` (reassignment), breaking any other module holding the prior reference. Switched to `enemies.length = 0` to preserve array identity.
  - **Waker of Winds Air hero "Storm" buff was permanent** (`AirHero.js:236-241`): unconditionally forced `speedMultiplier ≥ 1.5`, `damageMultiplier ≥ 1.5`, and 0.5 HP/s regen every frame, ungated by transform/hurricane state. Now gated on `transformActive || hurricaneActive`.
  - **Waker of Winds story injected duplicates on hot reload** — `injectStory` always concat'd `WIND_STORY_CHAPTERS` onto `STORY_EVENTS`; now guards against a duplicate by checking the first chapter id.
  - **`Managers/StoryManager.js` mutated shared `STORY_EVENTS` entries** — Arc-2 hero-biome enforcement wrote `event.data.biome = 'HERO'` onto the shared event, so subsequent runs read leftover state from a previous hero. Now clones the event before annotating.
  - **`UI/SkillTree.js` skill trees collided** for heroes whose `type` strings have the same length (e.g. `fire` and `ice` both → `seed = 4` → identical sin sequence). Replaced length-as-seed with a string-hash so each hero gets a distinct deterministic tree. Removed two no-op `desc.replace('+','+').replace('-','-')` no-ops.
  - **`UI/Achievements.js` rendered titles/descriptions via `innerHTML`** — XSS risk for any DLC-supplied or future user-derived text. Now built via `textContent` + `appendChild`.
  - **`Enemy.js`**: (1) `getBiomeEnemyType(wave, ...)` referenced the bare global `wave` instead of the world-injected `_wave`, breaking server-side simulation; (2) the spawn safe-loop's `if (!_arena?.checkCollision(...)) safe = true; else if (!_arena) safe = true;` had an unreachable second branch (the `?.` already returns `undefined` for null arena, taking the truthy branch) — would loop forever in a worldless context. Reordered to `if (!_arena) safe; else if (!_arena.checkCollision(...)) safe`. (3) `AURA_HEAL` healed beyond cap when `e.maxHp` was falsy (fallback was `e.hp * 2`); now `e.maxHp ?? e.hp`. (4) Initialized `hitFlashTimer` to `0` in the constructor and added optional-chain guards on `saveData.collection.includes(...)`.
  - **`Player.js`**: (1) `combo > 50` overwrote `goldMultiplier = 2`, wiping all the meta/achievement bonuses set in the constructor. Now stores a `_goldMultBase` baseline and applies the combo multiplier on top. (2) Added optional-chain on `saveData.metaUpgrades.greed` (crashed if save not yet loaded). (3) `getHeroStats` deep-cloned via `JSON.parse(JSON.stringify(...))` on every call (level-up, hero swap); now uses `structuredClone` when available. (4) `incrementCombo` now null-checks `currentRunStats` before reading `maxCombo`.
  - **`ChaosMode.js` defaulted multiplier penalties to `0`** — `(p.speedMultiplier || 0) - 0.1` and `(p.damageMultiplier || 0) * 0.9` would set the multiplier to `-0.1` / `0` for any player without a prior chaos modifier. Switched to `?? 1`. Also fixed `val.isMelee` access on `val` parameters that are sometimes numbers (KILL events use `val.isMelee`, GOLD events use a numeric val) — now type-checks before reading.
  - **DLC fixes**:
    - `LoveHero.useSpecial` — `Math.max(e.frozenTimer, 600)` returned `NaN` if `frozenTimer` was undefined, locking the enemy permanently. Now uses `Math.max(e.frozenTimer || 0, 600)`.
    - `TimeHero.update` — removed dead `Math.floor(Date.now()/750) % 1` (always 0) burnTick line; the actual tick already fires through `_ct3Tick`.
    - `GravityHero.update` — passive gravity pull wrote `saveData.global.gravity_pull_count++` per enemy per frame (60 writes/sec × N enemies). Now aggregates and writes once per second. Fixed missing `floatingTexts` typeof guard in the Quasar block (would throw if `floatingTexts` undefined).
    - `SmokeHero.useSpecial` — Blackout cap inconsistency (`cloudMax + 1` vs `cloudMax`); unified to `cloudMax`.
  - **Server / networking**:
    - `GameSession._giveXP` only granted **one** level even if `amount > maxXp` multiple times over (e.g. boss kill). Now drains queued XP in a `while` loop with a safety cap.
    - Pending action latches (`_pendingShoot/_pendingMelee/_pendingDash/_pendingSpecial`) were not cleared after `applyLevelUpChoice` resumed from the level-up modal — a held shoot/melee during the modal would auto-fire on resume. Now cleared on level-up resolution.
    - `server.js` `send(ws, msg)` had no try/catch on `ws.send()`; a closed/backpressured socket threw inside snapshot loops, breaking the snapshot mid-broadcast. Now wrapped with `terminate()` on failure.
  - **Cleanups & MED/LOW**:
    - Removed empty `if (msg.type === 'CONNECTED') {}` dead block in `Managers/NetworkManager.js`.
    - Removed dead `level_up`/`role: 'guest'` events branch in `OnlineTestBot.js` (server never emits this event type).
    - `UI/Statistics.js` "Least Played" hero now filters out `count === 0` heroes (fresh unlocks were always picked); only considers played heroes, falls back to "None".
    - `Utils.js` `shadeColor` modernized: `var` → `let`, `parseInt(float)` → `Math.round`, `==` → `===`, hex padding via `padStart`.
    - `Entities/Projectile.js` cached `Date.now()` once per `draw()` (used multiple times per frame across metal/black/time/love types).
    - `Entities/GoldDrop.js` removed no-op `replace('rgba','rgba')`.
    - DLC double-registration: `SpiritHero.init` and `ChanceHero.init` were re-assigning `window.HERO_LOGIC[type]` on every player spawn; bottom-of-file registration is the canonical site.
    - Faith of Fortune + Symphony of Sickness `index.js` had duplicate `audioManager.registerExclamationPath` calls.
    - `dlc/faith_of_fortune/Story.js` typos ("pursure" → "pursue", missing "but" in fortune_3); leading-space title in `dlc/champions_of_chaos/Story.js`.
    - `Managers/UIManager.js` removed `console.log("UI State:", …)` from production code path.
    - `UI/MainMenu.js` `renderHeroSelect` initialized missing per-hero `saveData` entries without persisting; now calls `saveGame()` once per render if any defaults were created.
- **Disciples of Deception biome zones rendered as flickering white boxes**: the Mindscape (`FRACTURE` zone) and Smog Quarter (`SMOG_POCKET` zones) added biome zones to `arena.biomeZones`, but `BiomeZone.draw()` had no case for those types — the canvas fell through to default fillStyle/strokeStyle and produced random flickering rectangles in the arena center. Fixed by adding an early-return case (matching the existing `TIME_RIFT`/`HEART_NEXUS` pattern) so the zone visuals are handled exclusively by each biome's own `draw()` method.
- **Gravity hero attack sound spam**: throttled `attack_gravity` SFX to minimum 250 ms between plays, matching Air hero behavior.
- **Online: stop-rubber-band on releasing move keys** — even with server speed matching client and reconciliation suspended during motion, releasing the move keys produced a small but noticeable jerk forward. Cause: when the player stops, the client stops its predicted position immediately, but the server is still applying the last received "move" input for ~RTT until the "stop" packet arrives — so the next snapshot's server position is briefly *past* the client. Reconciling against that overshot snapshot pulled the player forward = visible rubber-band on release. Fixed by tracking the move→idle transition and starting a 36-frame (~600 ms) `_postMoveGrace` window during which reconciliation is suspended, giving the server time to receive the stop input and settle. The idle reconciliation dead-zone was also widened from 4 px → 30 px and the pull strength softened from 10 % → 4 % per frame, so the rare correction that does fire after the grace expires is invisible. Players can now run, dash, and stop with no perceptible position adjustment.
- **Online: server player physics now matches client frame rate** — root cause of the residual movement / dash backtracking. Server tick ran `Player.update()` once per tick (30 Hz, one frame's worth of motion) while the client predicts at 60 fps. Client position drifted ahead by ~120 px/sec during sustained motion, eventually crossing the hard-snap threshold and yanking the player backward — particularly noticeable mid-dash and after a few seconds of running. Fixed by sub-stepping the server player update: `GameSession._tick()` now calls `p.update()` `Math.round(TICK_FRAMES)` times per tick (= 2× at 30 Hz), so server-simulated motion advances at 60 px-frames/sec to match the client. Action latches (`_pendingShoot/_pendingMelee/_pendingDash/_pendingSpecial`) are cleared on the first sub-step's `getInput()`, so abilities never double-fire; subsequent sub-steps only apply movement, friction, and cooldown decrements. With server and client speeds matched, divergence stays within RTT-induced jitter (~10–20 px on a typical link) instead of accumulating to hard-snap range.
- **Online: trust client prediction during active movement** — the previous reconciliation loop pulled the local player toward the server's authoritative position every frame, which fought intentional motion. Server simulation runs at 30 Hz (one physics step per tick) while the client predicts at 60 fps, so the client is structurally ~1 frame ahead of the latest server snapshot during continuous motion; reconciling produced a constant backward drag while running and a hard yank-back at the end of every dash. Reconciliation is now skipped entirely while `moveInput` is non-zero (or `isDashing` / dash grace is active). Convergence only runs while the player is idle (gentle 10 %/frame pull) or when the divergence is huge (>300 px → instant snap for teleports / death / extreme lag spikes). Net effect: full client-speed running and dashes, no more rubber-banding, while still correcting genuine mispredictions when the player isn't actively driving motion.
- **Online: projectiles dying mid-flight before reaching enemies** — when the server detected a projectile hit it removed the projectile from the next snapshot, and the client immediately dropped it from `projectiles[]`. Combined with the 100 ms interpolation delay this made projectiles visually pop out of existence before they reached the enemy on screen, especially noticeable with the auto-aim pickup. Fixed by carrying orphaned ghost projectiles forward across snapshot rebuilds: when a projectile id disappears from a new snapshot it's marked `_orphanAt` and kept in the array; the render loop interpolates it to its last buffered position, then drops it once `renderTime` passes that point. Projectiles now visibly complete their flight to the impact point and trigger the enemy hit-flash at the correct visual moment.
- **Online: internet-grade smoothness pass** — second wave of fixes after LAN testing still showed visible jumping. (1) Server tick rate bumped 20 Hz → 30 Hz (`TICK_MS = 33` in `server/simulation/constants.js`); smaller gaps between snapshots reduce coarseness of interpolation. (2) Player/enemy/projectile positions in the snapshot now use 1-decimal precision (`Math.round(x * 10) / 10`) instead of integer pixels — eliminates the integer-pixel quantization stutter at slow speeds without bloating bandwidth. (3) Snapshot interpolation buffer is now indexed by **server timestamp `s.t`** instead of client receipt time. Internet jitter caused 50 ms server intervals to stretch/compress to 30–80 ms in receipt time, making interpolation rate vary visibly; using server time keeps interpolation cadence in sync with actual world motion. (4) New `_onlineClockOffset` tracker locks to the minimum observed `clientNow − serverT` (least-delayed packet) and slowly drifts to follow clock skew; render time = `clientNow − offset − 100 ms` so renderTime advances smoothly each frame even between snapshots. (5) `_INTERP_DELAY_MS` raised 80 → 100 ms and snapshot ring buffer raised 4 → 6 entries to absorb wider internet jitter without falling back to extrapolation. (6) Own-player reconciliation moved from per-snapshot 8 % blend (which produced a visible 20 Hz wobble) to per-frame continuous pull at 6 %/frame (smooth at 60 fps); hard-snap threshold raised 80 px → 300 px so brief network hiccups, dashes, and knockbacks don't teleport the player. (7) Reconciliation suspended while `player.isDashing` and for an 18-frame grace window afterwards — a dash covers ~160 px in 10 frames and races a full server tick ahead, so reconciling mid-dash visibly yanked the player back to the server's lagging position. (8) `_onlineClockOffset` resets in `_resetGameState()` so a new session locks to a fresh server clock.
- **Online: smooth multiplayer rendering** — replaced direct snapshot-position application with a ring-buffer interpolation system (`_onlineInterpBuf`). Remote player, enemies, and projectiles are now rendered 100 ms behind the latest server time, interpolating between two buffered snapshots instead of jumping to the latest server position every tick. New ghost entities and single-snapshot startup still hard-snap as before; fallback extrapolation keeps rendering smooth while the buffer fills. Ghost enemies now show a brief white hit-flash when their HP drops between snapshots, giving immediate tactile feedback for projectile impacts without waiting for the server death event.
- **Online: Waker of Winds challenge system** — Air Hero wave-objective mechanic now works in online co-op. Server-side: `_checkWaveAdvance()` respects `world.objectiveLocked` set by `AirHero.update()`; `showNotification` is stubbed on the server world to relay objective alerts to both clients as `notification` events. Client-side: objective state is included in each player snapshot and applied on the guest so the HUD renders the correct progress; the guest skips local objective generation and boss-blocking (server is authoritative). Both host and guest see the same objective text and receive notifications for goal start, completion, and boss lock/unlock.
- **P2 HUD stuck on screen**: the bottom-right co-op HUD now hides immediately when a game ends (via `gameOver()` or `_resetGameState()`). Previously it remained visible after online co-op or local co-op games ended.
- **Online hero selection**: changing hero in the online lobby/pre-game screen now correctly applies to the local player's own character in-game. Previously `selectedHeroType` was not updated before `startGame()`, so the client still spawned the main-menu hero.
- **Online versus mode**: selecting versus mode now correctly suppresses enemy spawning and wave advancement on the server. Server simulation now handles PvP projectile and melee collision. First player to die loses; no enemy-wave revival between rounds.
- **Online versus: projectiles never hit after first damage** — `GameSession._damagePlayer()` set `player.isInvincible = true` but `Player.update()` only decrements `invincibleTimer` without clearing the boolean. After the first hit both players were permanently invincible. Fixed by clearing `isInvincible` in `_tick()` whenever `invincibleTimer` reaches zero.
- **Online: different biomes per player** — biome type and arena layout were chosen with independent `Math.random()` calls on each client. Fixed by (1) deriving a shared numeric seed from the lobby code, (2) using a deterministic hash of seed + wave number for biome selection, (3) temporarily replacing `Math.random` with a seeded Mulberry32 random generator around `arena.generate()` so both clients produce identical obstacle layouts; wave-1 biome now uses the host's hero type so both clients agree from the start.
- **Online: ghost partner wobbles when moving** — the ghost-player extrapolation multiplied raw input by a hardcoded `4 px/frame` without normalizing the direction vector; diagonal movement overshot by up to 41 % and snapped back at every 50 ms snapshot. Fixed by normalizing the input vector and using the actual hero speed (`stats.speed × speedMultiplier`). Local-player reconciliation dead-zone raised from 10 px to 25 px and blend reduced from 30 % to 8 % per snapshot to eliminate the 20 Hz oscillation caused by RTT drift.
- **Online arena flip / visual corruption**: ghost enemies created via delta snapshot encoding could lack a `radius` field, causing `createRadialGradient` to throw a non-finite error. The unhandled exception skipped `ctx.restore()`, leaving the canvas transform stack corrupted. Progressive corruption across frames caused the arena to visually flip and rotate. Fixed with: (1) early-out guard in `Enemy.draw()` before any canvas operations if `radius` is invalid, (2) safe default `radius`/`color`/`sides` on new ghost enemy objects, (3) `_bodyGradient` cache invalidation when the real radius arrives, (4) `??` instead of `||` in ghost enemy extrapolation (enemy at x=0 was falsy, causing velocity to compound each frame), (5) `ctx.setTransform(identity)` at the start of each game frame to reset any accumulated corruption.

### Changed
- **Online: "Play Again Online"** — after a game ends, clicking "Play Again Online" sends both players back to the shared lobby screen (hero & mode selection) without disconnecting. Server resets the lobby to `hero_select` phase and broadcasts to both. If a partner quit mid-game the button falls back to opening a fresh lobby. Server auto-cleans finished lobbies after 5 minutes.
- **Online lobby hero pre-selection** — the online lobby (both hosted games and global lobby) now pre-selects the hero chosen on the main menu instead of always defaulting to Fire.

- **Admin dashboard** (`/admin`): password-protected single-page web UI served by the game server. Shows active and completed game sessions (with live player HP bars, wave/score, enemy count, duration), per-session detail view (run stats: enemies killed, damage dealt/taken, missiles fired, melee hits, gold), currently online players (museum vs in-game), all registered players, top-20 leaderboard with hero/mode/outcome, and cloud save status per player (size, last-saved timestamp). Auto-refreshes every 5 seconds. Completed sessions tracked in-memory (last 100) across the server's lifetime. Set `ADMIN_PASSWORD` in `.env` to secure the endpoint (defaults to `admin`).

- **World-context refactor — Phase 8 (full enemy AI on server)**: `GameSession._updateEnemies()` replaced with direct `enemy.update()` calls. Server enemies now run the complete AI loop — GHOST alpha fading, SNIPER range-keeping, BOMBER self-destruct, TOXIC puddles, SHOOTER kiting, SUMMONER flanking + minion spawn, elite auras, weather modifiers, DLC ENEMY_LOGIC hooks. `loader.js` `getCoopTarget` fixed to be world-aware (`global._world?.player2 ?? global._world?.player`, with proper nearest-player selection by distance). SUMMONER minion spawn fixed to pass world (`new Enemy(true, 'BASIC', _w)`). Kill-reward deduplication added via `_killProcessed` flag + `_onEnemyKilled()` helper so BOMBER self-destructs and projectile hits cannot both award XP/gold. Contact damage and dead-enemy pruning extracted into `_applyEnemyContactDamage()` and the filter step in `_tick()`. All 80 parity assertions pass.
- **World-context refactor — cleanup (Phase 7 cont.)**: DLC sweep complete — all `window.enemies/projectiles/particles/floatingTexts` fallback reads eliminated from 7 DLC files (`PoisonHero`, `SoundHero`, `AirHero`, `EarthHero`, `LightningHero`, `SpiritHero`, `symphony/index.js`). Backwards-compat bridge (`window.enemies = enemies` etc.) removed from `game.js` (7 assignment sites across lines 1737-1740, 4176, 4202, 4886). CI lint step added to `.github/workflows/test.yml` — `grep` fails the build if any `window.(enemies|projectiles|particles|floatingTexts)` appears in `dlc/`, `Entities/`, `Player.js`, `Enemy.js`, or `EvilHeroes.js`. Enemy factory complete: `Enemy` constructor now accepts `world = null` as 3rd param, resolves all globals (`arena`, `saveData`, `wave`, `player`, `ENEMY_LOGIC`) via world fallbacks, and uses a player-relative spawn path on server (no camera needed). `WaveManager._createEnemy` delegates to `new Enemy(false, subType, world)` when `Enemy` is available, giving spawned enemies full `update()` / ENEMY_LOGIC / DLC support. All 80 parity assertions still pass.
- **World-context refactor — Phase 7 (parity test + CI)**: Added `server/simulation/parityTest.js` — 80-assertion test suite covering session isolation (no global state leakage between concurrent sessions), player movement direction, all 17 hero HP at init, enemy spawning, snapshot schema, delta-compression correctness, DLC hero smoke (all heroes × 5 ticks), and level-up flow (XP → LEVEL_UP message → choice → upgrade applied). Added `"test": "node server/simulation/parityTest.js"` to `package.json`. Added `.github/workflows/test.yml` — runs `npm test` on every push and pull request.
- **World-context refactor — Phase 6 (server adopts shared classes)**: The Node.js server now `require()`s real client game files and runs them inside a `World.createServerWorld()` context. `server/simulation/loader.js` bootstraps the Node.js environment with browser shims (`global.window = global`, no-op canvas context proxy, `document`/`Image` stubs, `saveData` Proxy, `SYMPHONY_STATE` stub) and loads all game classes (`Player`, `Enemy`, `Arena`, entity classes) plus all 13 DLC HERO_LOGIC registries. `server/simulation/NetworkInputController.js` bridges `player.moveInput` / `_pendingXxx` flags into the `Player.controller.getInput()` interface so `Player.update()` dispatches real movement and combat actions on the server. `GameSession.js` was fully rewritten: internal lightweight player/enemy structs replaced by real `Player` / `Enemy` instances; `Player.update()` called directly each tick for movement, DLC hooks, and one-shot actions; `_processMeleeAttacks()` applies `MeleeSwipe` damage; `_updateProjectiles()` handles both real-`Projectile` (`velocity.x/y`) and plain-object (`vx/vy`) formats. All 17 hero types (core + DLC) verified to initialize and tick without error in a Node.js smoke test.
- **World-context refactor — Phase 5 (game.js sync)**: `_world` now receives a complete state snapshot at game start and a per-frame refresh every tick in `masterLoop`. On start: `createExplosion`, `showNotification`, `audioManager`, `HERO_LOGIC`, `ENEMY_LOGIC`, `getDecoyTarget`, `frame`, `wave`, `score`, `currentWeather`, `currentObjective`, and `bossActive` are all wired into the world object. Each frame: `_world.frame/wave/score/gamePaused/isLevelingUp/isShopping/currentWeather/currentObjective/bossActive/enemies` are refreshed inside the `masterLoop` body. All bare `enemies = []` / `projectiles = []` reassignment sites in debug key-handlers and combat resolution now also re-point `window._world.enemies` / `.projectiles` immediately after the reset, preventing stale references.
- **World-context refactor — Phase 4 (DLC heroes)**: All 12 DLC hero files now receive `world` as the last parameter on every registered HERO_LOGIC hook (`init`, `update`, `shoot`, `melee`, `useSpecial`, `applyUpgrade`, and internal helpers called from those hooks). Each hook opens with `const _w = world ?? window._world; const { … } = _w ?? {};` so it reads from the world object when running server-side and falls back to `window._world` when called from legacy browser code. Bare global calls (`showNotification(…)`, `createExplosion(…)`) that had no `typeof` guard were converted to optional chaining (`showNotification?.(…)`). `window.frame` / `window.wave` / `window.enemies` references replaced with destructured locals. All 12 files pass `node --check`. Files migrated: `EvilHeroes.js`, `PoisonHero.js`, `SoundHero.js`, `AirHero.js`, `ChanceHero.js`, `SpiritHero.js`, `VoidHero.js`, `GravityHero.js`, `LightningHero.js`, `TimeHero.js`, `LoveHero.js`, `EarthHero.js`.
- **World-context refactor — Phase 3 (entity classes)**: Added `world = null` parameter and `this._world` binding to every entity constructor: `Projectile`, `MeleeSwipe`, `FloatingText`, `GoldDrop`, `Particle`. `MeleeSwipe.update()` now resolves the player reference via `this._world?.player` instead of a bare global. `HolyMask` is draw-only and required no changes. All updated files pass `node --check`.
- **World-context refactor — Phase 2 (`Player.js` + `Enemy.js`)**: All logic methods in `Player.js` (`update`, `shoot`, `melee`, `dash`, `onKill`, `takeDamage`, `useSpecial`, `addCombo`) now resolve globals via `const _w = this._world ?? window._world` and destructure from the world object instead of reading ambient globals. Keyboard-input block guarded for `keys === null` (server-safe). All `audioManager` calls use optional chaining (`audioManager?.play(…)`). `Enemy.update()` receives the same treatment — world passed to ENEMY_LOGIC hooks as a third argument. Every `new Player()` callsite in `game.js` now sets `player._world = window._world`. Both files remain fully browser-compatible via the `window._world` fallback.
- **World-context refactor — Phase 1 (bridge)**: Introduced `shared/world.js`, a UMD-compatible `World` class that holds all game-session state (entity arrays, players, flags, injectable side-effects). A fresh `World` is created at the start of every game run in `startGame()`; its arrays are aliased to the existing global arrays so old code and new `this._world`-reading code share the same objects. All array-reassignment sites (`_resetGameState`, `advanceWave`, `_onlineApplySnapshot`) now also update `window._world` to keep the world in sync. Subsequent phases will migrate each class to read from `this._world` instead of ambient globals.
- **Server-authoritative online multiplayer**: The dedicated server now runs the full game simulation at 20 Hz. Both clients send inputs and receive state snapshots from the server — neither player's machine acts as a host any more. This eliminates the host-advantage lag and makes the gameplay experience symmetric regardless of each player's upload speed or CPU.
  - New `server/simulation/GameSession.js` — 20 Hz server-side tick loop (player movement, projectile physics, enemy AI, wave spawning, collision, XP/level-up, gold drops).
  - New `server/simulation/WaveManager.js` — enemy spawning and wave progression logic.
  - New `server/simulation/constants.js` — Node.js-compatible hero stats and game constants.
  - `server/server.js` — creates a `GameSession` on `GAME_START`, routes `INPUT` messages directly to the session, and handles `LEVEL_UP_CHOICE` without relaying to a peer.
  - `game.js` — both clients are now symmetric guests (`isOnlineHost` permanently `false`); both flush input every frame; both apply server snapshots via the existing `_onlineApplySnapshot` path. Removed all dead `isOnlineHost` branches and the now-unused `_onlineSendSnapshot` function.
- **Client-side prediction (Phase 2)**: Own-player movement is now predicted locally every frame via the pre-existing `player.update()` path. Position reconciliation on each server snapshot uses a tiered strategy: drifts under 10 px are silently accepted, 10–80 px receive a smooth 30 % correction per snapshot to avoid visual teleports, and drifts over 80 px (knockback, revival) still hard-snap immediately.
- **Bandwidth optimisation (Phase 3)**: Snapshot payload reduced ~45 %. Static entity fields (`maxHp`, `subType`, `color`, `sides`, `radius` for enemies; `color`, `radius`, `isEnemy`, `isExplosive`, `isCrit` for projectiles) are now sent only on first appearance — subsequent snapshots carry only the dynamic fields that change every tick. Client merges delta updates with cached state. Projectile ghost objects are now reused across snapshots instead of being recreated, reducing GC pressure.
  - `Managers/NetworkManager.js` — `flushInput` now sends `INPUT` as a direct top-level message (no RELAY wrapper); server-pushed `SNAPSHOT`, `LEVEL_UP`, `PARTNER_LEVELING`, and `LEVEL_UP_DONE` are dispatched as first-class message types.
  - `UI/LevelUp.js` — level-up choice is sent directly to the server (`LEVEL_UP_CHOICE`) instead of being relayed to the peer.

### Changed
- **Online 2-Player: snapshot now sent by server, not by host client**: Snapshot frequency is now controlled by the server tick rate (20 Hz), with client-side extrapolation bridging frames at 60 Hz.

### Fixed
- **Online 2-Player: evil heroes (Green Goblin, Makuta) visible in online hero select**: These heroes are now always filtered out of the online pre-game hero grid, regardless of whether the player has unlocked Evil Mode.
- **Online 2-Player: Story mode shows each player's own hero story**: Both host and guest were independently running story events based on their own hero type, causing out-of-sync narratives. The guest now uses the host's hero type for story event lookups (`window._onlineStoryHero` set in `startOnlineGame`), so both players see the host's hero story.
- **Online Co-op: revive marker appears but game immediately ends**: After a revival marker was placed (setting `player.isDead = true` and `player.hp = 0`), the death check re-fired every frame because `hp` stayed at 0. On the second frame the co-op revival branch was skipped (`!player.isDead` was false) and the death-animation timer started, leading to game over 3 seconds later. Fixed by guarding the outer death check with `!player.isDead`; a separate "both players dead → game over" check now handles the case where both die simultaneously.
- **Online Co-op: ghost enemies appear permanently white on guest side**: When the guest's local projectiles hit ghost enemies (server-synced via snapshot), `enemy.hitFlashTimer = 6` was set on the ghost. Because ghost enemies skip `enemy.update()`, the timer never decremented and the white-flash overlay rendered every frame. Fixed by short-circuiting the collision block for ghost enemies — the projectile is consumed for visual feedback but no damage, flash, or score is applied.
- **Online 2-Player: Play Again after game over starts a new solo game**: Play Again now reopens the online lobby screen so players can rematch without re-entering lobby codes. The button label updates to "🌐 PLAY AGAIN ONLINE" when the session was online.
- **Online 2-Player: Quit Run during online game does not notify partner**: Quitting during an online game now calls `signalGameOver()` first, informing the partner (who will see their own game-over screen), then opens the online lobby for the quitting player.
- **Online 2-Player: Chaos mode available in online despite not being supported in co-op**: The "Chaos" mode button is now hidden in the online pre-game screen. Chaos Shuffle is not supported in any co-op mode (the local co-op gate at `startGame` already blocks it) and was silently broken online.

### Changed
- **Online Co-op: snapshot frequency doubled**: Host now sends a state snapshot every frame (60 fps) instead of every 2 frames (30 fps), halving the maximum guest-side entity latency.

### Added
- **Game Guide: Online Multiplayer tab**: Added a dedicated "Online Multiplayer" tab to the in-game manual (Game Guide) covering account setup, private lobby creation/joining, online versus gameplay, the Global Lobby social hub, emotes, proximity challenges, and auto-reconnect behavior.
- **Online 2-Player: pre-game hero & mode selection**: After both players confirm their hero in the invite-code lobby (or accept a Global Lobby challenge), the game no longer starts immediately. Instead, the shared **Versus/2-Player** hero-select screen opens (`versus-selection-screen`) in an online configuration: each player sees a hero grid where they can change **their own** hero (changes sync live to the partner via `HERO_SELECT`); a partner section shows the partner's current hero and name; the host picks one of four modes — **Standard**, **Story**, **Chaos**, or **Versus** (mode syncs live via `MODE_SELECT`/`MODE_UPDATE`); the host presses **START** to launch the match. Keyboard, mouse, and gamepad (D-Pad/A) are all supported. Guests are locked to row 0 (hero grid) on gamepad. The server handles the new `pre_game` lobby phase, `START_ONLINE_GAME`, and `MODE_SELECT` message types.

### Fixed
- **Museum & Global Lobby: gamepad not detected on Windows**: Both scenes hardcoded `navigator.getGamepads()[0]`, which on Windows can be a USB receiver or other HID device rather than the actual controller. All four Museum gamepad reads and the GlobalLobbyScene gamepad read now scan for the first device that passes `window.isRealGamepad()` (≥10 buttons, ≥2 axes, connected).
- **Online 2-Player: mode selection screen not appearing / wrong heroes**: `NetworkManager._dispatch()` did not handle the `PRE_GAME` message, so `nm.role` and `nm.lobbyCode` were never set for the Global Lobby invite path (unlike the invite-code path which sets them via `LOBBY_CREATED`/`LOBBY_JOINED`). With `nm.role = null`, both `nm.isHost()` and `nm.isGuest()` returned `false`, causing `startOnlineGame()` to assign heroes incorrectly (both players used `guestHero`). Fixed by adding `PRE_GAME` bookkeeping in `_dispatch()` — role is determined by comparing `msg.hostUsername` to the local account username; `lobbyCode` and `phase` are also set. The server now includes `lobbyCode` in every `PRE_GAME` message (both invite-code and Global Lobby paths).
- **Online 2-Player: guest always starts as 'water'**: `JOIN_LOBBY` on the server ignored `msg.hero` and left `lobby.guestHero` at the hardcoded `'water'` default. The guest's selected hero (sent with the `JOIN_LOBBY` message) is now applied to the lobby, so the hero displayed in the grid is the hero used in-game if the guest confirms without changing it.

### Changed
- **Online latency**: Global Lobby player-move updates throttle reduced from 100 ms to 50 ms (doubling update frequency); angle change threshold tightened from 0.09 to 0.05 rad for smoother remote-player rotation. In-game host snapshots increased from every 3 frames (~20 fps) to every 2 frames (~30 fps). Guest flushes input every frame (not every 2) for minimum host-side input lag. Ghost enemies, projectiles, and the host character (player 2 on the guest screen) are now forward-predicted between snapshots using velocity/move-direction, eliminating visible inter-snapshot teleportation on the guest side.

- **Online Co-op: guest triggers local death timer on death**: When the guest's player died, the host snapshot set `player.isDead = true` before the local co-op revival branch (which requires `!player.isDead`) could run, causing the guest to start a 3-second death timer and call `gameOver()`. Fixed by adding `!isOnlineGuest` to the death-timer guard so guests never start the local death countdown. Additionally, when the host revives the guest (`isDead` transitions `true → false` in a snapshot), `isPlayerDying` and `playerDeathTimer` are now reset on the guest. Revival markers (`p1RevivalMarker`, `p2RevivalMarker`) are included in every snapshot with perspective-swapped keys so the guest sees the correct revival progress indicator.
- **Global Lobby: gamepad controls**: `Museum.update()` was handling the Escape and B-button exit even when running inside the Global Lobby (`noInteraction: true`). This caused the B button to exit to the main menu immediately (bypassing the lobby-menu toggle) and left the museum canvas visible as a background when the HTML menu overlay appeared. Both handlers are now guarded with `!this.noInteraction` so `GlobalLobbyScene` manages its own exits, which also prevents the artifact of online-count and hero-selector button remnants persisting on screen.
- **VersusMenu: controller focus swallowed**: `inputLoop()` did not update `this.inputLoopId` with each new `requestAnimationFrame` call, so `close()` always cancelled a stale handle. When the menu was closed and reopened, two loops ran simultaneously, causing controller input to be processed twice per frame (swallowed or erratic focus). `inputLoopId` is now updated every iteration, and `open()` / `open2PVersus()` cancel any existing RAF before starting a fresh loop.

### Changed
- **Global Lobby: removed hero-selector HUD button**: The flame-emoji / `[TAB]` button that appeared in the top-right corner of the Global Lobby has been removed. The hero-selector overlay is still accessible via the **Tab** key (keyboard) or **Y** button (gamepad) — only the visual indicator is gone, decluttering the HUD.

### Fixed
- **Achievements menu: Echos of Eternity**: The 10 achievements for the Echos of Eternity DLC (`echo_` prefix) were falling into the "CORE" category because the `categoryFor()` function had no case for them. Added the missing prefix check and a new `DLC: ECHOS OF ETERNITY` section in the group order.

### Fixed
- **DLC menu back button**: Added `flex-shrink: 0` to the DLC list and back button so they are no longer compressed in the scrollable container. Renamed button label from "CLOSE" to "BACK" for consistency with other menus.
- **DLC menu controller support**: The gamepad B button now closes the DLC menu (added `DLC` state to the back-button handler). The `dlc-card` elements were already focusable; navigation and selection already worked.
- **DLC toggle dialog**: Replaced the native browser `alert()` with the custom styled `info-dialogue-screen`. The dialog is controller-navigable (A to confirm, B to dismiss), shows the DLC title and action, and returns to the DLC menu on close.
- **Info dialogue controller input**: Added `!lastGamepadState` guards to the A and B handlers in the `INFO_DIALOGUE` gamepad block. Without them, holding A to select a DLC card would cause the restart dialog to auto-dismiss once the debounce expired (~250 ms).

### Added
- **Global Lobby: controller-friendly emotes**: Shoulder buttons now trigger all 5 emotes on gamepad — LB=Wave, RB=Dance, LT=Laugh, RT=Cheer, X=Shrug. The emote bar and nearby-player prompt adapt their labels to show gamepad button names when a controller is active. The hero-change button shows `[Y]` instead of `[TAB]` for gamepad users.
- **Global Lobby: lobby menu (Start / B)**: Pressing Start or B (when no invite is pending) in the Global Lobby walk-around scene opens an overlay with Resume and Quit Lobby options, navigable with D-Pad / A / B. The scene continues running in the background so networking stays alive. The old behavior of B immediately exiting is removed.
- **Online dialogues: controller hints**: The Sign In modal, Server Config modal, and Online Lobby screen now show a `🎮 Navigate: D-Pad · Confirm: A · Cancel: B` hint line. The join-code input on the Online Lobby screen is now reachable via D-Pad navigation (it was previously excluded from the controller focus list).

### Fixed
- **Global Lobby duplicate session**: When the same account connects from a second device while already present in the Global Lobby, the server now terminates the stale connection and lets the new device take over cleanly (session takeover). Previously the second join was silently dropped, leaving the new device invisible to all other players.
- **Online lobby WebSocket connection**: When the server hostname was configured via Options → Server (which stores a bare hostname like `192.168.1.x`), the Online lobby passed it raw to `NetworkManager.connect()`, whose `replace(/^http/, 'ws')` found no match and produced a relative URL. Running from a `file://` page, the browser resolved this to `file:///...`, causing "file is not allowed" WebSocket errors. The lobby now routes through `CloudSaveManager._baseUrl()`, which already prepends `http://` and port 3001 for bare hostnames.

### Added
- **Global Lobby**: A new persistent social hub (secret levers are hidden in this mode) accessible from the Online 2-Player screen. All players currently online can walk around the museum map together, see each other's hero avatars with username labels, and interact. Includes 5 emotes (Wave, Dance, Laugh, Cheer, Shrug) triggered by keys 1–5 shown in a bottom HUD bar. Players near each other (within 80 px) see a challenge prompt; accepting auto-creates a private match using both players' current heroes. An in-scene hero selector (Tab / Y button) lets players change their hero and have others see the update in real time. Pressing Escape returns to the main menu. NPC heroes and memory shards are hidden in this mode; the run history billboard remains interactive.
- **Run history — online indicator**: Runs completed in Online 2-Player mode now show a 🌐 Online badge next to the mode label in the museum run history screen.
- **Global Leaderboard**: Server-side leaderboard (`scores` SQLite table, `POST /api/leaderboard`, `GET /api/leaderboard`). After every online run the client submits the score automatically. In the Global Lobby museum a second billboard (blue, left side of the gallery) shows the top 3 scores; pressing E / A opens a full-screen top-10 view with rank, username, hero, mode, score, wave, time, and outcome.
- **Online achievements**: 8 new achievements in the ONLINE category tracking online games played (Going Online → Online Legend) and max wave reached in Online 2-Player (Online Survivor → Online Champion).
- **Server config modal**: Options → Server & Account now shows a "CHANGE" button that opens a dedicated modal where only the hostname/IP is entered (port 3001 assumed). Includes a "Test Connection" button with a live health-check result. The raw URL is no longer shown in the options list.
- **Account badge repositioned**: "Logged in as \<username\>" badge moved to below the challenge buttons on the main menu.
- **Unified server account**: Server URL (shared between cloud saves and online play) is now its own field in Options → Server &amp; Account. Login/logout uses a single account for both features. Account status (● username / ○ Sign in) is shown directly on the main menu; clicking it opens the sign-in modal. Cloud Sync remains an independent toggle in Options, defaulting to on.
- **In-game player names**: When playing online, a name bar at the top of the screen shows both players' usernames (host in amber, guest in blue).
- **Online multiplayer (2-player)**: Host-authoritative relay architecture via the cloud save server's WebSocket endpoint. One player creates a lobby and shares the 6-character join code; the other player joins. Both players select their hero in the lobby screen. The host runs the full simulation and streams compact snapshots (~20 fps) to the guest; the guest's character is replicated as a ghost on the host side and vice versa. Level-ups are routed correctly: when the guest's character levels up the host relays the options, the guest chooses, and the host applies the upgrade to the ghost. Partner-leveling overlays notify each player when the other is choosing an upgrade. Automatic 90-second reconnect window if either player drops mid-game.
- **Cloud Save system**: New server component (`server/`) provides REST API-backed cloud save storage running on any Node.js host (e.g. Raspberry Pi). Players can register/login from the Options menu → Cloud Save section, then enable Cloud Sync to automatically upload saves after every save and download on game start. Conflict detection compares local and cloud saves; a modal lets the player choose which version to keep when both differ since the last sync.
- **Cloud Save info dialogue**: One-time menu announcement that explains the cloud save feature and directs players to Options → Cloud Save to set it up.

### Changed
- **Skill tree badge**: Replaced the plain red "!" badge with a gold gradient badge showing the actual unspent point count, a pulsing glow animation, and a subtle golden highlight on the button itself.
- **Museum — door widths**: Doors between top rooms (Fire, Water, Ice) and the gallery corridor are now 200 px (Fire/Ice) and 300 px (Water), centred within each room, replacing the previous 100 px / 600 px extremes.
- **Museum — Metal Room decorations**: Replaced floor-level gears with a compact clockwork cluster mounted on the north wall; removed the south-wall pipe; added four metal shields on the side walls and a crossed-sword rack on the south wall.

### Added
- **Museum — Forbidden Archive (basement)**: A hidden basement section below the Creature Wing accessible via a staircase. Entry is sealed until the player finds and activates three hidden switches scattered across the museum (fire room, ice room, gallery). Once all switches are activated the locked gate opens permanently. The Makuta and Green Goblin memory shards are housed exclusively here instead of the main gallery.

## [1.1.0] - 2026-04-25

### Added
- **Info dialogue system**: Generic queued info dialogue that appears on main menu launch; controller-compatible (navigate with stick/D-pad, A to activate, B to close); dismissable via "Don't show this again" checkbox persisted in config; plays menu music seamlessly; new dialogues can be registered via `infoDialogueManager.register()`
- **"Expansions Available" info dialogue**: First-launch popup pointing players to the Expansions section; can be permanently dismissed
- **Sound Hero — GRAND FINALE ultimate**: PERFORMER form now triggers a full musical assault: shots become a 5-way (7-way on-beat) sonic fan at 2.5×–3.5× damage; every beat auto-fires an 8-way omnidirectional note burst; orbiting musical notes deal close-range AoE damage every 30 frames; lasts 10 seconds
- **Gravity Hero — DARK STAR ultimate**: Gravity Hero now has the DARK STAR form: activation jolts all enemies inward; gravity pull range triples with 5× force; enemies within 80px take 15 DPS × damageMultiplier; spinning purple accretion disk ellipses render around the player; deactivates on damage (standard rule)
- **Poison Hero — PANDEMIC PROTOCOL ultimate**: PLAGUEBRINGER form now activates a true pandemic: all living enemies are instantly infected to 100 poison stacks on activation; turbo stack field applies +10 stacks/10 frames in radius 400; poison DoT ticks 3× faster (every 10 frames); plague explosions chain on poison kills; player leeches 5% of all poison DoT dealt; lasts 15 seconds
- **Air Hero — STORM INCARNATE ultimate**: ZEPHYR form fires all 4 compass attack styles simultaneously (scatter shotgun, sniper lance, rapid blades, vortex orb) per shot; hurricane ring damage boosted 7.5× (15 × damageMultiplier); activation triggers a 5-point wind burst; lasts 10 seconds
- **Lightning Hero — ABSOLUTE DISCHARGE ultimate**: FLASH form activates with an 8-way super lightning omni-burst; static charge generates 3× faster; every time charge fills to 100% an 8-way super omni-burst auto-fires; speed boost to 1.6× while active; lasts 10 seconds
- **Chance Hero — ALL IN ultimate**: JACKPOT form forces all dice to be explosive (always `allMatch`); explosive hits have a 25% chance to chain-bounce to the nearest enemy for 75% damage; slot machine auto-fires every second with a guaranteed JACKPOT outcome; 4 lucky symbols orbit the player; lasts 10 seconds
- **Spirit Hero — DIVINE RADIANCE ultimate**: Level-10 ENLIGHTENED form fully heals the player and fills Inner Peace; purification aura boosted to 40 × damageMultiplier every 10 frames at radius 250 (vs. 20 × mult / 15 frames / radius 150 from special); each aura pulse heals 0.5 HP; shots become Sacred Beams (pierce 10, 3× damage, 2× speed); Inner Peace drains at 0.2/frame for longer duration
- **Void Hero — SYSTEM COLLAPSE ultimate**: ENTROPY form instantly marks all living enemies as glitched; emits expanding corruption wave rings every 2 seconds (40 × damageMultiplier to all enemies within 300px); System Corruption AoE boosted to every 8 frames with 20% instant-delete for non-boss enemies below 50% HP; on form end, all remaining glitched enemies detonate for 200 × damageMultiplier; lasts 10 seconds
- **Poison visual on enemies**: Enemies with active poison stacks now show a green body tint, a glowing pulsing toxic ring, and rising bubble drips — all scaling with stack intensity (0–100) so lightly and heavily poisoned enemies are clearly distinguishable
- **Hit flash on enemies**: Enemies briefly flash white when hit (6-frame fade), giving instant visual confirmation on every shot and melee strike
- **Enemy death burst**: Regular enemies emit a small directional 8-particle burst in their own color on death
- **Hit stop**: Significant hits (melee: 2 frames, crit projectile: 3 frames, melee crit: 4 frames) freeze enemy and projectile movement for classic fighting-game impact feel
- **Combo milestone animation**: Reaching combo x10, x25, x50 or x100 triggers a scale-bounce and color pop on the combo counter
- **Low health heartbeat**: The health bar now persistently pulses at <25% HP (slow) and <10% HP (fast panic-rate); canvas vignette also activates earlier (25% vs 20%) with pulse speed scaling down with HP

### Fixed
- Weather effects now always reset at the start of each new wave instead of persisting into the next wave
- Gravity Hero no longer plays the shoot sound or fires a projectile while in DARK STAR ultimate form
- Memory shard pickups now display the correct color for every hero type (all DLC heroes — earth, lightning, gravity, void, spirit, chance, time, love, air — were previously defaulting to white)
- Time and Love heroes now render in their correct colors in the Museum (player avatar and entity sprites)
- Void Hero special icon now correctly shows 👻 (was showing default icon because the override applied after the first setup call)
- Void Hero ENTROPY ultimate now correctly marks all enemies as glitched and runs its 10-second form; the `chooseUpgrade` path was bypassing `HERO_LOGIC.applyUpgrade`, so the timer and glitch-marking logic never executed
- Void Hero biome obstacles now render with the cyan fractured-reality style instead of default stone (FracturedBiome was creating obstacles without a `biomeType`)
- Hero exclamations are now clearly audible over music — volume raised to 1.0 and background music ducks to 10% while a line plays, then restores
- DLC Expansions menu no longer plays the battle theme — `'DLC'` added to the menu state list in AudioManager

### Changed
- Developer logo intro screen now plays an animated video (`developer_animated.mp4`) instead of a static image
- Title background in the press-start screen now plays a looping animated video (`title_animated.mp4`) instead of a static image

## [1.0.3] — 2026-04-19

### Added
- Automatic update check on launch: fetches the latest GitHub release and shows a dialog if a newer version is available, with a direct download button

### Fixed
- Spirit Hero's charging sound loop no longer continues playing after pausing the game
- Boss defeated screen (story mode) is now controller-navigable on Windows Electron: gamepad detection now uses `isRealGamepad()` to skip USB receivers at index 0
- Boss defeated screen no longer persists over the story chapter screen after selecting Continue: canvas is cleared immediately on dismissal and the render loop early-exits when the screen is dismissed mid-frame

## [1.0.2] — 2026-04-19

### Added
- Electron file logging (`game.log`) is now toggled by a `LOGGING_ENABLED` constant in `index.js` (disabled by default)
- README now includes Windows build instructions; GitHub Actions workflow file renamed from `build-windows.yml` to `build.yml`

### Changed
- Standard boss enemies (Rhino, Speedster, Summoner, Nova, Hydra) now face the direction they are moving instead of spinning around their own axis
- DLC enabled-state is now stored in `dlcs.json` (Electron) / `localStorage` (web), mirroring the Config.js pattern; all DLCs are disabled by default on first run and must be explicitly enabled

### Fixed
- Entering any game mode with a controller connected no longer crashes: `PlayerController.js` now uses optional chaining (`?.pressed`) on all gamepad button accesses so gamepads with fewer than 10 buttons are handled safely
- Controller inputs in-game no longer go dead when a USB receiver occupies gamepad index 0: `HumanController` now uses `isRealGamepad()` to find the first real controller, consistent with menu navigation
- Completion menu no longer crashes (showing only the toggle and back button) on first run: `CompletionMenu.calculateProgress()` now uses optional chaining when reading `.prestige` for altar convergence nodes whose hero slots may not yet exist in `saveData`
- "Continue Run" no longer opens the shop when resuming a saved run on a wave that follows a shop wave (e.g. wave 5 after the wave-4 shop)

## [1.0.1] — 2026-04-17

### Added
- Story intro screen is now gamepad-compatible: pressing A skips it, matching the press-start screen behaviour
- Skip hint on story intro screen updated to mention A button
- Bug report overlay closes with gamepad B button (tracks `BUGREPORT` UI state)
- Manual closes with gamepad B button (fixed broken `closeTutorial` reference → `Manual.close()`)
- Evil Mode button and all other main-menu button types now show the yellow gamepad-focus ring when selected (added `.selected` overrides for `menu-mode-btn`, `menu-lib-btn`, `menu-challenge-btn`, `menu-continue-btn`, `menu-sys-btn`)
- Symphony of Sickness DLC: added Collector Cards for Toxic Crawler, Speedster, and Shadow Clone enemy types (`injectCards`)

### Fixed
- Story Mode button now shows yellow controller-selection outline when navigated to with a gamepad (was hidden by `border: none !important`)
- Windows installer now uses the correct app icon (`images/icons/win/icon.ico`) instead of the default Electron icon; icon path is now platform-aware in `forge.config.js`
- Installed app on Windows now correctly shows `5 Freunde: Elemental Arena` as product name instead of `5-freunde-arena`
- USB receivers and other non-controller HID devices are no longer mistaken for gamepads; `isRealGamepad()` filter (≥10 buttons, ≥2 axes) is applied in menu navigation, co-op assignment, and the `gamepadconnected` event handler
- Lightning Hero: `specialMaxCooldown` corrected from 15000 to 900 frames (was effectively a 4-minute cooldown instead of 15 seconds)
- Time Hero: `rangeCooldown` and `meleeCooldown` now respect `cooldownMultiplier` on assignment (cooldown reduction upgrades had no effect)
- Time Hero: CPU/co-op AI no longer returns `null`; now uses standard target-following behaviour
- Symphony of Sickness DLC: beat loop `setInterval` now clears any previous interval before creating a new one, preventing stacked ticks on reload
- Air Hero, Poison Hero, Sound Hero: special-icon DOM access is now guarded by `!player.isCPU`, preventing errors in CPU/co-op contexts

## [1.0.0] — 2026-04-17

### Added
- Completion menu: "Hide Completed" toggle button filters out 100% categories and sub-categories; controller-compatible
- Version number displayed subtly in the bottom-right of the main menu footer; single source of truth is `APP_VERSION` in `Constants.js`
- Boss-win audio exclamation plays for the winning hero when defeating an opponent in versus mode (all four KO code paths: AI-opponent projectile, AI-opponent melee, 2P projectile, 2P melee)
- GitHub Actions workflow
- Level-up sounds
- Bug report form
- SFX for all weather effects
- Themed obstacles per biome
- Earth hero impact sound
- Controller vibration and screen shake effects
- Evil Mode with villain exclamations and memory shards
- All hero exclamations (boss moments, wins, failures, level-up, found)
- Pickup SFX for all items
- Improved menu design
- Story-relevant achievement gating
- AI Companion mode
- Chaos Shuffle mode
- Weekly Challenge mode
- 2-Player Co-op and Versus modes
- DLCs: Rise of the Rock, Waker of Winds, Tournament of Thunder, Symphony of Sickness, Faith of Fortune, Champions of Chaos, Echos of Eternity
- Maze of Time (Echos of Eternity story dungeon)
- Skill Tree, Void Shop, Chaos Shop, Altar, Museum, Collection, Achievements, Statistics screens
- Daily Challenge mode
- Save import/export

### Changed
- Manual: expanded Game Modes section with entries for Versus Mode, Co-op Mode, Chaos Shuffle Mode, and Evil Mode

### Fixed
- Completion menu BACK button now appears directly below the content instead of being pushed to the bottom of the screen
- Completion menu now scrolls as a whole page (no inner scrollbar) and expanded categories scroll into view automatically
- Version number and copyright text no longer overlap in the main menu footer
