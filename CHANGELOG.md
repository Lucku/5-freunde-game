# Changelog

All notable changes to this project will be documented in this file, starting with version 1.0.0 and recent changes involved just before that release.

## [Unreleased]

### Fixed
- **Online: own projectiles cut off / only visible near the player.** Online co-op reused the local shared-screen camera (`updateCameraForTwo`), which centres on the *midpoint* of both players and zooms to fit them. Online players are on separate screens, so the local player sat off-centre and their own projectiles left the framed view on the side away from the partner (while still hitting enemies). Online now centres on the local player like single-player ([core/updateGameplayPre.js](core/updateGameplayPre.js)); local co-op keeps the fit-both camera.
- **Online projectiles clipped early.** The guest builds a full biomed arena *with* obstacles (seeded) but the server arena is flat, so the local projectile kill-pass killed server-driven *ghost* projectiles the instant they crossed a client-only obstacle/boundary — they vanished mid-flight then popped back in on the next snapshot. The kill-pass now skips ghosts entirely (`if (proj._ghost) continue;` in [core/updateGameplayMid.js](core/updateGameplayMid.js)); ghost lifetime stays owned by the snapshot rebuild + orphan-splice loop. Client-only render fix; server sim + parity untouched.

### Changed
- **Online netplay fluidity (tier 2).** Interpolation delay is now adaptive instead of a flat 100 ms: it tracks the measured inter-snapshot gap + jitter (EMA) and sizes the render-behind delay to ~2 gaps + jitter headroom, clamped 55–140 ms — a calm 60 Hz link now renders only ~55 ms behind while still buffering ≥2 snapshots ([game.js](game.js) `_currentInterpDelay()`). The locally-predicted own-shot TTL is likewise adaptive (`latency + interp delay + 40 ms`, 140–400 ms) so a player's own projectile no longer snaps backward when the server ghost takes over. Client-only.
- **#147 — replay-driven regression tests.** New [scripts/replay.js](scripts/replay.js) (`record`/`verify`) drives the authoritative `GameSession` from a seeded trace (`rngSeed` + `inputSeed` + heroes/mode/ticks) and asserts a structured end-state fingerprint against committed goldens ([scripts/replay-traces/](scripts/replay-traces/)); wired into `npm test` + `npm run test:replay`. `record` double-runs each scenario and refuses flaky (non-deterministic) goldens. It immediately caught a real online-coop determinism bug — `Player.js` shoot/melee rolled crit / projectile-spread / fire-explode via `Math.random()` (a gap in the #196/#197/#200 seeded-RNG arc) — now migrated to `runState.rng()`. ParityTest 127/127 unaffected.
- **#48 — cross-platform vector icons (no more emoji render drift).** Powerup world-icons (HEAL/MAXHP/SPEED/MULTI/AUTOAIM) are now drawn as procedural white vector shapes on canvas instead of `♥ ⚡ ⁙ 🎯` glyphs that rendered as mono/color/tofu inconsistently across Windows/Linux/Steam Deck ([core/systems/powerUpSystem.js](core/systems/powerUpSystem.js)). New [Icons.js](Icons.js) `iconHTML()` maps the CHAOS_REWARDS / UPGRADE_POOL emoji vocab to inline `currentColor` SVG (with raw-glyph fallback); wired into the level-up cards and chaos reward HUD. Cosmetic + client-only — parity untouched. New [tests/icons.test.js](tests/icons.test.js).

### Added
- **Online: off-screen partner indicator.** With online co-op now using a per-player camera, teammates can roam to opposite ends of the arena and lose sight of each other. A pulsing edge arrow (tinted with the partner's hero colour, friendly dot at its base to distinguish it from the hostile boss chevron) points toward the partner whenever they leave the viewport ([core/drawGameplayMid.js](core/drawGameplayMid.js)). Online-only — local shared-screen co-op already fits both players. Reuses the boss-arrow edge-projection math; client-only, no parity impact.
- **#41 — per-enemy-type death animations.** `createDeathBurst` now plays a distinct particle recipe per enemy subtype: BOMBER fizzle, GHOST dissolve, SHIELDER shatter, BRUTE heavy debris, TOXIC gas puff, SUMMONER arcane implosion, SPEEDSTER motion streak, SWARM cheap poof, GOBLIN gold sparkle (BASIC/SHOOTER/elites keep the generic burst). Cosmetic + client-only (server no-ops it; fires after kill rewards, zero parity impact); respects reduced-motion. See [game.js](game.js).

**New DLCs**
- **DLC: Radiance of Ruin** — three heroes (Light / Mask Integrity drain, Thorn / Blood Bond HP-cost, Dream / Lucidity gauge); three biomes (Reliquary, Crimson Greenhouse, Dreamspace); 10 achievements; full SFX + voice + 60 altar convergence entries; in-game manual entry. Unique projectile sprites (Light sun-mote, Thorn barbed shard, Dream Whisper).
- **DLC: Disciples of Deception** — three heroes (Psycho / Hysteria, Mirror / reflect + heal, Smoke / clouds + blind); three biomes (Mindscape, Hall of Mirrors, Smog Quarter); per-hero ultimates (DELIRIUM / REFRACTION / INK STORM); 13 cross-hero convergences; altar trees; level-up SFX; manual entry. Thematic projectile sprites.

**Online multiplayer**
- **Server-authoritative online multiplayer** — `server/simulation/GameSession.js` runs the full sim at 30 Hz (adaptive 30/20 Hz on entity load); both clients symmetric guests; `INPUT`/`SNAPSHOT`/`LEVEL_UP` first-class WS message types.
- **Online netplay rubber-band reduction (tier 1)**:
  - velocity extrapolation past last snapshot ([core/updateGameplayMid.js:160](core/updateGameplayMid.js#L160)); caps 150 ms enemies / 80 ms projectiles
  - tri-state adaptive snapshot rate 60/30/20 Hz on entity count (enter 60 < 50, hysteresis 80; SUB_STEPS scales so sim stays bit-identical)
  - adaptive self-recon lerp 300-500 px (`k = min(0.4, dist/800)`); hard snap > 500 px only
  - predicted projectiles survive snapshot wipe via `_predicted` tag + 140 ms TTL
- **Online seed handshake**: server generates `runSeed` at GAME_START, both clients install `runState.rng = mulberry32(seed)` for byte-identical enemy spawns, crit rolls, drops, boss attacks.
- **Online 2-Player core** — host-authoritative relay; 90 s reconnect; pre-game hero & mode select; controller D-pad+A; live `HERO_SELECT`/`MODE_SELECT`.
- **Reconnect grace window** — server holds 30 s after disconnect, sends `PARTNER_RECONNECTING { timeoutSec }` instead of immediate disconnect.
- **WebRTC voice chat** — `Managers/VoiceChatManager.js`; WS-relayed signaling (WEBRTC_OFFER/ANSWER/ICE); STUN-only.
- **Friends list** — `friendships` SQLite table; mutual-request auto-accept; REST CRUD.
- **Server world events** — `world_events` table; admin CRUD + dashboard tab; `Managers/WorldEventsManager.js` polls `/api/events` every 5 min; XP-boost multiplier applied client-side.
- **Online: story chapter sync** (continue waits for both) + **Maze of Time sync** (host interactive, guest spectator + `MAZE_NODE_SELECTED` relay).
- **Client-side prediction (phase 2)** — tiered reconciliation (< 10 px accept, 10-80 px 30 % blend, > 80 px snap).
- **Bandwidth optimisation (phase 3)** — static fields (maxHp/subType/color/sides/radius/isEnemy/isExplosive/isCrit) first-appearance only; client merges delta + reuses ghost objects.
- **Cubic-Hermite snapshot interp** (Catmull-Rom) + **MTU-aware snapshot chunking** (`chunk: {seq, idx, of}`) + **WebSocket permessage-deflate** (level 3, 256 B threshold).
- **Variable server tick** 30/20 Hz on entity count; per-tick `TICK_FRAMES` sub-stepping.
- **HTTPS / WSS** — `TLS_CERT_PATH`/`TLS_KEY_PATH` env-driven boot; client `serverUrl` auto-upgrades.
- **Online anti-cheat session tokens** — server-clamped score + `verified` column.
- **Rate limiting** — token-bucket per-IP on register/login/leaderboard/crash.
- **Lag compensation** — player projectile vs enemy collision radius +18 px on server (~66 ms positional cover).
- **Global Lobby** — persistent museum hub; 5 emotes; proximity challenge prompt; in-scene hero selector. Run-history online badge 🌐.
- **Global Leaderboard** — `scores` SQLite table; second museum billboard top-3; full-screen top-10.
- **Server config modal + unified server account** (Options → Server & Account). Account badge under challenge buttons.
- **In-game player-name bar** (host amber / guest blue).
- **Cloud Save** — REST API server, register/login, conflict modal decoded + summarised.
- **Online Sim mode in Testing Grounds** (auto-bot second player).
- **`test-arena.js`** standalone solo online-mode test.

**Map editor + workshop**
- **Map editor** — standalone `map-editor.html` canvas builder (obstacles/biome zones/traps with teleporter pair-linking). `Arena.generateFromMap(mapData)` reconstructs layout. Ships as separate executable (`MapEditor.{bat,sh}` → `--map-editor` flag). Wave config v2: count, enemies/wave, spawn rate decay, enemy pool, boss type.
- **Community Map Workshop** — `POST /api/maps`, GET/DELETE, like + leaderboard endpoints. `custom_maps` / `custom_map_scores` / `custom_map_likes` tables. 64 KB cap, 20 maps/user. New `CustomMapsPanel` + lobby `[M]` shortcut. Three seeded example maps (Volcanic Crucible / Cryo Gauntlet / Magnetic Coliseum).
- **Map workshop moderation** — `custom_maps` gains `hidden`/`rating_sum`/`rating_count`/`report_count`; new `custom_map_ratings` + `custom_map_reports`. Public `POST /api/maps/:id/rate` + `/report`; admin endpoints `GET /api/admin/maps`, `/maps/:id`, hide/unhide/delete, `GET/POST/DELETE /api/admin/map-reports`. Dashboard Maps tab with KPI cards + reports table.

**Game feel & visuals**
- **Boss intro cinematic** — 2.5 s pan + zoom + banner; gameplay frozen. Skip on re-encounters (per-save `bossesSeen`; ESC / B-button).
- **Modern post-processing stack** (WebGL fragment shader) — bloom, chromatic aberration, vignette, per-biome color grade; overlay `<canvas id="postFxCanvas">`; `postFX` setting; auto-disabled under reducedMotion.
- **Hero walk + fire animations** — `drawHeroSprite` gains `anim` param (`lean` shear, `fireRaise` nozzle translate).
- **Per-subType enemy death FX** — BOMBER fizzle, GHOST dissolve, SHIELDER shatter via `createDeathBurst(x, y, color, subType)`.
- **Loading screen redesign — Elemental Ring** — branded SVG ring + 5 element pips + rotating tip line + real progress driver wired through `DLCManager.init({ onProgress })`.
- **Configurable HUD layout** — `Managers/HUDLayout.js`; per-element `{left, top}` overrides; edit mode (mouse drag, keyboard tab/space/arrows, gamepad D-pad + LB/RB).
- **Music ducking / sidechain** — `_musicGain` node; `_duckMusicPulse` (one-shots) + `_duckMusicHold` (voice lines, refcount); curated trigger list.
- **End-of-run breakdown** — Damage by Source bars, Upgrades Picked timeline, Key Moments per wave.

**Modes & content**
- **Story Speedrun mode** — per-hero unlock on story completion; live `mm:ss.t` HUD; arc-boundary splits; `bestSpeedrunSec` saved; new ⏱ SPEEDRUN leaderboard tab; `POST /api/speedrun` (4 s/wave plausibility floor).
- **Museum Forbidden Archive (basement)** — three hidden switches unlock; Makuta + Green Goblin shards housed here.
- **Game Guide: Online Multiplayer tab**.
- **`LORE.md`** canonical lore compendium.

**Accessibility & QoL**
- **Accessibility pass** — remappable keys + gamepad buttons, hold-to-fire toggle, aim assist 0-100% lerp, high-contrast UI, font scale, screen-reader hints via `a11yAnnounce`, one-handed schemes (left/right hand presets), pause-on-tab-switch + controller-disconnect.
- **Pass A — high-leverage sweep**: 5-slot save backups + restore UI; in-game changelog modal (Markdown-rendered); per-channel audio sliders music/sfx/voice/ui; subtitle wire-through + ARIA mirror; hit-stop on boss kill (12 f); `ALLOWED_WS_ORIGINS` CSRF allowlist; `cachedRadial` gradient cache; F1 debug overlay; Particle + FloatingText pools.
- **Pass B — feel + recovery**: `SHAKE_PRESETS` taxonomy; crit float arc + flash; cheat console via tilde; pause-menu rework with run stats + last 24 upgrades + 12 cards; crash recovery via `5FreundeRunActive` sentinel; pause music lowpass via Web Audio; `recordPlayerDamage` → "Defeated by X" line.
- **Pass C** — `GAMEPLAY` constants block; `applyDamage(target, dmg, opts)` pipeline (5 cookie-cutter sites first, extended to 8 hero-specific paths); minimap canvas top-right; photo mode F2 live; `CONTRIBUTING.md` + `ARCHITECTURE.md`.
- **Reduced Motion** + **Colorblind correction modes** (SVG `<feColorMatrix>` daltonization).
- **Crash reporting** — `Managers/CrashReporter.js`, breadcrumbs, coalesced POST `/api/crash`; admin dashboard last 200.
- **Daily seed leaderboard tagging** + `mulberry32`-backed `getSeededRng()`.
- **Save schema versioning + migrations** — `SCHEMA_VERSION` + `MIGRATIONS` table + pre-migration `.bak`.

**Admin & analytics**
- **Admin dashboard** at `/admin` — `ADMIN_PASSWORD_HASH` (bcrypt + 8h JWT). Tabs: Sessions, Players, Leaderboard, Cloud Save, Balance, Analytics, Events, Technical, Maps.
- **Telemetry pipeline (opt-in)** — `Managers/TelemetryManager.js`; strict event + field whitelist, 60/min self-rate-limit, sendBeacon on pagehide. Server `telemetry_events` table + `POST /api/telemetry` + Analytics tab (per-hero runs/wins/winrate/drop-off curve, top upgrades + death causes).
- **Hero balance dashboard** — `GET /api/admin/hero-balance` + dashboard Balance tab (pick/win rate, avg wave/score, colour-coded).

**Technical & infrastructure**
- **Server-sim arc — bridge-driven `_tick`**: `_useBridge` flag (flipped default true, then legacy retired). Phases 3a-3h migrated projectile / melee / contact-damage / kill-reward / wave-advance authority to leaf modules. `parityTest.js` 80 → 127. `GameSession.js` 851 → 679 LOC. `WaveManager` retired. `server/simulation/constants.js` reduced to merge layer (sources `BASE_HERO_STATS` + `UPGRADE_POOL` from `Constants.js`).
- **Per-session `runState` Proxy** — `setActiveRunState(rs)` swap; `GameSession` owns `this._runState`; concurrent multi-match server now structurally possible.
- **Renderer `runState.rng` install at run start** (`startGame()`). Daily/weekly seed sources wired.
- **`Boss.js` `Math.random()` → `runState.rng()`** (14 sites; boss-fight determinism in online coop).
- **ECS migration** (phases 5.1-5.12): PowerUp, CardDrop, Particle, FloatingText, MemoryShard, GoldDrop, HolyMask, Companion, Projectile, Enemy migrated to typed-array slots + swap-with-last; Proxy-wrapped slot accessors keep `.update()`/`.draw()` working. MeleeSwipe + Boss intentionally kept-as-class (N≈3 / N=1-2).
- **RunState carve (8 phases)** — ~85 fields moved off game.js module scope onto `runState` container; entity arrays mutate-in-place; scalars + mode flags + story/objective/weather + combat/cinematic + players + revival + stats all migrated.
- **Update/draw split (10 phases, closed)** — `_updateGameplayPre` + `_updateGameplayMid` + `_drawGameplayMid` + `_drawGameplayPost` lifted into `core/*.js` leaf modules.
- **World-context refactor** (phases 1-8) — `shared/world.js` `World` class; per-class `_world` binding; `Player`/`Enemy`/Entities/12 DLC heroes/game.js migrated. Phase 8: full enemy AI runs server-side via direct `enemy.update()`.
- **ESM migration completion** — single `main.js` entry; `game.html` collapsed from 70 module scripts to one. Vite bundles single `dist/assets/main-<hash>.js`.
- **DLC dynamic-import migration** — `import.meta.glob('./*/*.js')` static manifest emits 59 code-split chunks; explicit imports across renderer + DLC; per-file `window.X = X` shims removed.
- **GameContext** (5 sessions) — singleton getter view, `_ownedDescriptor` ownership flip for `canvas`/`ctx`/`defaultSaveData`/`gameConfig`/`saveData`.
- **game.js split** — `Camera.js`, `Spawner.js`, `Wave.js`, `RunState.js`, `GameLoop.js` extracted.
- **`getHeroStats` memoization** — key includes prestige/unlocked/level/metaUpgrades/achievements.
- **Pass H step 1** — `Platform.js` Electron detection; AudioManager three-tier lazy preload; `structuredClone` sweep; `BiomeRegistry` with required/recommended-method validation.
- **Performance passes P1-P10**:
  - P1 off-camera particle cull; 30 Hz biome-zone cache; frame-budget HUD
  - P2 spatial-hash phase 2; enemy-projectile→player lifted out of inner loop; `_projectileSpatialHash` 128 px cells; `_SPATIAL_HASH_MIN = 30` small-N bypass
  - P3 Projectile/MeleeSwipe/GoldDrop pools (85 spawn sites; release-before-splice)
  - P4 gradient cache sweep (15 more sites)
  - P5 offscreen static obstacle layer with fingerprint invalidation
  - P6 `ensureDLCLoaded` memoization, hero-card hover prefetch
  - P7 particle sprite cache (8×8 offscreen per color, 64-entry LRU)
  - P8 Web Audio fast path for 12 hot SFX via `_sfxBuffers` Map
  - P9 snapshot position deltas; 30-tick keyframes prevent drift
  - P10 `_phaseTimes` ring buffer + `_recordPhase` / `_phaseP50/P99` for #24/#30 gating
- **TLS / HTTPS setup scripts** — `scripts/server-tls-{letsencrypt,selfsigned,renew}.sh`, `server-start.sh`, `server-systemd.service.example`, `server-setup.md`. Env-driven.
- **JSDoc schema typedefs** — `types/schemas.js` centralises `SaveData`/`HeroStats`/`GameConfig`/`WorldState`/`GameplayConstants`. `jsconfig.json` enables JS IntelliSense.
- **Server README** rewrite (TLS, ALLOWED_WS_ORIGINS, bcrypt admin auth, session tokens, map workshop, telemetry, world events, friends, voice signalling, snapshot delta + permessage-deflate, expanded message + REST tables).
- **ESLint flat config + Prettier + custom `no-foreach-splice` rule**. **Lint baseline cleanup** 371 → 95.
- **Vitest harness** — 38 → 90 tests across saveManager, mulberry32, spatialHash, hermiteInterp, plausibility, rateLimit, telemetryManager, adminAuth, entityPools. `server/anticheat.js` extracted.
- **GitHub Actions CI** + **`// Exposed for DLC` window-binding guard** — fails build on regression class that produced gray enemies.
- **Nightly headless harness** — `scripts/nightly-headless.js` drives N=100 authoritative `GameSession` sims with bot input; virtual `Date.now()`; p50/p95/p99 tick time + max load report. Daily 04:17 UTC workflow.
- **Hero smoke test suite (phase 1)** — `tests/smoke/heroAbilities.test.js` exercises every server-supported hero (8 base + 11 DLC + 2 evil = 21 keys) across four ability paths: init + idle, sustained shoot, sustained melee, forced special activation. Reuses `server/simulation/loader.js` browser shim so DLC `HERO_LOGIC` modules load in Node. 76 deterministic tests in ~470 ms. Shared testkit (`tests/smoke/_testkit.js`) exposes `createSmokeSession` / `tickN` / `pressSpecial` / `forceSpecialReady` for phases 2–5. New `npm run test:smoke` script for fast local iteration. Caught a real production bug on the first run — see "floatingTexts.push on undefined" fix below.
- **Enemy smoke test suite (phase 2)** — `tests/smoke/enemies.test.js` exercises every base enemy subtype from `Constants.js:19` (BASIC, SHOOTER, BRUTE, SPEEDSTER, SWARM, SUMMONER, GHOST, SNIPER, BOMBER, TOXIC, SHIELDER = 11) across two paths: idle spawn + lethal-damage death sweep, and sustained-fire absorb. Spawn via `new Enemy(false, subType, session._world)` mirrors `TestingGrounds.spawnEnemy` (TestingGrounds.js:234-236). 22 deterministic tests. Total smoke suite now 98 tests in ~370 ms. DLC enemy subtypes (GOLEM, BURROWER, HARPY, AERO_DRONE, CLOUD_MANTA, VOID_WALKER, GLITCH, ENTROPY_MAGE, CLOUD_BAT, STORM_ELEMENTAL, ZEUS_BOT) deferred — they live in DLC enemy modules the server loader doesn't import; covering them needs a separate phase 2.5 harness.
- **Boss smoke test suite (phase 3)** — `tests/smoke/bosses.test.js` exercises every boss type recognized by `Boss.js` (TANK, SPEEDSTER, SUMMONER, NOVA, RHINO, HYDRA from `BOSS_TYPES` + MAKUTA / GREEN_GOBLIN / DARK_GOLEM / ZEUS from the constructor's if/else branches = 10) across two paths: idle spawn → forced phase-2 transition (hp = maxHp × 0.4) → lethal-damage death, and sustained-fire absorb. Spawn via `new Boss(type)` mirrors `TestingGrounds.spawnBoss` (TestingGrounds.js:267-270). `spawnBossOfType` testkit helper also flips `runState.bossActive` so phase-2 gating (Boss.js:180) + scaled XP (updateGameplayMid.js:1395) fire. 20 deterministic tests. Total smoke suite now 118 tests in ~380 ms. DLC boss classes (`WindBosses`, `TimeBosses`, `ThunderBoss`) deferred to phase 3.5 — same coverage gap as DLC enemy subtypes. Driving this phase required a one-tick warmup in `createSmokeSession`: `Boss.js:32` reads bare-name `player` / `saveData`, which `server/simulation/RendererBridge.js:104` (`syncWorldToGlobals`) populates per-tick — pre-tick spawn crashed on `null.type`. Production gameplay never hits this because bosses spawn inside the gameplay loop where the sync already ran; the warmup tick is harness-side only.
- **Biome smoke test suite (phase 4)** — `tests/smoke/biomes.test.js` exercises 19 biome instances (6 base from `Biomes.js` + 13 DLC: ChaosBiome, FracturedBiome, MindscapeBiome, HallOfMirrorsBiome, SmogQuarterBiome, MadnessBiome, TempleBiome, CrimsonGreenhouseBiome, DreamspaceBiome, ReliquaryBiome, PoisonBiome, SoundBiome, CloudBiome) across two paths: registration-shape check (`generate()` must exist) and full lifecycle (`generate(arena)` → 30× `update(arena, player, enemies)` → `drawBackground(ctx, arena)` → `draw(ctx, arena)` → `drawObstacle(ctx, obs)`). Testkit's new `loadBiomes()` best-effort `require()`s each module; failures are logged but don't tank the suite. `getBiomeRoster()` dedupes by underlying class instance so aliased keys (`POISON_SWAMP` → `poison`, `SOUND_PLAINS` → `sound`, etc.) don't re-test the same biome twice. 39 deterministic tests. Total smoke suite now **157 tests in ~410 ms**. Surfaced biome-API drift (not bugs, but undocumented inconsistencies): the canonical `Arena.js:119` call site passes `arena` to `generate()`, but base biomes' signature is `generate()` while DLC biomes are `generate(arena)`; most biomes use `update(arena, player)` but CloudBiome + PoisonBiome use `update(arena, player, enemies)`; CloudBiome uses all-static methods + registers the class itself (not an instance) into `BIOME_LOGIC['lightning']`. Smoke now passes the 3-arg / arena-aware form so all variants exercise. **Coverage gap:** 4 biomes that only register via their pack's `index.js` bootstrap (WindBiome via `dlc/waker_of_winds`, TimeBiome via `dlc/echos_of_eternity` (also aliased `eternity`), LoveBiome (also aliased `heart`), RockBiome via `dlc/rise_of_the_rock` (also aliased `earth`)) deferred to phase 4.5 — they require running each pack's bootstrap, which depends on browser-only setup the loader doesn't fully stub.
- **Static manifest smoke (phase 5)** — `tests/smoke/manifest.test.js` is a pure FS scan (no runtime, no harness load). Four lint sets: (1) every DLC hero key registered via `BASE_HERO_STATS['key'] = {…}` must have a matching `KeyHero.js` file under its DLC pack; (2) every `new Audio('PATH')` string-literal callsite must point to a file that exists on disk; (3) every relative `.src = 'PATH'` assignment (paths starting with `audio/`, `images/`, or `dlc/`) must point to a file that exists; (4) server vs browser `BASE_HERO_STATS` drift — orphan check (server-only heroes the browser doesn't have = hard fail) plus baseline-snapshot of the 6 browser-only DLC heroes the server constants table intentionally doesn't enumerate (`smoke`, `psycho`, `mirror`, `thorn`, `dream`, `light`). Walks every `.js` / `.mjs` under the repo root (excluding `node_modules`, `dist`, `out`, `.git`, `tests`); dedupes paths so a heavily-referenced sprite or sound runs as one test, not N. **104 deterministic tests**. Total smoke suite now **261 tests in ~480 ms**. No new findings — every asset literal in the codebase resolves to a real file, the documented browser-only hero baseline is intact.
- **`better-sqlite3` 9.4.3 → 12.10.0** — Node 24 prebuilts.
- **`window.GAME_API`** — formal DLC integration namespace with getter/setter pairs and `version`.

### Changed
- **#174 — DLC stub layer + contract validator.** New [dlc/dlcContracts.js](dlc/dlcContracts.js): `registry(name)` is one env-agnostic accessor (resolves the shared registry on `window` | `global` | `globalThis`, lazy-inits, returns the live reference) — the canonical surface for the mutable dual-env registries (`HERO_LOGIC`/`BIOME_LOGIC`/…) that DLC hero files write into and the server `require()`s directly. `validateDLCContract(id, dlc)` asserts every hero a manifest declares (`dlc.heroes`) was actually registered into `HERO_LOGIC`; [DLCManager](dlc/DLCManager.js) runs it after each activation and logs violations loudly. Unblocks the eventual 53-read sweep (left as follow-up). New [tests/dlcContracts.test.js](tests/dlcContracts.test.js).
- **#175 — per-DLC version stamps.** Every DLC manifest declares `dlcVersion: 1`; `saveData.dlcVersions` records `{ '<dlcId>': version }`. New `SaveManager.applyDLCVersion` + `DLC_MIGRATIONS` framework runs per-DLC migrations between the recorded and current version (lets DLC schema changes live per-DLC instead of the monolithic top-level table). DLCManager stamps on activation (guarded). New [tests/dlcVersion.test.js](tests/dlcVersion.test.js).
- **#8 — DLC declarative manifest + auto-loader** (refactor, no behavior change). New `DLCManager._activateDLC` drives each DLC's bring-up from two manifest fields — `scripts: [...]` (dependency files, loaded in order) + `inject: [...]` (ordered hook suffixes → `inject<Name>()` calls) — instead of the per-DLC `loadScript` chain + `this.injectX()` sequence boilerplate. 8 of 9 DLCs converted ([dlc/champions_of_chaos](dlc/champions_of_chaos/index.js), [disciples_of_deception](dlc/disciples_of_deception/index.js), [echos_of_eternity](dlc/echos_of_eternity/index.js), [faith_of_fortune](dlc/faith_of_fortune/index.js), [radiance_of_ruin](dlc/radiance_of_ruin/index.js), [rise_of_the_rock](dlc/rise_of_the_rock/index.js), [tournament_of_thunder](dlc/tournament_of_thunder/index.js), [waker_of_winds](dlc/waker_of_winds/index.js)); each DLC's inline audio-registration block (which holds closures) moved verbatim into an `injectAudio()` method. `symphony_of_sickness` stays on the legacy `load()` escape hatch (non-uniform bring-up) — the loader falls back to `load()` when no `scripts` array is present. Net −119 lines of boilerplate. New [tests/dlcManifest.test.js](tests/dlcManifest.test.js): loader unit tests + a static contract test validating every DLC manifest against its file. Vitest 388, ParityTest 127/127, build + lint green.
- **#171 Phase 2 — retired 12 `window.*` back-compat class shims** (internal refactor, no behavior change). `Projectile`, `Particle`, `FloatingText`, `HumanController`, `AIController`, `CloudSaveManager`, `HUDLayout`, `TelemetryManager`, `MapManager`, `CustomMapsPanel`, `WorkshopPanel`, `SaveManager` are now reached via explicit ES imports instead of bare-name global lookup; the renderer→leaf-module bridge lines and the "Expose Classes for DLC" block in [game.js](game.js) dropped the three entity-class assignments. Consumers gained imports ([core/updateGameplayMid.js](core/updateGameplayMid.js), [core/updateGameplayPre.js](core/updateGameplayPre.js), [Player.js](Player.js), [EvilMode.js](EvilMode.js), [UI/Options.js](UI/Options.js), [UI/OnlineLobby.js](UI/OnlineLobby.js), [Museum.js](Museum.js), [UI/CustomMapsPanel.js](UI/CustomMapsPanel.js), [GlobalLobbyScene.js](GlobalLobbyScene.js), plus DLC/script callers). Entity-class exports verified bit-identical to the former shim values, so browser + server (loader `global.X`) bindings are unchanged. ParityTest 127/127, build + Vitest green, lint flat at baseline. Intentional remainders kept: `window.STORY_EVENTS`/`chaosState`/`bumpDamageSource`, entity-array sentinels, lowercase singletons, and inline-HTML handler bindings.
- **Backlog tracking** — flipped stale [#11](tasks/improvements-2026-05-10.md) (539 globals → RunState) to done; carve shipped across the RunState arc (#195/#196). Added `tasks/autonomous-specs-2026-05-29.md` with autonomous-agent specs for #171 Phase 2 (shim retirement), #8 (DLC manifest), #174 (DLC stub layer), #175 (DLC version stamps), #6 (Actor base).
- **Mindscape biome center motif** — replaced 24-spoke mandala + rings with EEG brainwave traces + drifting mind glyphs (?, !, Ψ, ∞, ◊, ∴, ※, ⊗); Hysteria doubles activity.
- **Smog Quarter + Hall of Mirrors biomes** — multi-layer composites (sodium gradient + grate + soot + fog + searchlights / radial marble + diamond tile shimmer + fracture clusters + concentric ripples + prismatic rays + echo silhouettes).
- **Reliquary + Crimson Greenhouse biomes** — chandeliers, candles, banners, eye medallions, sunbursts, mask shrines, rose window, gold embers / hanging vines, climbing edge-vines, rose buds, briar patches, additional leaf-fall.
- **Dreamspace** — added constellation lines, crescent moons, nebula blooms, drifting "Z" puffs; clock-face shards removed (read as Time biome copy).
- **Light hero character-select tile** — metallic gold gradient with clipped text fill.
- **Trap visual redesign** (all six types: gradient + glow + layered geometry).
- **SLOW trap** now appears from wave 3.
- **Main menu "Online 2-Player" → "Online"**.
- **Skill tree badge** — gold gradient + pulsing glow + unspent count.
- **Museum door widths** — 200 / 300 / 200 px instead of 100 / 600 / 100.
- **Museum Metal Room** — north-wall clockwork cluster + side shields + crossed swords; south pipe removed.
- **Audio Music/Sound-FX ON/OFF toggles removed** (0 % slider covers off).
- **Online Co-op: snapshot frequency 30 → 60 fps** (host snapshots; guest input every frame).
- **Online: trust client prediction during active movement** (no recon while `moveInput` non-zero or dashing); idle dead-zone 4 → 30 px, pull 10 → 4 %.
- **Online: 1-decimal-precision snapshot positions; interp indexed by `s.t`; `_onlineClockOffset` skew tracker; `_INTERP_DELAY_MS` 80 → 100; ring buffer 4 → 6**.
- **Online: server player sub-stepped** `Math.round(TICK_FRAMES)` per tick (2× at 30 Hz) so server motion matches 60 fps client.
- **Online: 36-frame `_postMoveGrace` after move→idle** suspends recon while server settles stop input.
- **Online: hero pre-selection** uses main-menu hero (was always Fire).
- **Online "Play Again Online"** returns both to shared lobby; auto-clean lobby after 5 min.
- **DLC: Disciples of Deception — Smoke hero BLACKOUT special recolored** ([dlc/disciples_of_deception/SmokeHero.js](dlc/disciples_of_deception/SmokeHero.js)): cast burst, cloud gradient, swirl rings, shockwave, and per-enemy blind marker swapped from purple (`#9a6bd6`, `rgba(180,120,230,...)`, `rgba(200,150,255,...)`) to smoke palette (charcoal core `rgba(20,20,26)` → slate `rgba(70,70,82)` → pale ash wisps `rgba(210,210,220)`). Matches hero base color `#5a5a6e` and the dense-fog theme; no gameplay change.
- **DLC: Waker of Winds — Cloud Golem overhaul** ([dlc/waker_of_winds/WindBosses.js](dlc/waker_of_winds/WindBosses.js)): stone-core eye slits now rotate to face the player (was locked north → looked 180° backward when player approached from south). Combat hardened: base speed 0.6→0.85; hail cooldown 240→180f (12-bolt rings in phase 2); new **aimed hail volley** (3-shot tracking spread every ~3.3 s); new telegraphed **Storm Called** lightning AoE at player position (1 strike phase 1 every 6 s, 3-strike fork phase 2 every ~3.7 s, 60 f windup, ~150 r damage); new **Charging** lunge (60 f telegraph then 40 f dash at 6× speed, contact deals 1.1× damage + heavy knockback) on a 7 s cooldown. Phase 2 (auto at 50 % HP) surges speed ×1.4, halves every cooldown, and spawns 2 minions on transition.

### Fixed

- **Museum / Global Lobby showed a frozen frame of the last arena after quitting a run**. The WebGL post-processing overlay `<canvas id="postFxCanvas">` ([core/postProcess.js:137-139](core/postProcess.js#L137-L139)) sits at `z-index:2` over `gameCanvas` and only re-uploads its texture (or hides itself) inside `renderPostFX()`, which is called solely from `_runGameplayFrame()` ([game.js:6638](game.js#L6638)). The museum + lobby scenes early-return in `masterFrame` ([game.js:6657-6661](game.js#L6657-L6661)) before that call, so after any run the overlay stayed visible holding the last gameplay texture and covered the scene drawn to `gameCanvas` underneath. At the main menu it was masked by `#menu-bg-canvas` (z-index 19), so the stale overlay only surfaced in the museum/lobby. Same class of bug as the boss-defeat freeze below. **Fix:** export `hidePostFX()` from postProcess.js and call it in the museum + lobby early-return branches so the overlay is hidden whenever a standalone scene owns the frame.

**Windows Electron playtest (2026-05-28)**
- **Music broke permanently after first level-up or boss appearance** in packaged build. Root cause: [Managers/AudioManager.js:303-326](Managers/AudioManager.js#L303-L326) `_ensureMusicFilter()` only created `_musicFilter` + `_musicGain` when `_audioCtx` was null. But `_ensureAudioCtx()` (called by SFX prewarm at boot via [Managers/AudioManager.js:241](Managers/AudioManager.js#L241) `_loadSFXBuffer`) had already populated `_audioCtx` without creating the filter graph, so on the first `_duckMusicPulse` → `_routeActiveMusic` → `_ensureMusicFilter` call the filter init was skipped, then `source.connect(this._musicFilter)` ran with `_musicFilter === null` and threw. `createMediaElementSource(track)` had already rerouted the audio element away from the default destination, so the music went silent permanently with no recovery. Triggered by every level-up (`level_up_<hero>` SFX matches `DUCK_TRIGGER_PREFIX`) and every boss/voice cue (`playHeroExclamation` → `_duckMusicHold`). **Fix:** split the init so `_musicFilter` + `_musicGain` are created whenever they're missing, independent of `_audioCtx` state.
- **Edit HUD unreachable with controller — focus stuck in the (hidden) Options menu behind**. [Managers/HUDLayout.js:294](Managers/HUDLayout.js#L294) `_pollGamepad()` picked the first gamepad whose `.connected` was true; on Windows a phantom XInput/USB-receiver slot at index 0 satisfies that without exposing real buttons, so polling latched onto the dead pad and the user's real controller (higher index) was ignored. `handleGamepadMenu()` in [game.js:676-678](game.js#L676-L678) already filters via `window.isRealGamepad()` for exactly this reason. **Fix:** mirror the same filter in `_pollGamepad`.
- **Global Lobby (and Museum) opened to a blank canvas — only the main menu particle background was visible**. [Museum.js:2415-2429](Museum.js#L2415-L2429) `openMuseum()` / `openGlobalLobby()` guarded the menu-background teardown with `if (typeof MenuBackground !== 'undefined') MenuBackground.stop();`, but Museum.js never imported `MenuBackground` and the `window.MenuBackground` shim was retired in #171 phase 2 ([UI/MenuBackground.js:173-174](UI/MenuBackground.js#L173-L174)). `typeof MenuBackground` therefore evaluated to `"undefined"`, the guard short-circuited, and `#menu-bg-canvas` (z-index 19) stayed visible covering the `gameCanvas` (default stacking). `_renderGlobalLobbyScene()` was happily drawing the lobby underneath the whole time. **Fix:** add the explicit `import { MenuBackground } from './UI/MenuBackground.js';` and drop both `typeof` guards.

**Packaging / installer**
- **Installed Windows build showed base-game title image on every DLC story chapter** instead of the per-DLC banner. Root cause in the build pipeline: [vite.config.mjs](vite.config.mjs) only mirrored `audio/` and `images/` into `dist/`, and [forge.config.js](forge.config.js) `PACKAGE_INCLUDE` whitelists only `dist/`, so `dlc/*/images/` and `dlc/*/audio/` were absent from the asar. DLC title-image paths (`dlc/<id>/images/title.png`, mapped in [game.js:3740-3759](game.js#L3740-L3759) on `window.STORY_TITLE_IMAGES`) 404'd in the packaged build, then [game.js:3762](game.js#L3762) `bgImgEl.onerror` swapped `src` to the base-game `images/title.png` — exactly the reported symptom. DLC audio was silently broken by the same gap. **Fix:** vite's `closeBundle` step now walks `dlc/<id>/` and mirrors each DLC's `images/` and `audio/` subdirs (non-JS only — `.js` files are already bundled via `import.meta.glob` and duplicating them at raw paths would ship stale code) into `dist/dlc/<id>/<sub>/`, so the existing `PACKAGE_INCLUDE: ['dist', …]` whitelist now picks them up automatically.
- **Packaged build crashed every frame with `fs.existsSync is not a function` / `fs.writeFileSync is not a function`** — saves never persisted, GameLoop swallowed the exception per frame, and Config writes were silently lost. Root cause: Vite 8 uses rolldown, which (unlike `@rollup/plugin-commonjs` under Vite 7) pattern-matches a literal `require('fs')` / `require('path')` in source and replaces it with its own bundled CJS interop stub (`hr=(qn(),Ve(We))` in the minified output, where `We` is a synthetic namespace whose `existsSync` / `writeFileSync` getters all return `undefined`). `vite-plugin-electron-renderer`'s alias only intercepts ESM `import` resolution for Node builtins — it never sees the CJS-style `require()` calls in [Platform.js](Platform.js) and [dlc/DLCManager.js](dlc/DLCManager.js), so those went through rolldown's stub path. Before the previous fix the installer shipped raw `Platform.js` (no bundler) and the bug was masked; once `dist/` became the canonical asar entry it was permanently exposed. **Fix:** routed both files through an `_electronRequire = globalThis.require` indirection — bundler only rewrites literal `require('string')` calls, so the variable-aliased call is left intact and resolves at runtime to the renderer's real Node `require` (available because `nodeIntegration: true` is set on the BrowserWindow in [index.js](index.js)). Verified by rebuilding and grepping the bundle: zero remaining `(qn(),Ve(We))` interop wrappers for fs; six `('fs')` / `('path')` string args now sit as plain calls on the `_electronRequire` variable.
- **Installed Windows build hung at 88% on the loading screen** (background tips + ring animations kept ticking; gameplay never started). Root cause: `electron-forge make` never ran `vite build`, so `dist/` was missing from the asar. `index.js` then took its legacy fallback branch (`win.loadFile('game.html')`) and loaded raw source. Raw `dlc/DLCManager.js:9` reads `const DLC_MODULES = import.meta.glob('./*/*.js')`, which only exists as a Vite build-time transform — at runtime it threw `Uncaught TypeError: (intermediate value).glob is not a function`, taking out the entire DLC init promise. Save loading still completed (→ 0.20), but the DLC progress signal never advanced, so the loading ring sat at the 0.88 idle-creep cap defined in [game.html:464](game.html#L464); the tip carousel + particle drift live on independent `setInterval`s so they kept animating, hiding the stall. **Fix (two pieces):** [forge.config.js](forge.config.js) gained a `hooks.generateAssets` that always runs `npm run build` before packaging, plus an `ignore` whitelist that restricts asar contents to `dist/`, `index.js`, `package.json`, `icon.png`, `images/icons/`, and `node_modules/` — raw `game.html` / `main.js` / source `.js` are now physically excluded from shipped builds. [index.js:38-50](index.js#L38-L50) dropped the third "fallback to raw game.html" branch entirely; if `dist/game.html` is missing the main process now throws `dist/game.html missing — run \`npm run build\` before launching the packaged app.` instead of silently loading untransformed source.

**User-facing**
- **Tutorial mode completely broken — no objective HUD, no awakening narrative chapters, behaved like Standard mode**. `TutorialMode._resetForStage()` ([TutorialMode.js:159-168](TutorialMode.js#L159-L168)) cleared per-stage entity arrays with `window.particles.length = 0` and `window.floatingTexts.length = 0`. After commits `d0096b1` (Particle → ECS shim) and `4577b29` (FloatingText → ECS shim) those window globals are plain objects with a getter-only `length` property — assigning to it under ESM strict mode throws `TypeError: Cannot set property length of #<Object> which has only a getter`. The throw aborted `_resetForStage()` before it reached `openStory({fromTutorial:true})`, so the awakening chapter modal never opened, `TutorialMode.objective` stayed null, and gameplay continued as a normal wave (no HUD, no scripted boss). **Fix:** import `clearParticles` / `clearFloatingTexts` from [core/systems/particleSystem.js](core/systems/particleSystem.js) / [core/systems/floatingTextSystem.js](core/systems/floatingTextSystem.js) and call them with `runState` instead of poking the shim's read-only `length`. `window.enemies.length = 0` and `window.projectiles.length = 0` keep working — those sentinels are `Proxy` objects with a `set` trap that maps `length=0` to their respective `clearEnemies` / `clearProjectiles` typed-array resets.
- **Air hero "Wind Shifts X!" notification rendered as a huge screen-dominating banner instead of a normal floating text**. [dlc/waker_of_winds/AirHero.js:189](dlc/waker_of_winds/AirHero.js#L189) always passed `size: 120` to the FloatingText spawn, but the call was originally `new FloatingText(...)` — a dangling constructor that never pushed into `floatingTexts`, so the wave-start feedback was silently invisible. Commit `8fd84fc` (May 12) mechanically renamed the call to `FloatingText.acquire(...)` as part of a wider sweep, and commit `4577b29` (May 18, "FloatingText to ECS via compat shim") then made `acquire()` itself self-spawn into the ECS render arrays — at which point the always-120 size suddenly became visible. Size 120 also exceeds the `size >= 25` crit threshold in [core/systems/floatingTextSystem.js:46](core/systems/floatingTextSystem.js#L46), triggering the crit scale-pulse (~1.6×) and the white-flash strobe on top of the already-massive base font. **Fix:** dropped size from `120` to `24`, matching peer wave/state notifications (PHASE 2! = 30, GOING MAD! = 28, SHATTER! = 14) and staying below the crit threshold so no pulse/flash either.
- **Boss-defeat screen never appeared — game looked frozen after every boss kill** (pause menu still worked, no console error). `_runGameplayFrame` ([game.js:6605](game.js#L6605)) early-returned as soon as `_updateGameplayPre` reported a cinematic was owning the frame (`if (_updateGameplayPre(dt)) return;`), which skipped `renderPostFX()`. The post-fx WebGL overlay `<canvas id="postFxCanvas">` ([core/postProcess.js:134](core/postProcess.js#L134)) sits at `position:absolute; z-index:2; pointer-events:none` over the main canvas and only re-uploads its texture inside `renderPostFX()`. With that call skipped for every cinematic frame, the overlay kept showing the last pre-boss-kill texture forever; the boss-death cinematic, boss-defeat choice screen and boss-intro cinematic were all drawing to the source canvas underneath but were hidden by the stale overlay. The pause menu still opened because it's a DOM element with higher z-index. Fix: split the early-return — `_updateGameplayPre` may still own the frame, but `renderPostFX()` runs unconditionally so the overlay always sees the latest source-canvas contents. `_drawGameplayMid/Post` stay skipped when the cinematic owns the frame so the cinematic's own canvas draws aren't overwritten.
- **Pause-menu upgrade log: Time hero picks showed raw ids (`time_fast_forward`, `time_reverse`) and duplicates listed N times instead of grouped**. `LevelUpUI.chooseUpgrade()` resolved the title via `_upgradeTitle(id)`, which only checks the built-in map + static `HERO_LOGIC[type].upgradePool`. TimeHero injects its options dynamically via `getCustomLevelUpOptions()` ([dlc/echos_of_eternity/TimeHero.js:629](dlc/echos_of_eternity/TimeHero.js#L629)) and has no `upgradePool`, so the lookup fell through and stored the raw id. Fix: card click now passes `displayOpt.title` straight into `chooseUpgrade(type, player, displayTitle)` so the rendered label is what gets logged; `_upgradeTitle` stays as the fallback. Pause menu `renderPauseMenu()` in [game.js:2737](game.js#L2737) now buckets `upgradesPicked` by title and renders `N× Label` chips (count omitted for 1×), keeping the existing 24-entry cap on the grouped list.
- **Sound / Chance / Void / Air heroes all had silently-dropped projectiles — same ECS-proxy `_push` root cause as Spirit and Lightning**. Audit across all DLC + base hero files turned up four more files still using the pre-#5-phase-5.10b `projectiles.push(plainObject)` / `projectiles.push(new Class(...))` pattern: [dlc/symphony_of_sickness/SoundHero.js](dlc/symphony_of_sickness/SoundHero.js) (5 sites — base NOTE attack, Sync Wave fan, Grand Finale notes, Grand Finale 8-way Omni burst, CRESCENDO_RING expanding shockwave), [dlc/faith_of_fortune/ChanceHero.js](dlc/faith_of_fortune/ChanceHero.js) (1 site — every DICE / EXPLOSIVE_DICE shot), [dlc/champions_of_chaos/VoidHero.js](dlc/champions_of_chaos/VoidHero.js) (1 site — every GLITCH_BOLT main shot), [dlc/waker_of_winds/AirHero.js](dlc/waker_of_winds/AirHero.js) (2 sites — regular-form COMPASS WIND_BURST and ZEPHYR transform-form storm push). Every one of those shots was dropped silently — muzzle audio fired but nothing hit. **Fix:** all 9 spawn sites converted to `Projectile.acquire(x, y, {x,y}, damage, color, radius, type, knockback, isEnemy, [isExplosive], [isCrit])` with slot-proxy setters (`life`, `pierce`, `owner`, `onHit`, `update`, `draw`); plain instance fields (`vx`/`vy`, `windStyle`, `face`, `rotation`, `spinSpeed`, `note`, `hitEnemies`, `isExplosive`, `isJackpotForm`, `isGlitch`) renamed underscore-prefixed (`_windStyle`, `_face`, `_rotation`, `_spinSpeed`, `_note`, `_hitEnemies`, `_isExplosive`, `_isJackpotForm`, `_isGlitch`) so the slot-proxy `_*` trap routes them through per-slot `projectileExtras`. Custom `update` closures now read velocity via `this.velocity.x` / `.y` (typed-array-backed) instead of stale `this.vx` / `this.vy`, and decrement `life` via `if (l !== null) this.life = l - 1` instead of the no-op `this.life--`. Custom `draw` closures use `this.radius` (slot) instead of the dead `this.size` (Void) and read `_windStyle` from extras. SoundHero CRESCENDO_RING expanding-shockwave preserves its self-managed collision loop in `update` (engine only fires `onHit` which returns `'STOP'`) and now reads `hitEnemies` via `_hitEnemies` extras. Added `Projectile` import to Sound, Chance, and Air (Void already had it). Smoke suite 261/261 stable; no other heroes in the DLC tree carry the broken pattern (audit complete — all remaining `projectiles.push(...)` sites already pass `Projectile.acquire` proxies which is a redundant but harmless no-op since the slot is allocated during `acquire`).
- **Lightning hero shot nothing — every bolt dropped on the floor, same root cause as the Spirit hero fix earlier in this Unreleased block**. [dlc/tournament_of_thunder/LightningHero.js](dlc/tournament_of_thunder/LightningHero.js) predated the ECS migration and spawned every projectile with `projectiles.push(new LightningProjectile(...))` (main shot in `LightningHero.shoot`, multishot/buff variants, `fireFlashOmniBurst` for the STORM special, and recursive `chain()` inside the class). `window.projectiles` is now a proxy sentinel whose `_push` is a no-op ([Entities/Projectile.js:228-232](Entities/Projectile.js#L228-L232)); slot allocation only happens inside `spawnProjectile`. Static Spark, Chain Lightning, Buff Multishot, extraProjectiles, STORM omni-burst, and every chain hop were silently dropped — only the muzzle `createExplosion` for the super shot was firing, which is why it presented as "lightning hero can't shoot" rather than "spawn skipped". **Fix:** all five `new LightningProjectile(...)` spawn sites routed through a new `_spawnLightningProjectile(opts)` helper that wraps `Projectile.acquire(x, y, {x,y}, damage, color, radius, type, knockback, isEnemy, isExplosive, isCrit)` and applies the slot-proxy setters for `life` / `owner` / `onHit` / `update` / `draw`. The class's instance methods (`generateSegments`, `update`, `onHit`, `hit`, `chain`, `findNextTarget`, `draw`) were converted to free `_lightning*` functions that take the slot proxy `p` so closures captured by `p.update`/`p.draw`/`p.onHit` can call them. Class-shaped fields (`isSuper`, `chainsLeft`, `range`, `ignored`, `canPierce`, `color`, `segments`, `_world`) renamed to underscore-prefixed extras (`_isSuper`, `_chainsLeft`, `_range`, `_ignored`, `_canPierce`, `_color`, `_segments`, `_world`) so the slot-proxy `_*` trap routes them through per-slot `projectileExtras` storage. Removed the per-frame manual `checkCollisions` failsafe — the engine collision pass at [core/updateGameplayMid.js:1098](core/updateGameplayMid.js#L1098) calls `proj.onHit` already, so the duplicate was wasted work. `onHit` returns `'STOP'` so the engine skips its default damage path (we apply damage manually with the Superconductor / crit / shock logic). `kill()` replaced with `p.life = 0` so the projectile loop splices on the next tick (`x = 999999` was a pre-ECS hack that no longer works against typed-array bounds). The legacy `class LightningProjectile` was deleted entirely — every consumer now goes through the helper, dead code carrying the broken pattern is gone.
- **Smoke hero BLACKOUT cloud (and every Smoke cloud) never rendered — only the two cast explosions were visible**. Root cause: [dlc/disciples_of_deception/SmokeHero.js](dlc/disciples_of_deception/SmokeHero.js) drew clouds, swirl rings and the INK STORM aura from inside `HERO_LOGIC.smoke.update` (called from `runState.player.update()` in [core/updateGameplayMid.js:96](core/updateGameplayMid.js#L96)). That runs in the **update** pass, but the very next thing the frame does is `_drawGameplayMid()` whose first action is `ctx.clearRect(0, 0, canvas.width, canvas.height)` at [core/drawGameplayMid.js:38](core/drawGameplayMid.js#L38) — wiping every smoke draw before the frame is presented. Pre-fix this was masked because dash drops were also subtle near-black `rgba(15,15,20)`; the burst was the only place where missing visuals were noticeable. Fix: `init` now also wraps `player.draw` to call a new `_drawWorld(player)` after the default sprite render, so clouds/shockwave/blind markers render in the **draw** pass while the camera transform is still active (player.draw is invoked from [core/drawGameplayMid.js:186](core/drawGameplayMid.js#L186) inside the camera `save/restore`). State updates (life decrement, status decay, damage application) stay in `update`. While in the file: brightened blackout cloud (ink-purple gradient `25,12,40` → `90,45,150`, alpha cap `0.75`, glowing outline + counter-rotating swirls, gentle pulse), brightened cast explosions (`#5a5a6e` → `#9a6bd6`, radii 60/45 → 70/50), added an expanding shockwave ring (30 frames, `200,150,255`) tracked in `player.blackoutShockwave`, and added a small purple `✕` over the head of any enemy with `_smokeBlind > 0`.
- **`floatingTexts.push` on undefined — plant / metal / black special crashed `useSpecial`**. Caught by the new hero smoke suite on first run. Since #5 phase 5.5 the `floatingTexts` array migrated to `runState.floatingText*` typed arrays and was removed from `World.constructor` ([shared/world.js:23](shared/world.js#L23)). `FloatingText.acquire(...)` self-spawns into the ECS, and `window.floatingTexts` got replaced with a no-op `push()` shim ([Entities/FloatingText.js:70](Entities/FloatingText.js#L70)). Most callsites use the safe `floatingTexts?.push(FloatingText.acquire(...))` pattern, but 22 sites in `Player.js` (across `useSpecial`, `onKill`, `update`) had unsafe `.push` — the destructure `const { floatingTexts } = _w ?? {}` returned `undefined` since `_w` lacks the field post-migration. plant (heal text), metal (invincible text), black (+50 HP text) tripped it unconditionally during special activation; the other 19 sites were gated behind hit/kill conditions so latent. Fix: sweep `floatingTexts.push(` → `floatingTexts?.push(` across Player.js — push was already a vestigial no-op (acquire self-spawns), so the `?.` is purely a guard against the missing field. Identical pattern still latent in `Enemy.js` (1 site), `Boss.js` (6), `EvilHeroes.js` (2) — will be swept when smoke phases 2–3 surface them.
- **Spirit hero projectiles never left the player — impact animation fired, but the shot itself was invisible / had zero range**. Same root cause as the online-ghost-projectile bug ([#5 phase 5.10b](#5)): `dlc/faith_of_fortune/SpiritHero.js` predates the ECS migration and spawned every projectile with `projectiles.push({...plain object with custom update/draw closures...})`. The post-migration `window.projectiles` Proxy sentinel's `_push` is a no-op ([Entities/Projectile.js:228-232](Entities/Projectile.js#L228-L232)) — slot allocation only happens inside `spawnProjectile`, never via array push. Every Spirit shot (MANTRA), Lotus VINE, Divine Radiance SPIRIT_AURA, and Transcendence ult SPIRIT_AURA was silently dropped on the floor. The muzzle-flash `createExplosion` still ran (hence the impact-on-self look), so the bug presented as "range = 0" rather than "spawn skipped". **Fix:** five plain-object pushes in `SpiritHero.js` routed through a one-shot `_spawnSpiritProjectile(opts)` helper that wraps `Projectile.acquire(x, y, {x,y}, damage, color, radius, type, knockback, isEnemy, isExplosive, isCrit)` and applies the slot-proxy setters for `life` / `pierce` / `owner` / `onHit` / `update` / `draw`. MANTRA's custom `update` (which only did the base `pos += vel; life--`) deleted so the system's `_baseUpdate` runs — zero closure allocation per shot. VINE same (`vx=vy=0`, base update is a no-op for position and still decrements life). MANTRA `draw` reads `this.velocity.x / .y` instead of stale `this.vx / .vy` so tail rotation tracks the typed-array velocity through the proxy. The two SPIRIT_AURA overrides (Divine Radiance unlock + Transcendence ult) kept custom `update` for owner-follow + angle increment, but renamed `this.angle → this._auraAngle` so the slot-proxy `_*` trap routes it through per-slot `projectileExtras` storage. Aura termination switched from `this.dead = true` to `this.life = 0` — the main projectile loop only splices on `proj.life <= 0` outside the PVP branch, so the original `dead = true` was a pre-existing no-op leak even before ECS. Unused `projectiles` destructures dropped from four `_w` deconstructs. ParityTest 127/127 + Vitest 90/90 stable. 0 new lint errors.
- **Completion menu rendered blank** (ESM regression) — `UI/Completion.js` gated on `typeof CompletionMenu !== 'undefined'`; added explicit import; new `resetFilter()` method preserves filter-reset behaviour.
- **HUD layout editor resized P2 HUD instead of moving it** — `el.style.right = ''` only cleared inline; stylesheet `right: 20px` still applied. Switched to `el.style.right = 'auto'`.
- **Mindscape + Smog Quarter biome zones drew as flickering white boxes** — added early-return cases in `BiomeZone.draw()` for `FRACTURE` + `SMOG_POCKET` (and later `LIGHT_SHAFT`/`BLOOM_PATCH`/`DREAM_POCKET`).
- **Gravity hero attack SFX spam** — throttled 250 ms.
- **Custom map biome override + layout reset every wave** — Workshop mode now excluded from biome rotation and calls `arena.generateFromMap()` per wave.
- **Hero projectiles render as plain circles in multiplayer** — `type` field included in server snapshot.
- **Battle theme in online lobby / global lobby** — `ONLINE_LOBBY`/`GLOBAL_LOBBY` added to `AudioManager.menuStates`.
- **DLC modules fail in Electron `file://`** — `new URL(rel, document.baseURI).href`.
- **Map Workshop loading screen z-index** (4000 → 10000) above conflict modal.
- **TimeHero crash on game start** — `saveData` undefined; optional chaining.
- **Radiance of Ruin various** — biomeZone red-box (early-return for new zone types); default grey obstacles in RoR biomes (`ownsObstacles` + `noTraps` flags + trap obstacle inherits `biomeType`); `onKill` enemy null-guards; Mask/HP drained on shoot/melee *call* not actual fire (deduct only when projectile/cooldown changed); biomes hid player on start (split `drawBackground` vs `draw`).
- **Speedrun unlock + PB follows story-completion group logic** (base heroes share, gravity↔void, spirit↔chance).
- **Options "Show Intro Screens" label** unambiguous.
- **Options toggles broken** — `Config.loadConfig` reassigned `gameConfig`; now uses `Object.assign` to mutate in place.
- **Audit pass (2026-05-10) — bugs across core/DLC/networking/UI**: Player.js altar cooldown reductions hoisted out of `chance` branch; 11 `forEach + arr.splice(index)` index-skip bugs converted to backwards `for`; Companion synergy beam split logic/render; Boss.js DLC saveData via optional chaining; TimeBosses `spawnProj` velocity arg shape; ChanceHero Windfall per-frame multiplication; SpiritHero `shootMantra` onHit `e` → `enemy`; JACKPOT skipped `onKill`; Spirit Tranquility never restored slow; AirHero Storm buff was permanent (now gated); WindStory injected duplicates; StoryManager mutated shared events (clone); SkillTree hero-tree seed collision (length → string hash); Achievements `innerHTML` → `textContent`; Enemy bare `wave` → `_wave`; AURA_HEAL cap; `combo > 50` `goldMultiplier` overwrite (now `_goldMultBase` baseline); `LoveHero` `Math.max` `NaN`; `TimeHero` dead burnTick; `GravityHero` 60 writes/sec → 1/sec; `SmokeHero` cloud cap unified.
- **`shadeColor` `'#XXYYNaN'` on `#RGB` shorthand** — expand to `#RRGGBB` + validate channels.
- **PoisonHero `saveData.altar` access guarded**.
- **InputManager crash on synthetic key events** (`e.key` undefined) — guard.

**Online netcode**
- **Online player projectiles invisible** — `_onlineApplySnapshot` rebuilt projectiles via `_replaceArrInPlace` which called `length = 0` on the ECS sentinel (`clearProjectiles`) + no-op `push`. Rewrote rebuild as reconcile-by-id over slot pool with `Projectile.acquire` for new ghosts and `_orphanAt` for snapshot-dropped slots.
- **Multiple online netcode regressions** — rubber-band on key release; projectiles dying mid-flight (carry orphaned ghosts via `_orphanAt`); ghost partner wobble (normalized input + hero speed); arena flip / canvas transform corruption (`ctx.setTransform(identity)` + `??` instead of `||` in ghost extrap + safe defaults on radius/color/sides).
- **Online versus: permanent invincibility after first hit** — `isInvincible` never cleared; cleared in `_tick()` when `invincibleTimer` reaches zero.
- **Online: different biomes per player** — shared lobby seed + deterministic hash for biome selection + seeded Mulberry32 around `arena.generate()`.
- **Online: P2 HUD stuck on screen** after game end.
- **Online: hero selection mid-lobby applied to local player**.
- **Online: chaos mode hidden** in pre-game (not supported in any co-op).
- **Online Co-op: ghost enemies permanently white** — short-circuited collision block for ghosts (no flash/damage/score).
- **Online Co-op: revive marker → game over** — outer death check guarded by `!player.isDead`; both-dead handled separately.
- **Online 2P Story** — guest uses host's hero type for story event lookups.
- **Online 2P mode-select wrong heroes** — `NetworkManager._dispatch()` now handles `PRE_GAME` (sets role/lobbyCode/phase).
- **Online 2P guest always 'water'** — `JOIN_LOBBY` ignored `msg.hero`; now applied.
- **Online debug sim `_onlineFrame is not defined`** — counter was dead code; removed declaration + increment + loader stub.
- **Server `send(ws, msg)` no try/catch** — wrapped + `terminate()` on failure.
- **`GameSession._giveXP` only granted one level on multi-level XP gains** — `while` loop with safety cap.
- **Pending action latches not cleared after level-up modal** — held shoot/melee auto-fired on resume.

**Admin / server**
- **Admin dashboard "Create Event" `Document.createEvent` collision** — renamed helper to `createWorldEvent` to dodge inline `onclick` `with (document)` scope shadow.
- **Admin dashboard Events tab date inputs reset mid-typing** — `render()` short-circuits `innerHTML` rewrite when `document.activeElement` is editable inside active tab; switched `datetime-local` to `type="text"` with `pattern` since partial entries can't snapshot.
- **Telemetry POST blocked by CORS preflight** — added `credentials: true` to `cors()` middleware; sendBeacon spec requires it.
- **CrashReporter URL `_baseUrl()` mirrors CloudSaveManager** — bare IP no longer relative.
- **`SaveManager` not defined in CloudSaveManager** — `window.SaveManager = SaveManager`.

**ESM / refactor regressions**
- **`killEnemy` use-after-overwrite race** (`core/systems/enemySystem.js`) — cleanup `_slot = -1` ran on the same proxy ref that had just been moved into slot `i`. Capture `dyingProxy` before swap; only mark `-1` if not the same object now serving slot `i`. Same fix applied to `killProjectile`.
- **Multiple bare-name `ReferenceError`s after leaf-module extraction** — bridges added to `window.X` block for `_renderBossIntroCinematic`/`_renderBossDeathCinematic`/`_renderBossChoiceScreen`/`Enemy`/`_replaceArrInPlace`/`EvilMode`/`_SPATIAL_HASH_MIN`/`Projectile`/`GoldDrop`/`HolyMask`/`_recordPhase`/`getCollectionBonuses`/`_onlineInterpBuf`/`_onlineRenderTime`/`_renderMinimap`/`MeleeSwipe`/`PowerUp`/`MEMORY_STORIES`/etc.
- **`ReferenceError: ACHIEVEMENTS is not defined`** at Player.js:1557 — added explicit `import { ACHIEVEMENTS } from './Constants.js'` to Player.js + game.js; Constants.js window-mirror retained for classic-script DLCs.
- **`ReferenceError: _bossChoiceFrame is not defined`** — mixed bare-name + `runState.` qualifier; one-character fix at game.js:6312.
- **`TypeError: proj.draw is not a function`** at first projectile spawn — ECS slot proxy's `get draw()` returned raw `projectileDrawFn[i]` (or null); mirrored the `get update()` base-tick fallback closure pattern.
- **Cannot access `runState` before initialization** — `saveRunState()` local `const runState = {…}` shadowed module-singleton through TDZ window; renamed to `payload`.
- **`isPhotoMode is not defined`** — `core/updateGameplayPre.js` bare-name lookup; added explicit `import { isPhotoMode } from '../Camera.js'`.
- **Server-sim `runState` singleton ECS slot leak across sequential `GameSession` instances** — new `_resetEcsState()` zeros every ECS slot count + clears `bossInstances` + nulls cached proxy refs; bridge `_tick` sub-steps `bridge.runUpdate` by `Math.max(1, Math.round(_currentTickFrames))` to match renderer pace.
- **ESM ReferenceErrors on game start** — `masksDroppedInWave` / `waveTimer` undeclared; `Object.defineProperty` getter/setter pairs on `window.wave`/`bossActive`/`enemiesKilledInWave`/`isPlayerDying`/etc. so DLC write-back lands on live module vars.
- **DLC bare-global vars not on window** — 9 vars (`companions`/`projectiles`/`particles`/`currentObjective`/`currentWeather`/`activeMutators`/`isDailyMode`/`isWeeklyMode`/`isChaosShuffleMode`) routed through defineProperty bridges.
- **`TutorialMode`/`EvilMode` bare function globals not on window** — 6 functions exposed.
- **Enemy draw crash `player is undefined`** — `window.player = player` synced at every reassignment site.
- **Player.js dead `window.TutorialMode` + `window._syncSoundBiomeMusic` guards** — explicit imports added; tutorial dash/ability progress + Sound music sync now actually fire.
- **`tasks/saveManager.test.js` failed to import** — `Managers/SaveManager.js:415` `window.SaveManager = SaveManager` unguarded; wrapped in `typeof window !== 'undefined'`.
- **Audio: hero exclamations volume 1.0 + 10 % music duck (1.1.0 carry)**.

### Removed
- **`server/simulation/WaveManager.js`** retired (bridge owns spawn).
- **Server-sim legacy `_tick` body + helpers retired** — `GameSession.js` 851 → 679 LOC; `_processMeleeAttacks` / `_updateProjectiles` / `_applyEnemyContactDamage` / `_checkWaveAdvance` / `_useBridge` flag all deleted; bridge path canonical.
- **Floating clock-face shards** from Dreamspace.
- **9 `window.X = X` mirror writes** + **13 `_world.*` resync sites** dropped (RunState carve cleanup).

### Security
- **Admin dashboard bcrypt + JWT session** — extracted `server/adminAuth.js`; plain `ADMIN_PASSWORD` no longer honored; `POST /api/admin/login` 5/15 min rate-limit; 8h JWT; `requireAdmin` rejects non-`kind: 'admin'`. `scripts/admin-hash.js` helper. `tests/adminAuth.test.js` (18 assertions).
- **JWT_SECRET fail-secure on prod boot** — exits 1 with FATAL when secret is placeholder default; mirrors `ADMIN_PASSWORD_HASH` pattern.
- **Admin login audit log** — `admin_login_attempts` table; all five outcome paths logged; rate-limit case loggable via `onReject(req)` hook on `rateLimit({})`. Admin endpoint `GET /api/admin/login-attempts?success=…` + Technical-tab 4-card summary (logins / bad-pass / rate-limited / unique IPs over 24 h).

## [1.1.0] - 2026-04-25

### Added
- **Info dialogue system**: Generic queued info dialogue that appears on main menu launch; controller-compatible (navigate with stick/D-pad, A to activate, B to close); dismissable via "Don't show this again" checkbox persisted in config; plays menu music seamlessly; new dialogues can be registered via `infoDialogueManager.register()`
- **"Expansions Available" info dialogue**: First-launch popup pointing players to the Expansions section; can be permanently dismissed
- **Sound Hero — GRAND FINALE ultimate**: PERFORMER form now triggers a full musical assault: shots become a 5-way (7-way on-beat) sonic fan at 2.5×–3.5× damage; every beat auto-fires an 8-way omnidirectional note burst; orbiting musical notes deal close-range AoE damage every 30 frames; lasts 10 seconds
- **Gravity Hero — DARK STAR ultimate**: Gravity Hero now has the DARK STAR form: activation jolts all enemies inward; gravity pull range triples with 5× force; enemies within 80px take 15 DPS × damageMultiplier; spinning purple accretion disk ellipses render around the player; deactivates on damage (standard rule)
- **Poison Hero — PANDEMIC PROTOCOL ultimate**: PLAGUEBRINGER form now activates a true pandemic: all living enemies are instantly infected to 100 poison stacks on activation; turbo stack field applies +10 stacks/10 frames in radius 400; poison DoT ticks 3× faster (every 10 frames); plague explosions chain on poison kills; player leeches 5% of all poison DoT dealt; lasts 15 seconds
- **Air Hero — STORM INCARNATE ultimate**: ZEPHYR form fires all 4 compass attack styles simultaneously (scatter shotgun, sniper lance, rapid blades, vortex orb) per shot; hurricane ring damage boosted 7.5× (15 × damageMultiplier); activation triggers a 5-point wind burst; lasts 10 seconds
- **Lightning Hero — ABSOLUTE DISCHARGE ultimate**: FLASH form activates with an 8-way super lightning omni-burst; static charge generates 3× faster; every time charge fills to 100% an 8-way super omni-burst auto-fires; speed boost to 1.6× while active; lasts 10 seconds
- **Chance Hero — ALL IN ultimate**: JACKPOT form forces all dice to be explosive (always `allMatch`); explosive hits have a 25% chance to chain-bounce to the nearest enemy for 75% damage; slot machine auto-fires every second with a guaranteed JACKPOT outcome; 4 lucky symbols orbit the player; lasts 10 seconds
- **Spirit Hero — DIVINE RADIANCE ultimate**: Level-10 ENLIGHTENED form fully heals the player and fills Inner Peace; purification aura boosted to 40 × damageMultiplier every 10 frames at radius 250 (vs. 20 × mult / 15 frames / radius 150 from special); each aura pulse heals 0.5 HP; shots become Sacred Beams (pierce 10, 3× damage, 2× speed); Inner Peace drains at 0.2/frame for longer duration
- **Void Hero — SYSTEM COLLAPSE ultimate**: ENTROPY form instantly marks all living enemies as glitched; emits expanding corruption wave rings every 2 seconds (40 × damageMultiplier to all enemies within 300px); System Corruption AoE boosted to every 8 frames with 20% instant-delete for non-boss enemies below 50% HP; on form end, all remaining glitched enemies detonate for 200 × damageMultiplier; lasts 10 seconds
- **Poison visual on enemies**: Enemies with active poison stacks now show a green body tint, a glowing pulsing toxic ring, and rising bubble drips — all scaling with stack intensity (0–100) so lightly and heavily poisoned enemies are clearly distinguishable
- **Hit flash on enemies**: Enemies briefly flash white when hit (6-frame fade), giving instant visual confirmation on every shot and melee strike
- **Enemy death burst**: Regular enemies emit a small directional 8-particle burst in their own color on death
- **Hit stop**: Significant hits (melee: 2 frames, crit projectile: 3 frames, melee crit: 4 frames) freeze enemy and projectile movement for classic fighting-game impact feel
- **Combo milestone animation**: Reaching combo x10, x25, x50 or x100 triggers a scale-bounce and color pop on the combo counter
- **Low health heartbeat**: The health bar now persistently pulses at <25% HP (slow) and <10% HP (fast panic-rate); canvas vignette also activates earlier (25% vs 20%) with pulse speed scaling down with HP

### Fixed
- Weather effects now always reset at the start of each new wave instead of persisting into the next wave
- Gravity Hero no longer plays the shoot sound or fires a projectile while in DARK STAR ultimate form
- Memory shard pickups now display the correct color for every hero type (all DLC heroes — earth, lightning, gravity, void, spirit, chance, time, love, air — were previously defaulting to white)
- Time and Love heroes now render in their correct colors in the Museum (player avatar and entity sprites)
- Void Hero special icon now correctly shows 👻 (was showing default icon because the override applied after the first setup call)
- Void Hero ENTROPY ultimate now correctly marks all enemies as glitched and runs its 10-second form; the `chooseUpgrade` path was bypassing `HERO_LOGIC.applyUpgrade`, so the timer and glitch-marking logic never executed
- Void Hero biome obstacles now render with the cyan fractured-reality style instead of default stone (FracturedBiome was creating obstacles without a `biomeType`)
- Hero exclamations are now clearly audible over music — volume raised to 1.0 and background music ducks to 10% while a line plays, then restores
- DLC Expansions menu no longer plays the battle theme — `'DLC'` added to the menu state list in AudioManager

### Changed
- Developer logo intro screen now plays an animated video (`developer_animated.mp4`) instead of a static image
- Title background in the press-start screen now plays a looping animated video (`title_animated.mp4`) instead of a static image

## [1.0.3] — 2026-04-19

### Added
- Automatic update check on launch: fetches the latest GitHub release and shows a dialog if a newer version is available, with a direct download button

### Fixed
- Spirit Hero's charging sound loop no longer continues playing after pausing the game
- Boss defeated screen (story mode) is now controller-navigable on Windows Electron: gamepad detection now uses `isRealGamepad()` to skip USB receivers at index 0
- Boss defeated screen no longer persists over the story chapter screen after selecting Continue: canvas is cleared immediately on dismissal and the render loop early-exits when the screen is dismissed mid-frame

## [1.0.2] — 2026-04-19

### Added
- Electron file logging (`game.log`) is now toggled by a `LOGGING_ENABLED` constant in `index.js` (disabled by default)
- README now includes Windows build instructions; GitHub Actions workflow file renamed from `build-windows.yml` to `build.yml`

### Changed
- Standard boss enemies (Rhino, Speedster, Summoner, Nova, Hydra) now face the direction they are moving instead of spinning around their own axis
- DLC enabled-state is now stored in `dlcs.json` (Electron) / `localStorage` (web), mirroring the Config.js pattern; all DLCs are disabled by default on first run and must be explicitly enabled

### Fixed
- Entering any game mode with a controller connected no longer crashes: `PlayerController.js` now uses optional chaining (`?.pressed`) on all gamepad button accesses so gamepads with fewer than 10 buttons are handled safely
- Controller inputs in-game no longer go dead when a USB receiver occupies gamepad index 0: `HumanController` now uses `isRealGamepad()` to find the first real controller, consistent with menu navigation
- Completion menu no longer crashes (showing only the toggle and back button) on first run: `CompletionMenu.calculateProgress()` now uses optional chaining when reading `.prestige` for altar convergence nodes whose hero slots may not yet exist in `saveData`
- "Continue Run" no longer opens the shop when resuming a saved run on a wave that follows a shop wave (e.g. wave 5 after the wave-4 shop)

## [1.0.1] — 2026-04-17

### Added
- Story intro screen is now gamepad-compatible: pressing A skips it, matching the press-start screen behaviour
- Skip hint on story intro screen updated to mention A button
- Bug report overlay closes with gamepad B button (tracks `BUGREPORT` UI state)
- Manual closes with gamepad B button (fixed broken `closeTutorial` reference → `Manual.close()`)
- Evil Mode button and all other main-menu button types now show the yellow gamepad-focus ring when selected (added `.selected` overrides for `menu-mode-btn`, `menu-lib-btn`, `menu-challenge-btn`, `menu-continue-btn`, `menu-sys-btn`)
- Symphony of Sickness DLC: added Collector Cards for Toxic Crawler, Speedster, and Shadow Clone enemy types (`injectCards`)

### Fixed
- Story Mode button now shows yellow controller-selection outline when navigated to with a gamepad (was hidden by `border: none !important`)
- Windows installer now uses the correct app icon (`images/icons/win/icon.ico`) instead of the default Electron icon; icon path is now platform-aware in `forge.config.js`
- Installed app on Windows now correctly shows `5 Freunde: Elemental Arena` as product name instead of `5-freunde-arena`
- USB receivers and other non-controller HID devices are no longer mistaken for gamepads; `isRealGamepad()` filter (≥10 buttons, ≥2 axes) is applied in menu navigation, co-op assignment, and the `gamepadconnected` event handler
- Lightning Hero: `specialMaxCooldown` corrected from 15000 to 900 frames (was effectively a 4-minute cooldown instead of 15 seconds)
- Time Hero: `rangeCooldown` and `meleeCooldown` now respect `cooldownMultiplier` on assignment (cooldown reduction upgrades had no effect)
- Time Hero: CPU/co-op AI no longer returns `null`; now uses standard target-following behaviour
- Symphony of Sickness DLC: beat loop `setInterval` now clears any previous interval before creating a new one, preventing stacked ticks on reload
- Air Hero, Poison Hero, Sound Hero: special-icon DOM access is now guarded by `!player.isCPU`, preventing errors in CPU/co-op contexts

## [1.0.0] — 2026-04-17

### Added
- Completion menu: "Hide Completed" toggle button filters out 100% categories and sub-categories; controller-compatible
- Version number displayed subtly in the bottom-right of the main menu footer; single source of truth is `APP_VERSION` in `Constants.js`
- Boss-win audio exclamation plays for the winning hero when defeating an opponent in versus mode (all four KO code paths: AI-opponent projectile, AI-opponent melee, 2P projectile, 2P melee)
- GitHub Actions workflow
- Level-up sounds
- Bug report form
- SFX for all weather effects
- Themed obstacles per biome
- Earth hero impact sound
- Controller vibration and screen shake effects
- Evil Mode with villain exclamations and memory shards
- All hero exclamations (boss moments, wins, failures, level-up, found)
- Pickup SFX for all items
- Improved menu design
- Story-relevant achievement gating
- AI Companion mode
- Chaos Shuffle mode
- Weekly Challenge mode
- 2-Player Co-op and Versus modes
- DLCs: Rise of the Rock, Waker of Winds, Tournament of Thunder, Symphony of Sickness, Faith of Fortune, Champions of Chaos, Echos of Eternity
- Maze of Time (Echos of Eternity story dungeon)
- Skill Tree, Void Shop, Chaos Shop, Altar, Museum, Collection, Achievements, Statistics screens
- Daily Challenge mode
- Save import/export

### Changed
- Manual: expanded Game Modes section with entries for Versus Mode, Co-op Mode, Chaos Shuffle Mode, and Evil Mode

### Fixed
- Completion menu BACK button now appears directly below the content instead of being pushed to the bottom of the screen
- Completion menu now scrolls as a whole page (no inner scrollbar) and expanded categories scroll into view automatically
- Version number and copyright text no longer overlap in the main menu footer
